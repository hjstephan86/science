\documentclass[a4paper,12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[german]{babel}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}

\geometry{margin=2.5cm}

% ---------- Ãœberlaufschutz ----------
\sloppy
\emergencystretch=3em
\setlength{\tolerance}{2000}
\setlength{\hbadness}{10000}

\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem{beispiel}{Beispiel}
\newtheorem{satz}{Satz}
\newtheorem{lemma}{Lemma}
\newtheorem{bemerkung}{Bemerkung}

\setlist[itemize,1]{label=--}

% ---------- Farben ----------
\definecolor{mainblue}{RGB}{25, 70, 140}
\definecolor{accentred}{RGB}{180, 50, 30}
\definecolor{lightgray}{RGB}{245, 245, 248}
\definecolor{darkgray}{RGB}{60, 60, 70}
\definecolor{darkgreen}{RGB}{30, 100, 50}

\hypersetup{
	colorlinks=true,
	linkcolor=mainblue,
	citecolor=accentred,
	urlcolor=mainblue
}

%  Code-Highlighting
\lstset{
	basicstyle=\ttfamily\small,
	keywordstyle=\color{blue},
	commentstyle=\color{gray},
	stringstyle=\color{red},
	numbers=left,
	numberstyle=\tiny,
	breaklines=true,
	breakatwhitespace=false,
	frame=single,
	columns=fullflexible,
	literate=%
	{Ã–}{{\"O}}1
	{Ã„}{{\"A}}1
	{Ãœ}{{\"U}}1
	{ÃŸ}{{\ss}}1
	{Ã¼}{{\"u}}1
	{Ã¤}{{\"a}}1
	{Ã¶}{{\"o}}1
	{Î£}{{$\Sigma$}}1
	{Ïƒ}{{$\sigma$}}1
	{Î´}{{$\delta$}}1
	{â†’}{{$\rightarrow$}}1
	{âŠ†}{{$\subseteq$}}1
	{âŠ‡}{{$\supseteq$}}1
	{â‰¥}{{$\geq$}}1
}

\title{Sequenzgebundene REST-Schnittstellen (SC-REST):\\
Ein formales Erweiterungsmuster fÃ¼r die Abbildung\\
von GeschÃ¤ftsprozessen auf REST-APIs}
\author{Stephan Epp}
\date{15. Juni 2026}

\begin{document}

\maketitle

\tableofcontents
\newpage

\section{EinfÃ¼hrung}

REST (\emph{Representational State Transfer}) ist nach Fielding~\cite{fielding2000} ein
Architekturstil, dessen zentrales Prinzip die \emph{Zustandslosigkeit} (\emph{Statelessness})
zwischen aufeinanderfolgenden Anfragen ist: Jede Anfrage muss alle zu ihrer Bearbeitung
notwendigen Informationen enthalten, der Server hÃ¤lt keinen Sitzungszustand zwischen
Requests vor~\cite{rfc7231}. Dieses Prinzip ist wesentlich fÃ¼r Skalierbarkeit, Cachebarkeit
und die unabhÃ¤ngige Entwicklung von Client und Server.

In der praktischen Anwendung treten jedoch hÃ¤ufig \emph{GeschÃ¤ftsprozesse} auf, die aus
fachlicher Sicht eine \emph{verbindliche Reihenfolge} von API-Aufrufen erfordern: Ein
Warenkorb muss erstellt werden, bevor Artikel hinzugefÃ¼gt werden kÃ¶nnen; eine Zahlung kann
erst nach Abschluss des Checkout-Vorgangs ausgelÃ¶st werden; eine Bestellung kann erst
bestÃ¤tigt werden, nachdem die Zahlung autorisiert wurde. Die klassische REST-Architektur
trifft hierzu keine normative Aussage: Ein zustandsloser Server kann grundsÃ¤tzlich nicht
unterscheiden, ob eine Anfrage â€žzur falschen Zeitâ€œ eintrifft, sofern die Anfrage selbst
syntaktisch und semantisch valide ist.

\subsection{Problemstellung}

Gegeben sei ein GeschÃ¤ftsprozess, der durch eine Menge von API-Operationen
$\Sigma = \{\sigma_1, \ldots, \sigma_m\}$ (HTTP-Methode und Ressourcenpfad) realisiert wird.
FÃ¼r eine konkrete \emph{Prozessinstanz} (z.\,B. eine Bestellung, einen Antrag, einen
Vertragsabschluss) existiert eine Menge \emph{zulÃ¤ssiger Aufrufsequenzen}
$c = (\sigma_{i_1}, \sigma_{i_2}, \ldots, \sigma_{i_k}) \in \Sigma^*$.

\textbf{Ziel:} Definiere eine formale, REST-kompatible Erweiterung, mit der ein Server
\begin{itemize}
	\item jede eingehende Anfrage gegen die fÃ¼r die jeweilige Prozessinstanz aktuell
	      zulÃ¤ssigen Operationen prÃ¼ft,
	\item bei Abweichung von der vereinbarten Sequenz eine eindeutige Fehlermeldung liefert,
	      ohne die Zustandslosigkeit auf Protokollebene (jede Anfrage bleibt vollstÃ¤ndig
	      selbstbeschreibend) aufzugeben,
	\item dem Client mittels \emph{Hypermedia as the Engine of Application State}
	      (HATEOAS) jederzeit die Menge der zulÃ¤ssigen Folgeoperationen mitteilt,
	\item die Kenntnis der wahrscheinlichen Folgeoperation(en) zur \emph{spekulativen
	      Vorbereitung} (Prefetching, Vorberechnung, Caching) nutzt.
\end{itemize}

\subsection{Motivation}

Drei Beobachtungen motivieren diese Arbeit:

\begin{enumerate}
	\item \textbf{FÃ¼hrung des Clients.} Ein Client, der eine ungÃ¼ltige Operation
	      versucht, erhÃ¤lt sofort eine maschinenlesbare Fehlermeldung mit der Menge der
	      tatsÃ¤chlich zulÃ¤ssigen Folgeoperationen. Dies reduziert Implementierungsfehler
	      und Dokumentationsaufwand, da der Prozess \emph{im Protokoll selbst} kodiert ist.
	\item \textbf{FÃ¼hrung des Servers.} Da der Server zu jedem Zeitpunkt $T_{\text{think}}$
	      des Clients die Menge der \emph{mÃ¶glichen} FolgezustÃ¤nde $N(s)$ kennt, kann er
	      Ressourcen, Datenbankabfragen oder Berechnungen fÃ¼r diese ZustÃ¤nde bereits wÃ¤hrend
	      der Bedenkzeit des Clients vorbereiten.
	\item \textbf{Strukturelle NÃ¤he zum Subgraph-Problem.} Die PrÃ¼fung, ob eine beobachtete
	      Aufrufsequenz mit einem vorgegebenen Prozessautomaten konform ist, lÃ¤sst sich als
	      Spezialfall eines Graph-Enthaltenseins-Problems auffassen. FÃ¼r Prozessphasen mit
	      \emph{permutierbaren} optionalen Teilschritten erweist sich der in
	      \cite{epp2026subgraph} entwickelte Subgraph Algorithmus mit zyklischer Rotation
	      als das strukturell passende und nachweislich optimale Werkzeug.
\end{enumerate}

\subsection{Beitrag dieser Arbeit}

Diese Arbeit liefert:
\begin{itemize}
	\item eine formale Definition eines \emph{Prozessautomaten} als Erweiterung des
	      REST-Architekturstils (Abschnitt~\ref{sec:grundlagen}),
	\item die Definition des \emph{SC-REST-Patterns} (\emph{Sequence-Constrained REST})
	      mit konkreter HTTP-Semantik, einschlieÃŸlich eines neuen Problem-Detail-Typs
	      gemÃ¤ÃŸ RFC~7807~\cite{rfc7807} (Abschnitt~\ref{sec:pattern}),
	\item den formalen Nachweis von Korrektheit (Soundness/Completeness),
	      LaufzeitkomplexitÃ¤t und OptimalitÃ¤t der SequenzprÃ¼fung
	      (Abschnitt~\ref{sec:analyse}),
	\item eine formale Anbindung permutierbarer optionaler Teilschritte an den Subgraph
	      Algorithmus aus~\cite{epp2026subgraph}, einschlieÃŸlich eines neuen
	      Zustandsraum-Explosions-Lemmas (Abschnitt~\ref{sec:permutierbar}),
	\item ein formales Latenzmodell fÃ¼r serverseitiges Prefetching mit Beweis des
	      Latenzgewinns (Abschnitt~\ref{sec:prefetch}),
	\item eine Referenzimplementierung in Python (Abschnitt~\ref{sec:implementierung}),
	\item eine Veranschaulichung aller Ergebnisse durch matplotlib-Diagramme,
	\item einen ausfÃ¼hrlichen Ausblick auf Standardisierung, verteilte Prozesse,
	      Verifikation und adaptive Vorhersagemodelle (Abschnitt~\ref{sec:ausblick}).
\end{itemize}

\section{Grundlagen}
\label{sec:grundlagen}

\subsection{REST und Hypermedia}

REST-APIs adressieren \emph{Ressourcen} Ã¼ber URIs und manipulieren diese mittels einer
einheitlichen Schnittstelle aus HTTP-Methoden (GET, POST, PUT, PATCH, DELETE). Im
\emph{Richardson Maturity Model}~\cite{fowler2010} bildet \emph{HATEOAS} die hÃ¶chste Stufe:
Antworten enthalten Links auf zulÃ¤ssige Folgeoperationen, sodass der Client nicht auf
statische, externe Dokumentation angewiesen ist. SC-REST baut unmittelbar auf dieser Stufe
auf, erweitert sie jedoch um eine \emph{normative, serverseitig durchgesetzte} Komponente:
Die in den Links angegebenen Folgeoperationen sind nicht nur \emph{VorschlÃ¤ge}, sondern die
\emph{einzigen} Operationen, die der Server fÃ¼r die betreffende Prozessinstanz akzeptiert.

\subsection{Formale Definitionen}

\begin{definition}[Operation]
	Eine \emph{Operation} $\sigma = (M, P)$ besteht aus einer HTTP-Methode
	$M \in \{\text{GET}, \text{POST}, \text{PUT}, \text{PATCH}, \text{DELETE}\}$ und einem
	Ressourcenpfad-Muster $P$. Die Menge aller im Prozess verwendeten Operationen wird mit
	$\Sigma$ bezeichnet (das \emph{Operationsalphabet}).
\end{definition}

\begin{definition}[Prozessautomat]
	\label{def:prozessautomat}
	Ein \emph{Prozessautomat} ist ein Tupel
	\[
		P = (S, \Sigma, \delta, s_0, F)
	\]
	mit:
	\begin{itemize}
		\item $S$: endliche Menge von \emph{ProzesszustÃ¤nden},
		\item $\Sigma$: endliches Operationsalphabet,
		\item $\delta: S \times \Sigma \rightharpoonup S$: \emph{partielle} (deterministische)
		      Ãœbergangsfunktion,
		\item $s_0 \in S$: Startzustand,
		\item $F \subseteq S$: Menge der \emph{EndzustÃ¤nde} (abgeschlossene Prozessinstanzen).
	\end{itemize}
	FÃ¼r $s \in S$ heiÃŸt
	\[
		N(s) := \{\, \delta(s,\sigma) \mid \sigma \in \Sigma,\ \delta(s,\sigma) \text{ ist definiert} \,\}
	\]
	die Menge der \emph{erreichbaren FolgezustÃ¤nde} und
	\[
		\Sigma(s) := \{\, \sigma \in \Sigma \mid \delta(s,\sigma) \text{ ist definiert} \,\}
	\]
	die Menge der in $s$ \emph{zulÃ¤ssigen Operationen}. Die GrÃ¶ÃŸe $d(s) := |\Sigma(s)|$
	heiÃŸt \emph{Verzweigungsgrad} von $s$.
\end{definition}

\begin{definition}[Aufrufsequenz und Lauf]
	\label{def:lauf}
	Eine \emph{Aufrufsequenz} $c = (c_1, \ldots, c_k) \in \Sigma^*$ ist eine endliche Folge
	von Operationen. Ein \emph{Lauf} von $P$ auf $c$ ist eine Folge von ZustÃ¤nden
	$s_0, s_1, \ldots, s_k$ mit $s_i = \delta(s_{i-1}, c_i)$ fÃ¼r $i = 1, \ldots, k$.
\end{definition}

\begin{definition}[KonformitÃ¤t]
	\label{def:konformitaet}
	Eine Aufrufsequenz $c = (c_1, \ldots, c_k)$ heiÃŸt \emph{(partiell) konform} bezÃ¼glich
	$P$, wenn ein Lauf $s_0, \ldots, s_k$ gemÃ¤ÃŸ Definition~\ref{def:lauf} existiert, d.\,h.
	wenn $\delta(s_{i-1}, c_i)$ fÃ¼r alle $i = 1, \ldots, k$ definiert ist. Die Sequenz heiÃŸt
	\emph{vollstÃ¤ndig konform} (oder \emph{abgeschlossen}), wenn zusÃ¤tzlich $s_k \in F$ gilt.
	Die Menge aller partiell konformen Sequenzen heiÃŸt $\mathcal{L}^*(P)$, die Menge aller
	vollstÃ¤ndig konformen Sequenzen $\mathcal{L}(P) \subseteq \mathcal{L}^*(P)$.
\end{definition}

\begin{bemerkung}
	Da $\delta$ als \emph{Funktion} (nicht als Relation) definiert ist, ist der Folgezustand
	$s_i = \delta(s_{i-1}, c_i)$ fÃ¼r jeden Schritt eindeutig bestimmt. Diese Eigenschaft ist
	notwendig (aber nicht hinreichend), damit ein Server zu jedem Zeitpunkt einen
	wohldefinierten Prozesszustand pro Prozessinstanz fÃ¼hren kann.
\end{bemerkung}

\begin{beispiel}[Bestellprozess]
	\label{bsp:bestellprozess}
	Abbildung~\ref{fig:automat} zeigt einen Prozessautomaten fÃ¼r einen vereinfachten
	Bestellprozess mit
	$S = \{s_0, s_1, s_2, s_3, s_4, s_5\}$, $F = \{s_5\}$ und
	$\Sigma = \{$\texttt{POST /cart}, \texttt{POST /cart/items}, \texttt{POST /checkout},
	\texttt{POST /payment}, \texttt{POST /confirm}$\}$. Tabelle~\ref{tab:delta} listet die
	Ãœbergangsfunktion $\delta$ vollstÃ¤ndig auf. Jede Operation, die in einem Zustand $s$
	nicht in $\Sigma(s)$ enthalten ist, fÃ¼hrt â€” gemÃ¤ÃŸ der in
	Abschnitt~\ref{sec:pattern} definierten Semantik â€” zu einer
	\texttt{409 Conflict}-Antwort, ohne dass sich der Prozesszustand Ã¤ndert.

	\begin{figure}[h]
		\centering
		\includegraphics[width=0.95\textwidth]{plots/plot1_automat.pdf}
		\caption{Prozessautomat eines Bestellprozesses. Durchgezogene blaue Pfeile
		bilden den (eindeutigen) Lauf des Hauptpfades; die Selbstschleife an $s_2$
		erlaubt das mehrfache HinzufÃ¼gen von Artikeln. Gestrichelte rote Pfeile
		reprÃ¤sentieren Operationen, die in dem jeweiligen Zustand \emph{nicht} in
		$\Sigma(s)$ enthalten sind und daher gemÃ¤ÃŸ Definition~\ref{def:sc-rest-server}
		mit \texttt{409 Conflict} beantwortet werden, ohne den Zustand zu verÃ¤ndern
		(der Zustand $\bot$ ist daher kein Element von $S$, sondern eine grafische
		Sammeldarstellung aller abgewiesenen ÃœbergÃ¤nge).}
		\label{fig:automat}
	\end{figure}

	\begin{table}[h]
		\centering
		\caption{Ãœbergangsfunktion $\delta$ des Bestellprozesses aus
		Abbildung~\ref{fig:automat}.}
		\label{tab:delta}
		\begin{tabular}{@{}lll@{}}
			\toprule
			Zustand $s$ & Operation $\sigma$ & Folgezustand $\delta(s,\sigma)$ \\
			\midrule
			$s_0$ & \texttt{POST /cart}       & $s_1$ \\
			$s_1$ & \texttt{POST /cart/items} & $s_2$ \\
			$s_2$ & \texttt{POST /cart/items} & $s_2$ \\
			$s_2$ & \texttt{POST /checkout}   & $s_3$ \\
			$s_3$ & \texttt{POST /payment}    & $s_4$ \\
			$s_4$ & \texttt{POST /confirm}    & $s_5$ \\
			\bottomrule
		\end{tabular}
	\end{table}

	Die Sequenz
	\[
		\begin{aligned}
			c = (&\texttt{POST /cart},\ \texttt{POST /cart/items},\ \texttt{POST /checkout},\\
			     &\texttt{POST /payment},\ \texttt{POST /confirm})
		\end{aligned}
	\]
	ist vollstÃ¤ndig konform: Der zugehÃ¶rige Lauf ist $s_0, s_1, s_2, s_3, s_4, s_5$ mit
	$s_5 \in F$. Die Sequenz $c' = (\texttt{POST /cart}, \texttt{POST /checkout})$ ist
	\emph{nicht} konform, da $\delta(s_1, \texttt{POST /checkout})$ undefiniert ist
	($\Sigma(s_1) = \{\texttt{POST /cart/items}\}$).
\end{beispiel}

\section{Das SC-REST-Pattern}
\label{sec:pattern}

\subsection{Prozessinstanzen und Serverzustand}

REST verlangt, dass \emph{jede Anfrage} alle notwendigen Informationen enthÃ¤lt. SC-REST
verletzt dieses Prinzip nicht: Der pro Prozessinstanz gefÃ¼hrte Zustand $s \in S$ ist kein
verstecktes Sitzungsobjekt, sondern wird als \emph{Ressourcenattribut} der Prozessinstanz
modelliert und ist Ã¼ber deren URI adressierbar.

\begin{definition}[Prozessinstanz]
	\label{def:prozessinstanz}
	Eine \emph{Prozessinstanz} ist ein Tupel $(id, s)$, wobei $id$ ein eindeutiger
	Bezeichner (z.\,B. eine UUID, kodiert in der Ressourcen-URI
	\texttt{/orders/\{id\}}) und $s \in S$ der aktuelle Zustand der Instanz im
	Prozessautomaten $P$ ist. Der Server fÃ¼hrt eine (partielle) Abbildung
	$\pi: \text{ID} \rightharpoonup S$ mit $\pi(id) = s$.
\end{definition}

\begin{definition}[SC-REST-Operationssemantik]
	\label{def:sc-rest-server}
	Sei $P = (S,\Sigma,\delta,s_0,F)$ ein Prozessautomat und $\pi(id) = s$ der aktuelle
	Zustand einer Prozessinstanz. Auf eine eingehende Anfrage $\sigma \in \Sigma$
	reagiert ein SC-REST-Server wie folgt:
	\begin{enumerate}
		\item \textbf{Falls $\delta(s,\sigma)$ definiert ist} (\emph{konformer Ãœbergang}):
		\begin{itemize}
			\item FÃ¼hre die durch $\sigma$ beschriebene Fachlogik aus,
			\item setze $\pi(id) := \delta(s,\sigma)$,
			\item antworte mit einem 2xx-Statuscode und einer ReprÃ¤sentation, die
			      gemÃ¤ÃŸ HATEOAS Links auf alle Operationen
			      $\Sigma(\delta(s,\sigma))$ enthÃ¤lt.
		\end{itemize}
		\item \textbf{Falls $\delta(s,\sigma)$ undefiniert ist} (\emph{Sequenzverletzung}):
		\begin{itemize}
			\item $\pi(id)$ bleibt unverÃ¤ndert,
			\item der Server antwortet mit \texttt{409 Conflict} und einem
			      Problem-Detail-Objekt gemÃ¤ÃŸ RFC~7807~\cite{rfc7807} vom Typ
			      \texttt{"{}sequence-violation"{}}, das die Menge $\Sigma(s)$ der
			      tatsÃ¤chlich zulÃ¤ssigen Operationen enthÃ¤lt.
		\end{itemize}
	\end{enumerate}
\end{definition}

\begin{lstlisting}[language=, caption=Problem-Detail-Antwort bei Sequenzverletzung (RFC 7807)]
HTTP/1.1 409 Conflict
Content-Type: application/problem+json

{
  "type": "https://api.example.com/probs/sequence-violation",
  "title": "Ungueltige Operation fuer aktuellen Prozesszustand",
  "status": 409,
  "instance": "/orders/7f3e-...-91ab",
  "currentState": "s1",
  "attemptedOperation": "POST /checkout",
  "allowedOperations": [
    { "method": "POST", "path": "/orders/7f3e-.../items",
      "rel": "add-item" }
  ]
}
\end{lstlisting}

\begin{lstlisting}[language=, caption=Erfolgsantwort mit HATEOAS-Links auf zulaessige Folgeoperationen]
HTTP/1.1 201 Created
Content-Type: application/json

{
  "orderId": "7f3e-...-91ab",
  "state": "s2",
  "_links": {
    "add-item":  { "method": "POST",
                    "href": "/orders/7f3e-.../items" },
    "checkout":  { "method": "POST",
                    "href": "/orders/7f3e-.../checkout" }
  }
}
\end{lstlisting}

\subsection{Zustandslosigkeit und SC-REST}

\begin{bemerkung}
	SC-REST hebt die Zustandslosigkeit der einzelnen Anfrage nicht auf. Der Zustand $s$
	ist eine \emph{persistente Eigenschaft der adressierten Ressource} (der
	Prozessinstanz), nicht ein an die Verbindung oder Sitzung gebundenes Artefakt. Jede
	Anfrage bleibt selbstbeschreibend: Methode, Pfad und ReprÃ¤sentation sind hinreichend,
	um die Anfrage zu bearbeiten; lediglich die \emph{Akzeptanz} der Anfrage hÃ¤ngt
	zusÃ¤tzlich vom persistenten, ressourcengebundenen Zustand $\pi(id)$ ab â€” analog zur
	bekannten Bedingung, dass ein \texttt{PUT} auf eine Ressource mit
	\texttt{If-Match}-Header vom aktuellen \texttt{ETag} der Ressource abhÃ¤ngt. SC-REST
	verallgemeinert dieses Prinzip von einzelnen Versionskonflikten auf
	\emph{Sequenzkonflikte}.
\end{bemerkung}

\subsection{Implementierung der ÃœbergangsprÃ¼fung}

Algorithmus~\ref{lst:transition} zeigt die KernprÃ¼fung als Python-Funktion. Die
Ãœbergangsfunktion $\delta$ wird als verschachteltes \texttt{dict} (Hashtabelle)
$\Sigma \times S \to S$ realisiert, sodass die PrÃ¼fung in konstanter erwarteter Zeit
erfolgt (siehe Satz~\ref{satz:laufzeit-pruefung}).

\begin{lstlisting}[language=Python, label=lst:transition, caption=SC-REST Uebergangspruefung als Middleware-Funktion]
def handle_request(process: ProcessAutomaton,
                    instance_id: str,
                    operation: tuple) -> Response:
    """
    Prueft eine eingehende Operation gegen den aktuellen
    Zustand einer Prozessinstanz und fuehrt bei Konformitaet
    den Zustandsuebergang durch.

    delta: Dict[Tuple[State, Operation], State]
    """
    current_state = process.pi[instance_id]

    next_state = process.delta.get((current_state, operation))

    if next_state is None:
        # Sequenzverletzung: Zustand bleibt unveraendert
        allowed = [op for (s, op) in process.delta
                   if s == current_state]
        return problem_detail(
            status=409,
            type="sequence-violation",
            current_state=current_state,
            attempted=operation,
            allowed_operations=allowed,
        )

    # Konformer Uebergang: Fachlogik ausfuehren, Zustand setzen
    result = process.execute(operation, instance_id)
    process.pi[instance_id] = next_state

    return success_response(
        result=result,
        state=next_state,
        links=hateoas_links(process, next_state, instance_id),
    )
\end{lstlisting}

\section{Formale Analyse}
\label{sec:analyse}

Dieser Abschnitt erbringt vier formale Nachweise: (1) Korrektheit der Sequenzkontrolle
(Soundness und Completeness), (2) Laufzeit- und OptimalitÃ¤tsschranken der
ÃœbergangsprÃ¼fung, (3) die Charakterisierung der KonformitÃ¤tsprÃ¼fung als
Pfad-Subgraph-Problem sowie deren Erweiterung auf permutierbare Teilschritte mittels des
Subgraph Algorithmus aus~\cite{epp2026subgraph}, und (4) ein formales Latenzmodell fÃ¼r
serverseitiges Prefetching.

\subsection{Korrektheit der Sequenzkontrolle}

\begin{satz}[Korrektheit der Sequenzkontrolle]
	\label{satz:korrektheit}
	Sei $P=(S,\Sigma,\delta,s_0,F)$ ein Prozessautomat und sei
	$c=(c_1,\ldots,c_k)\in\Sigma^*$ eine Aufrufsequenz, die einem SC-REST-Server gemÃ¤ÃŸ
	Definition~\ref{def:sc-rest-server} beginnend bei $\pi(id)=s_0$ Operation fÃ¼r
	Operation prÃ¤sentiert wird. Dann gilt:
	\begin{enumerate}
		\item[(a)] (\emph{Soundness}) Der Server antwortet auf $c_1,\ldots,c_k$ genau dann
		      \emph{niemals} mit \texttt{409 Conflict}, wenn $c \in \mathcal{L}^*(P)$
		      partiell konform ist.
		\item[(b)] (\emph{Completeness}) Ist $c$ partiell konform, so stimmt der nach
		      Verarbeitung von $c$ erreichte Serverzustand $\pi(id)$ exakt mit dem
		      eindeutigen Lauf-Endzustand $s_k$ aus Definition~\ref{def:lauf} Ã¼berein.
		\item[(c)] (\emph{Fehlerlokalisierung}) Ist $c$ \emph{nicht} konform, so existiert
		      ein eindeutiges minimales $i^\ast \in \{1,\ldots,k\}$, sodass
		      $c_1,\ldots,c_{i^\ast-1}$ konform ist, $\delta(s_{i^\ast-1},c_{i^\ast})$
		      undefiniert ist, und der Server bei genau diesem Schritt -- und nur bei
		      diesem -- mit \texttt{409 Conflict} antwortet, wobei $\pi(id)$ auf
		      $s_{i^\ast-1}$ verbleibt.
	\end{enumerate}
\end{satz}

\begin{proof}
	Wir fÃ¼hren vollstÃ¤ndige Induktion Ã¼ber $i=0,\ldots,k$ und zeigen die Invariante
	\[
		I(i):\quad \pi(id) \text{ ist nach Verarbeitung von } c_1,\ldots,c_i
		\text{ wohldefiniert und gleich } s_i,
	\]
	solange $c_1,\ldots,c_i$ konform ist, andernfalls gleich $s_{i^\ast-1}$ fÃ¼r das
	kleinste $i^\ast \le i$, fÃ¼r das $\delta(s_{i^\ast-1},c_{i^\ast})$ undefiniert ist.

	\textbf{Induktionsanfang ($i=0$):} $\pi(id)=s_0$ nach Voraussetzung; $I(0)$ gilt
	trivial, da die leere Sequenz konform ist mit Lauf $s_0$.

	\textbf{Induktionsschritt ($i-1 \to i$):} Nach Induktionsannahme gilt $I(i-1)$.
	Zwei FÃ¤lle:
	\begin{itemize}
		\item \emph{Fall A: $c_1,\ldots,c_{i-1}$ ist konform und $\pi(id)=s_{i-1}$.}
		      Bei Eintreffen von $c_i$ prÃ¼ft der Server gemÃ¤ÃŸ
		      Definition~\ref{def:sc-rest-server}, ob $\delta(s_{i-1},c_i)$ definiert ist.
		      \begin{itemize}
			      \item Ist es definiert, setzt der Server $\pi(id):=\delta(s_{i-1},c_i)=s_i$
			            (Definition~\ref{def:lauf}) und antwortet nicht mit \texttt{409}.
			            Damit ist $c_1,\ldots,c_i$ konform und $I(i)$ gilt mit $\pi(id)=s_i$.
			      \item Ist es undefiniert, antwortet der Server mit \texttt{409 Conflict}
			            und $\pi(id)$ bleibt bei $s_{i-1}$. Damit ist $i^\ast = i$ das
			            gesuchte minimale Indexelement, und $I(i)$ gilt mit
			            $\pi(id)=s_{i^\ast-1}=s_{i-1}$.
		      \end{itemize}
		\item \emph{Fall B: $c_1,\ldots,c_{i-1}$ ist bereits nicht konform mit
		      Verletzungsindex $i^\ast \le i-1$ und $\pi(id)=s_{i^\ast-1}$.} Da $\delta$ in
		      Definition~\ref{def:sc-rest-server} bei einer Sequenzverletzung den Zustand
		      \emph{nicht} verÃ¤ndert, bleibt $\pi(id)=s_{i^\ast-1}$ auch nach Verarbeitung
		      von $c_i$ -- unabhÃ¤ngig davon, ob $\delta(s_{i^\ast-1},c_i)$ definiert wÃ¤re,
		      denn der Server prÃ¼ft stets gegen den \emph{aktuellen}, nicht den
		      ursprÃ¼nglich vorgesehenen Zustand. $I(i)$ gilt also mit demselben $i^\ast$.
	\end{itemize}

	Aus $I(k)$ folgen unmittelbar (a)--(c): Tritt wÃ¤hrend der gesamten Verarbeitung von
	$c_1,\ldots,c_k$ kein Fall â€žundefiniertâ€œ auf, ist $c$ nach Definition~\ref{def:lauf}
	konform mit Lauf $s_0,\ldots,s_k$ und der Server antwortet nie mit \texttt{409}
	(Soundness, durch Kontraposition von Fall A in beiden Richtungen).
	Tritt der Fall â€žundefiniertâ€œ erstmals bei Index $i^\ast$ auf, so ist dies nach
	Konstruktion eindeutig und minimal, der Server antwortet exakt einmal -- bei
	$i^\ast$ -- mit \texttt{409}, und $\pi(id)$ verbleibt fÃ¼r alle $i \ge i^\ast$ bei
	$s_{i^\ast-1}$ (Fehlerlokalisierung, (c)). Im konformen Fall liefert $I(k)$ direkt
	(b). \qedhere
\end{proof}

\begin{bemerkung}
	Satz~\ref{satz:korrektheit}(c) ist die formale Grundlage fÃ¼r die in der EinfÃ¼hrung
	geforderte \emph{FÃ¼hrung des Clients}: Der Server lehnt nicht nur ab, sondern tut dies
	beim \emph{frÃ¼hestmÃ¶glichen} Zeitpunkt einer Sequenzverletzung, und der Zustand der
	Prozessinstanz bleibt fÃ¼r \emph{beliebig viele} nachfolgende fehlerhafte Anfragen
	stabil bei $s_{i^\ast-1}$ -- ein Client kann also nach einem \texttt{409} beliebig oft
	erneut die laut \texttt{allowedOperations} korrekte Operation versuchen, ohne dass
	der Prozesszustand durch die fehlgeschlagenen Versuche beeinflusst wird
	(\emph{StabilitÃ¤t unter Fehlversuchen}).
\end{bemerkung}

\subsection{Laufzeit und OptimalitÃ¤t der ÃœbergangsprÃ¼fung}

\begin{satz}[Laufzeit der ÃœbergangsprÃ¼fung]
	\label{satz:laufzeit-pruefung}
	Wird $\delta$ als Hashtabelle $H: S \times \Sigma \to S \cup \{\text{undef}\}$
	realisiert (Algorithmus~\ref{lst:transition}), so benÃ¶tigt die PrÃ¼fung, ob
	$\delta(s,\sigma)$ definiert ist, sowie -- falls definiert -- die Bestimmung von
	$\delta(s,\sigma)$, im Erwartungswert $O(1)$ Zeit, unabhÃ¤ngig von $|S|$, $|\Sigma|$
	und der LÃ¤nge $k$ der bisherigen Aufrufsequenz.
\end{satz}

\begin{proof}
	Ein Hashtabellen-Lookup $H[(s,\sigma)]$ erfordert die Berechnung eines Hashwerts Ã¼ber
	das Paar $(s,\sigma)$ -- beides Werte konstanter BeschreibungsgrÃ¶ÃŸe (Zustands-ID bzw.
	Methode+Pfad-Muster) -- sowie den Zugriff auf den entsprechenden Bucket. Beide
	Operationen sind unter Standardannahmen an die Hashfunktion (gleichverteilte Streuung,
	Lastfaktor $O(1)$) im Erwartungswert $O(1)$. Die Anzahl $|S|$ der ZustÃ¤nde und
	$|\Sigma|$ der Operationen beeinflusst lediglich die \emph{GrÃ¶ÃŸe} der Hashtabelle,
	nicht jedoch -- bei konstantem Lastfaktor -- die erwartete Zugriffszeit. Die LÃ¤nge $k$
	der bisherigen Sequenz geht nicht in die PrÃ¼fung ein, da nur der aktuelle Zustand
	$\pi(id)=s_{k}$ betrachtet wird (Markov-Eigenschaft des Prozessautomaten,
	Definition~\ref{def:lauf}). \qedhere
\end{proof}

\begin{satz}[OptimalitÃ¤t der GesamtprÃ¼fung einer Sequenz]
	\label{satz:optimalitaet-sequenz}
	Die PrÃ¼fung der KonformitÃ¤t einer vollstÃ¤ndigen Aufrufsequenz $c=(c_1,\ldots,c_k)$
	gemÃ¤ÃŸ Definition~\ref{def:konformitaet} benÃ¶tigt $\Theta(k)$ Zeit und ist
	asymptotisch optimal: Kein korrekter Algorithmus kann diese PrÃ¼fung in $o(k)$ Zeit
	durchfÃ¼hren.
\end{satz}

\begin{proof}
	\textbf{Obere Schranke $O(k)$.} Nach Satz~\ref{satz:laufzeit-pruefung} kostet jeder
	der $k$ Ãœbergangsschritte $O(1)$, in Summe also $O(k)$.

	\textbf{Untere Schranke $\Omega(k)$.} Sei $\mathcal{A}$ ein beliebiger korrekter
	Algorithmus, der fÃ¼r eine Eingabesequenz $c=(c_1,\ldots,c_k)$ entscheidet, ob
	$c \in \mathcal{L}^*(P)$. Angenommen, $\mathcal{A}$ liest ein Element $c_j$ fÃ¼r ein
	$j\in\{1,\ldots,k\}$ nicht. Wir konstruieren zwei Eingaben $c$ und $c'$, die sich nur
	in $c_j$ unterscheiden:
	\[
		c = (c_1,\ldots,c_{j-1}, \sigma, c_{j+1},\ldots,c_k), \qquad
		c' = (c_1,\ldots,c_{j-1}, \sigma', c_{j+1},\ldots,c_k),
	\]
	wobei $\sigma, \sigma' \in \Sigma(s_{j-1})$ mit $\sigma \neq \sigma'$ und
	$\delta(s_{j-1},\sigma) \neq \delta(s_{j-1},\sigma')$ -- eine solche Wahl existiert,
	sobald $d(s_{j-1}) \ge 2$, was o.\,B.\,d.\,A. fÃ¼r mindestens ein $j$ gilt (sonst ist
	$P$ trivial, da jeder Zustand hÃ¶chstens einen Folgezustand besitzt, und die Aussage
	wird leer). FÃ¼r geeignete Fortsetzungen $c_{j+1},\ldots,c_k$ -- etwa eine konforme
	Fortsetzung ab $\delta(s_{j-1},\sigma)$, die ab $\delta(s_{j-1},\sigma')$ \emph{nicht}
	konform ist -- unterscheiden sich die korrekten Antworten fÃ¼r $c$ und $c'$. Da
	$\mathcal{A}$ den Eintrag $c_j$ nicht liest, verhÃ¤lt sich $\mathcal{A}$ auf $c$ und
	$c'$ identisch und liefert fÃ¼r mindestens eine der beiden Eingaben ein falsches
	Ergebnis -- Widerspruch zur Korrektheit. Folglich muss $\mathcal{A}$ jedes Element
	$c_1,\ldots,c_k$ lesen, also $T_{\mathcal{A}}(k) \ge \Omega(k)$. \qedhere
\end{proof}

\begin{bemerkung}
	Satz~\ref{satz:optimalitaet-sequenz} zeigt, dass die SC-REST-KonformitÃ¤tsprÃ¼fung --
	anders als das allgemeine Subgraph-Problem aus~\cite{epp2026subgraph} mit seiner
	unteren Schranke $\Omega(n^{3-\varepsilon})$ -- \emph{linear} und damit
	vergleichsweise â€žbilligâ€œ ist. Der Grund liegt in der \emph{linearen Struktur} einer
	Aufrufsequenz: Sie ist ein einfacher Pfad, kein beliebiger Graph. Abschnitt
	\ref{sec:permutierbar} zeigt, dass diese Eigenschaft verloren geht, sobald
	\emph{permutierbare} Teilschritte zugelassen werden -- und genau dann wird der
	Subgraph Algorithmus mit zyklischer Rotation wieder relevant.
\end{bemerkung}

\subsection{KonformitÃ¤t als Pfad-Subgraph-Problem}

\begin{definition}[Prozessgraph]
	\label{def:prozessgraph}
	Der \emph{Prozessgraph} $G_P=(S,E)$ zu einem Prozessautomaten
	$P=(S,\Sigma,\delta,s_0,F)$ ist der gerichtete, kantenbeschriftete Graph mit
	\[
		E = \{\, (s,s',\sigma) \mid s,s' \in S,\ \sigma\in\Sigma,\ \delta(s,\sigma)=s' \,\}.
	\]
	Eine Aufrufsequenz $c=(c_1,\ldots,c_k)$ induziert den \emph{Sequenzgraphen}
	$G_c$: einen einfachen, gerichteten Pfad mit $k+1$ Knoten $u_0,\ldots,u_k$ und Kanten
	$(u_{i-1},u_i,c_i)$ fÃ¼r $i=1,\ldots,k$.
\end{definition}

\begin{satz}[Pfad-Charakterisierung der KonformitÃ¤t]
	\label{satz:pfadcharakterisierung}
	Eine Aufrufsequenz $c$ ist genau dann partiell konform bezÃ¼glich $P$
	(Definition~\ref{def:konformitaet}), wenn der Sequenzgraph $G_c$ unter der Abbildung
	$u_0 \mapsto s_0$ als beschrifteter Teilpfad in $G_P$ einbettbar ist, d.\,h. wenn eine
	Folge von Knoten $s_0=v_0,v_1,\ldots,v_k \in S$ existiert mit
	$(v_{i-1},v_i,c_i)\in E$ fÃ¼r alle $i=1,\ldots,k$.
\end{satz}

\begin{proof}
	â€ž$\Rightarrow$â€œ: Ist $c$ konform, existiert nach Definition~\ref{def:lauf} ein Lauf
	$s_0,\ldots,s_k$ mit $\delta(s_{i-1},c_i)=s_i$. Nach Definition~\ref{def:prozessgraph}
	ist dann $(s_{i-1},s_i,c_i)\in E$ fÃ¼r alle $i$, also bildet $v_i:=s_i$ die geforderte
	Pfadeinbettung.

	â€ž$\Leftarrow$â€œ: Existiert eine solche Knotenfolge $v_0=s_0,\ldots,v_k$ mit
	$(v_{i-1},v_i,c_i)\in E$, so gilt nach Definition~\ref{def:prozessgraph}
	$\delta(v_{i-1},c_i)=v_i$ fÃ¼r alle $i$. Da $\delta$ eine Funktion ist
	(Definition~\ref{def:prozessautomat}), ist diese Folge der \emph{eindeutige} Lauf von
	$P$ auf $c$ mit $s_i=v_i$, also ist $c$ konform. \qedhere
\end{proof}

\begin{bemerkung}
	\label{bem:einfacher-fall}
	Satz~\ref{satz:pfadcharakterisierung} reduziert die KonformitÃ¤tsprÃ¼fung auf die
	Einbettbarkeit eines \emph{einfachen, beschrifteten Pfades} $G_c$ in $G_P$ -- ein
	Spezialfall des allgemeinen Subgraph-Enthaltenseins-Problems. WÃ¤hrend das allgemeine
	Problem (zwei beliebige Graphen, Einbettung unter Knotenpermutation bzw. zyklischer
	Rotation) nach~\cite{epp2026subgraph} eine scharfe untere Schranke
	$\Omega(n^{3-\varepsilon})$ unter SETH besitzt, ist die Pfad-Einbettung \emph{ab dem
	festen Startknoten $s_0$} wegen der Determiniertheit von $\delta$ trivial in
	$\Theta(k)$ entscheidbar (Satz~\ref{satz:optimalitaet-sequenz}): An jedem Knoten $v_i$
	existiert hÃ¶chstens \emph{eine} mit $c_{i+1}$ beschriftete ausgehende Kante, sodass
	keine Suche Ã¼ber mehrere Kandidaten und keine Rotation erforderlich ist. Der
	allgemeine, schwere Fall des Subgraph-Problems tritt erst dann wieder auf, wenn der
	Startknoten \emph{nicht fest} ist oder wenn -- wie in
	Abschnitt~\ref{sec:permutierbar} -- mehrere Kanten mit unterschiedlichen Beschriftungen
	in beliebiger Reihenfolge durchlaufen werden dÃ¼rfen.
\end{bemerkung}

\subsection{Permutierbare optionale Teilschritte}
\label{sec:permutierbar}

Viele GeschÃ¤ftsprozesse enthalten Phasen, in denen mehrere optionale Teilschritte
\emph{in beliebiger Reihenfolge}, jedoch jeweils hÃ¶chstens einmal, ausgefÃ¼hrt werden
dÃ¼rfen, bevor der Prozess fortgesetzt wird -- etwa das HinzufÃ¼gen mehrerer unabhÃ¤ngiger
Zusatzoptionen zu einer Bestellung (Geschenkverpackung, Expresslieferung, Gutscheincode,
Versicherung), deren Reihenfolge fachlich irrelevant ist.

\begin{definition}[Sammelzustand mit permutierbaren Teilschritten]
	\label{def:sammelzustand}
	Ein Zustand $s_c \in S$ heiÃŸt \emph{Sammelzustand mit $r$ permutierbaren optionalen
	Teilschritten}, wenn ein Operationssatz
	$\Sigma_{\mathrm{opt}} = \{\sigma_1,\ldots,\sigma_r\} \subseteq \Sigma$ sowie eine
	Abschlussoperation $\sigma_{\mathrm{done}} \in \Sigma$ existieren, sodass fÃ¼r jede
	Teilmenge $T \subseteq \{1,\ldots,r\}$ der bereits ausgefÃ¼hrten Operationen und jedes
	$j \notin T$ gilt: Aus dem zu $T$ korrespondierenden (logischen) Zwischenzustand ist
	$\sigma_j$ zulÃ¤ssig und fÃ¼hrt zum Zwischenzustand $T \cup \{j\}$; aus dem
	Zwischenzustand zu $T=\{1,\ldots,r\}$ (alle Teilschritte ausgefÃ¼hrt, oder ein
	beliebiges zulÃ¤ssiges $T$, falls $\sigma_{\mathrm{done}}$ auch frÃ¼her erlaubt ist)
	fÃ¼hrt $\sigma_{\mathrm{done}}$ in den Folgezustand des Prozesses.
\end{definition}

\begin{lemma}[Zustandsraum-Explosion bei naiver Automatenkodierung]
	\label{lem:zustandsexplosion}
	Sei $s_c$ ein Sammelzustand mit $r$ permutierbaren optionalen Teilschritten gemÃ¤ÃŸ
	Definition~\ref{def:sammelzustand}, und seien fÃ¼r je zwei verschiedene Teilmengen
	$T \neq T' \subseteq \{1,\ldots,r\}$ die jeweils zulÃ¤ssigen Restoperationen
	$\Sigma_{\mathrm{opt}} \setminus T$ und $\Sigma_{\mathrm{opt}} \setminus T'$
	verschieden (d.\,h. $\Sigma(s_T) \neq \Sigma(s_{T'})$ fÃ¼r die zu $T$ bzw. $T'$
	gehÃ¶rigen ZwischenzustÃ¤nde $s_T,s_{T'}$). Dann benÃ¶tigt \emph{jeder} deterministische
	Prozessautomat, der diesen Sammelzustand abbildet, mindestens $2^r$ ZustÃ¤nde, also
	$|S| \geq 2^r$.
\end{lemma}

\begin{proof}
	Wir zeigen, dass die $2^r$ ZwischenzustÃ¤nde $s_T$, $T \subseteq \{1,\ldots,r\}$,
	paarweise \emph{verschieden} sein mÃ¼ssen -- ein Argument in der Tradition des
	Myhill-Nerode-Theorems fÃ¼r regulÃ¤re Sprachen, angewandt auf
	Erreichbarkeits-Ã„quivalenzklassen von ZustÃ¤nden.

	Angenommen, $s_T = s_{T'}$ fÃ¼r zwei verschiedene Teilmengen $T \neq T'$. Da $\delta$
	als Funktion eines einzelnen Zustands definiert ist
	(Definition~\ref{def:prozessautomat}), folgt $\Sigma(s_T) = \Sigma(s_{T'})$ -- die
	Menge der von $s_T$ aus zulÃ¤ssigen Operationen hÃ¤ngt nur vom Zustand selbst ab, nicht
	von der â€žHistorieâ€œ, Ã¼ber die er erreicht wurde. Dies widerspricht jedoch der
	Voraussetzung $\Sigma(s_T) \neq \Sigma(s_{T'})$ fÃ¼r $T \neq T'$.

	Also mÃ¼ssen alle $2^r$ Teilmengen $T \subseteq \{1,\ldots,r\}$ auf paarweise
	verschiedene ZustÃ¤nde $s_T \in S$ abgebildet werden, woraus $|S| \geq 2^r$ folgt.
	\qedhere
\end{proof}

\begin{bemerkung}
	Lemma~\ref{lem:zustandsexplosion} ist die formale BegrÃ¼ndung dafÃ¼r, dass ein
	SC-REST-Server fÃ¼r SammelzustÃ¤nde \emph{nicht} naiv $\delta$ Ã¼ber den vollstÃ¤ndigen
	Potenzmengen-Zustandsraum $2^{\{1,\ldots,r\}}$ kodieren sollte: Bereits fÃ¼r $r=20$
	permutierbare Teilschritte ergÃ¤be dies Ã¼ber $10^6$ ZustÃ¤nde allein fÃ¼r diese eine
	Prozessphase. Stattdessen genÃ¼gt es, gemÃ¤ÃŸ Satz~\ref{satz:permutierbar-subgraph} eine
	kompakte ReprÃ¤sentation der GrÃ¶ÃŸe $O(r)$ zu fÃ¼hren und die KonformitÃ¤t einer
	beobachteten Teilreihenfolge mittels des Subgraph Algorithmus zu prÃ¼fen.
\end{bemerkung}

\begin{definition}[Signaturfolgen fÃ¼r SammelzustÃ¤nde]
	\label{def:signaturfolge-sammel}
	FÃ¼r einen Sammelzustand $s_c$ mit Operationssatz
	$\Sigma_{\mathrm{opt}}=\{\sigma_1,\ldots,\sigma_r\}$ sei $\sigma^P =
	[\sigma_1,\ldots,\sigma_r]$ die \emph{kanonische Reihenfolge} (z.\,B. die im
	Prozessmodell dokumentierte Referenzreihenfolge). Eine vom Client beobachtete
	AusfÃ¼hrungsreihenfolge $c' = (c'_1,\ldots,c'_r)$ ist eine Permutation von
	$\Sigma_{\mathrm{opt}}$. Wir bezeichnen mit $\sigma^{C}=[c'_1,\ldots,c'_r]$ die
	zugehÃ¶rige \emph{Beobachtungsfolge}.
\end{definition}

\begin{satz}[Verifikation permutierbarer Teilschritte via Subgraph Algorithmus]
	\label{satz:permutierbar-subgraph}
	Sei $s_c$ ein Sammelzustand mit $r$ permutierbaren optionalen Teilschritten und sei
	$c'=(c'_1,\ldots,c'_r)$ eine vom Client vollstÃ¤ndig durchlaufene Beobachtungsfolge
	(jede Operation aus $\Sigma_{\mathrm{opt}}$ genau einmal, in beliebiger Reihenfolge).
	Dann gilt:
	\begin{enumerate}
		\item[(a)] Die Aussage â€ž$c'$ ist eine zulÃ¤ssige Reihenfolge (Permutation) der
		      Operationen aus $\Sigma_{\mathrm{opt}}$â€œ ist Ã¤quivalent zur Aussage â€ždie
		      Signaturfolge $\sigma^{C}$ ist -- als Multimenge -- identisch zu
		      $\sigma^{P}$, d.\,h. $\sigma^{C}$ entsteht aus $\sigma^{P}$ durch
		      Anwendung einer Permutation auf die Indexmenge $\{1,\ldots,r\}$â€œ.
		\item[(b)] BeschrÃ¤nkt man die zulÃ¤ssigen Umordnungen auf \emph{zyklische
		      Rotationen} von $\sigma^{P}$ (z.\,B. weil eine Teilmenge der $r$
		      Teilschritte eine vom Prozessdesign vorgegebene relative Ordnung
		      beibehalten muss, die nur als Ganzes zyklisch verschoben werden darf), so
		      lÃ¤sst sich die PrÃ¼fung von (a) mit dem Subgraph Algorithmus aus
		      \cite{epp2026subgraph} in $\Theta(r^3)$ Zeit durchfÃ¼hren, und diese
		      Laufzeit ist nach dem OptimalitÃ¤tssatz von~\cite{epp2026subgraph}
		      asymptotisch optimal unter SETH: Keine PrÃ¼fung ist in
		      $O(r^{3-\varepsilon})$ Zeit fÃ¼r $\varepsilon>0$ mÃ¶glich.
		\item[(c)] Im allgemeinen Fall (beliebige Permutationen statt zyklischer
		      Rotationen) genÃ¼gt bereits eine Sortierung der Signaturfolgen in
		      $O(r \log r)$ Zeit, um (a) zu entscheiden.
	\end{enumerate}
\end{satz}

\begin{proof}
	\textbf{(a)} Da jede Operation aus $\Sigma_{\mathrm{opt}}$ in $c'$ nach Voraussetzung
	genau einmal vorkommt, ist $c'$ per Definition eine Permutation von
	$(\sigma_1,\ldots,\sigma_r)$. Die zugehÃ¶rige Signaturfolge $\sigma^{C}$ entsteht somit
	aus $\sigma^{P}=[\sigma_1,\ldots,\sigma_r]$ durch Umordnung gemÃ¤ÃŸ genau dieser
	Permutation. Umgekehrt definiert jede Umordnung von $\sigma^{P}$, die als Multimenge
	mit $\sigma^{P}$ Ã¼bereinstimmt, eindeutig eine Permutation von
	$\{1,\ldots,r\}$ und damit eine zulÃ¤ssige AusfÃ¼hrungsreihenfolge im Sinne von
	Definition~\ref{def:sammelzustand} (jede Operation wird, unabhÃ¤ngig von der
	Reihenfolge, genau einmal ausgefÃ¼hrt und Ã¼berfÃ¼hrt den Sammelzustand in den
	Folgezustand des Prozesses).

	\textbf{(b)} Wende den Subgraph Algorithmus aus~\cite{epp2026subgraph} auf die beiden
	Sequenzen $\sigma^{P}$ und $\sigma^{C}$ an: Beide haben LÃ¤nge $r$, die
	Spalten-Signaturen $\sigma_j = \sum_i A_{ij}2^i + j\cdot 2^r$ werden fÃ¼r beide Folgen
	gebildet (hier interpretiert als $1\times r$-â€žAdjazenzmatrizenâ€œ Ã¼ber dem
	Operationsalphabet, wobei $A_{ij}=1$ gdw. die $i$-te Operation an Position $j$ der
	jeweiligen Folge steht). FÃ¼r jede der $r$ zyklischen Rotationen von $\sigma^{C}$ wird
	via LCS geprÃ¼ft, ob $\sigma^{P}\subseteq\mathrm{rot}_k(\sigma^{C})$ gilt
	(Satz~2 in~\cite{epp2026subgraph}). Nach Satz~â€žLaufzeitâ€œ und
	Satz~â€žOptimalitÃ¤t des Subgraph Algorithmusâ€œ aus~\cite{epp2026subgraph} ist diese
	PrÃ¼fung in $\Theta(r^3)$ durchfÃ¼hrbar und unter SETH nicht in
	$O(r^{3-\varepsilon})$ fÃ¼r $\varepsilon>0$ verbesserbar -- die untere Schranke aus
	\cite{epp2026subgraph} (kombiniert aus Eingabe-Leseschranke, Notwendigkeit aller
	Rotationen und der SETH-basierten LCS-Schranke) Ã¼bertragen sich unverÃ¤ndert, da die
	Signaturkonstruktion injektiv ist (Lemma â€žEindeutigkeit der Signaturenâ€œ,
	\cite{epp2026subgraph}) und somit keine Information verloren geht.

	\textbf{(c)} Da Signaturen nach \cite{epp2026subgraph} (Lemma â€žEindeutigkeit der
	Signaturenâ€œ) injektiv und damit als SchlÃ¼ssel fÃ¼r eine Ordnungsrelation geeignet sind,
	kann man $\sigma^{C}$ und $\sigma^{P}$ jeweils in $O(r\log r)$ sortieren und
	elementweise vergleichen: Gleichheit der sortierten Folgen ist Ã¤quivalent zur
	Multimengen-Gleichheit aus (a). Dies ist gÃ¼nstiger als $\Theta(r^3)$, jedoch nur
	anwendbar, wenn \emph{beliebige} Permutationen zulÃ¤ssig sind und keine relative
	Ordnung zwischen Teilgruppen der $r$ Operationen erhalten bleiben muss. \qedhere
\end{proof}

\begin{bemerkung}
	\label{bem:permutierbar-fazit}
	Satz~\ref{satz:permutierbar-subgraph} zeigt eine doppelte Erweiterung gegenÃ¼ber
	\cite{epp2026subgraph}: Erstens wird der Subgraph Algorithmus von seinem
	ursprÃ¼nglichen Anwendungsfeld (AST-Vergleich, Graph-Deduplizierung) auf die
	KonformitÃ¤tsprÃ¼fung permutierbarer API-Teilschritte \emph{Ã¼bertragen}. Zweitens zeigt
	Lemma~\ref{lem:zustandsexplosion} zusammen mit Satz~\ref{satz:permutierbar-subgraph},
	dass die signaturbasierte PrÃ¼fung eine \emph{exponentielle} Raumersparnis erzielt: Statt
	$\Omega(2^r)$ ZustÃ¤nden im naiv kodierten Automaten genÃ¼gt eine Signaturfolge der
	GrÃ¶ÃŸe $O(r)$ zuzÃ¼glich einer PrÃ¼fung in $\Theta(r^3)$ -- ein klassischer
	Zeit-Raum-Trade-off, der fÃ¼r praktisch relevante $r$ (typischerweise $r \le 20$, siehe
	Abbildung~\ref{fig:komplexitaet}) deutlich zugunsten der signaturbasierten Methode
	ausfÃ¤llt, da $2^{20} \approx 10^6 \gg 20^3 = 8000$.
\end{bemerkung}

\begin{figure}[h]
	\centering
	\includegraphics[width=\textwidth]{plots/plot3_komplexitaet.pdf}
	\caption{Links: Vergleich der KonformitÃ¤tsprÃ¼fung einer festen, linearen
	Aufrufsequenz ($O(k)$, Satz~\ref{satz:optimalitaet-sequenz}) mit der signaturbasierten
	PrÃ¼fung permutierbarer Teilschritte mittels Subgraph Algorithmus und zyklischer
	Rotation ($\Theta(r^3)$, Satz~\ref{satz:permutierbar-subgraph}). Beide Kurven wachsen
	polynomial, jedoch mit unterschiedlichem Grad -- die lineare SequenzprÃ¼fung bleibt
	fÃ¼r alle betrachteten GrÃ¶ÃŸen um GrÃ¶ÃŸenordnungen gÃ¼nstiger, was die in
	Bemerkung~\ref{bem:einfacher-fall} beschriebene strukturelle Vereinfachung
	widerspiegelt. Rechts: Vergleich des Subgraph Algorithmus ($\Theta(r^3)$) mit der
	naiven vollstÃ¤ndigen PermutationsprÃ¼fung ($O(r!\cdot r^2)$,
	vgl.~\cite{epp2026subgraph}) auf logarithmischer Skala fÃ¼r $r=1,\ldots,12$. Bereits ab
	$r\approx 6$ Ã¼bersteigt die naive Methode die rotationsbasierte Methode um mehrere
	GrÃ¶ÃŸenordnungen, was Lemma~\ref{lem:zustandsexplosion} und
	Bemerkung~\ref{bem:permutierbar-fazit} empirisch illustriert.}
	\label{fig:komplexitaet}
\end{figure}

\subsection{Spekulative Vorbereitung (Prefetching)}
\label{sec:prefetch}

Da der Server gemÃ¤ÃŸ Definition~\ref{def:prozessautomat} fÃ¼r jeden Zustand $s$ die Menge
$N(s)$ der erreichbaren FolgezustÃ¤nde \emph{vollstÃ¤ndig kennt}, kann er bereits wÃ¤hrend
der Bedenkzeit des Clients Vorbereitungsarbeiten fÃ¼r alle $s' \in N(s)$ anstoÃŸen.

\begin{definition}[Latenzmodell]
	\label{def:latenzmodell}
	FÃ¼r eine Operation $\sigma$ mit $\delta(s,\sigma)=s'$ seien:
	\begin{itemize}
		\item $T_{\mathrm{exec}}(\sigma)$: die Zeit zur AusfÃ¼hrung der eigentlichen
		      Fachlogik von $\sigma$ (z.\,B. Schreiboperation, Validierung),
		\item $T_{\mathrm{prep}}(s')$: die Zeit, um Ressourcen fÃ¼r den Zustand $s'$
		      \emph{vorzubereiten} (z.\,B. Vorab-Laden von Referenzdaten, Reservierung von
		      Inventar, AufwÃ¤rmen von Caches),
		\item $T_{\mathrm{think}}$: die Zeit zwischen dem Eintreffen der Antwort auf die
		      vorherige Operation (die in Zustand $s$ Ã¼berfÃ¼hrt hat) und dem Eintreffen
		      der nÃ¤chsten Anfrage $\sigma$ des Clients (\emph{Bedenkzeit}).
	\end{itemize}
	\textbf{Ohne Prefetching} beginnt die Vorbereitung von $s'$ erst, nachdem $\sigma$
	eingetroffen ist:
	\[
		E[L_{\text{ohne}}] = T_{\mathrm{prep}}(s') + T_{\mathrm{exec}}(\sigma).
	\]
	\textbf{Mit Prefetching} startet der Server, sobald $\pi(id)=s$ erreicht ist, fÃ¼r
	\emph{jedes} $s'' \in N(s)$ parallel eine Vorbereitung. Trifft $\sigma$ mit
	$\delta(s,\sigma)=s'$ ein, ist die Vorbereitung fÃ¼r $s'$ bereits
	$\min(T_{\mathrm{think}}, T_{\mathrm{prep}}(s'))$ weit fortgeschritten:
	\[
		E[L_{\mathrm{pf}}] = \max\bigl(T_{\mathrm{prep}}(s') - T_{\mathrm{think}},\, 0\bigr)
		+ T_{\mathrm{exec}}(\sigma).
	\]
\end{definition}

\begin{satz}[Latenzgewinn durch Prefetching]
	\label{satz:latenzgewinn}
	Unter dem Latenzmodell aus Definition~\ref{def:latenzmodell} gilt fÃ¼r den
	Latenzgewinn $\Delta := E[L_{\text{ohne}}] - E[L_{\mathrm{pf}}]$:
	\[
		\Delta = \min\bigl(T_{\mathrm{think}},\, T_{\mathrm{prep}}(s')\bigr) \;\geq\; 0.
	\]
	Insbesondere wird die Vorbereitungszeit $T_{\mathrm{prep}}(s')$ \emph{vollstÃ¤ndig}
	durch Prefetching verborgen, sobald $T_{\mathrm{think}} \geq T_{\mathrm{prep}}(s')$.
\end{satz}

\begin{proof}
	Wir unterscheiden zwei FÃ¤lle.

	\textbf{Fall 1: $T_{\mathrm{think}} \geq T_{\mathrm{prep}}(s')$.} Dann ist
	$T_{\mathrm{prep}}(s') - T_{\mathrm{think}} \leq 0$, also
	$\max(T_{\mathrm{prep}}(s')-T_{\mathrm{think}},0) = 0$ und somit
	$E[L_{\mathrm{pf}}] = T_{\mathrm{exec}}(\sigma)$. Es folgt
	\[
		\Delta = \bigl(T_{\mathrm{prep}}(s') + T_{\mathrm{exec}}(\sigma)\bigr)
		- T_{\mathrm{exec}}(\sigma) = T_{\mathrm{prep}}(s')
		= \min(T_{\mathrm{think}}, T_{\mathrm{prep}}(s')).
	\]

	\textbf{Fall 2: $T_{\mathrm{think}} < T_{\mathrm{prep}}(s')$.} Dann ist
	$T_{\mathrm{prep}}(s')-T_{\mathrm{think}} > 0$, also
	$\max(T_{\mathrm{prep}}(s')-T_{\mathrm{think}},0) = T_{\mathrm{prep}}(s')-T_{\mathrm{think}}$
	und somit
	\[
		E[L_{\mathrm{pf}}] = \bigl(T_{\mathrm{prep}}(s')-T_{\mathrm{think}}\bigr)
		+ T_{\mathrm{exec}}(\sigma).
	\]
	Es folgt
	\[
		\Delta = \bigl(T_{\mathrm{prep}}(s')+T_{\mathrm{exec}}(\sigma)\bigr)
		- \bigl((T_{\mathrm{prep}}(s')-T_{\mathrm{think}})+T_{\mathrm{exec}}(\sigma)\bigr)
		= T_{\mathrm{think}}
		= \min(T_{\mathrm{think}}, T_{\mathrm{prep}}(s')).
	\]

	In beiden FÃ¤llen gilt $\Delta = \min(T_{\mathrm{think}}, T_{\mathrm{prep}}(s'))$, und
	da $T_{\mathrm{think}}, T_{\mathrm{prep}}(s') \geq 0$ ist auch $\Delta \geq 0$.
	\qedhere
\end{proof}

\begin{bemerkung}[Kosten des Prefetchings]
	\label{bem:prefetch-kosten}
	Satz~\ref{satz:latenzgewinn} betrachtet nur die \emph{Latenz aus Sicht des Clients}.
	Die \emph{Kosten} des Prefetchings fÃ¼r den Server skalieren mit dem
	Verzweigungsgrad $d(s)=|N(s)|$ (Definition~\ref{def:prozessautomat}): FÃ¼r jeden der
	$d(s)$ mÃ¶glichen FolgezustÃ¤nde muss eine Vorbereitung angestoÃŸen werden, von denen
	hÃ¶chstens eine tatsÃ¤chlich genutzt wird (\emph{spekulative Verschwendung}). Ein
	SC-REST-Server sollte daher
	\begin{itemize}
		\item den Verzweigungsgrad $d(s)$ pro Zustand durch Prozessdesign begrenzen
		      (z.\,B. $d(s) \leq D_{\max}$ fÃ¼r ein konfigurierbares $D_{\max}$),
		\item Vorbereitungsoperationen mit geringen Kosten $T_{\mathrm{prep}}(s')$
		      priorisieren, da nach Satz~\ref{satz:latenzgewinn} der Gewinn durch
		      $\min(T_{\mathrm{think}},T_{\mathrm{prep}}(s'))$ fÃ¼r kleine
		      $T_{\mathrm{prep}}(s')$ ohnehin gering ist, wÃ¤hrend die
		      \emph{verschwendeten} Ressourcen proportional zu $T_{\mathrm{prep}}(s')$
		      anfallen,
		\item idempotente bzw. abbrechbare Vorbereitungsoperationen verwenden, sodass im
		      Fall einer nicht genutzten Vorbereitung kein inkonsistenter Zustand
		      entsteht.
	\end{itemize}
	Die empirische Auswertung in Abschnitt~\ref{sec:empirisch} (Abbildung
	\ref{fig:lernkurve}, rechts) zeigt den kumulierten Effekt des Latenzgewinns Ã¼ber
	mehrere Prozessinstanzen unter realistischer Trefferquote.
\end{bemerkung}

\begin{figure}[h]
	\centering
	\includegraphics[width=\textwidth]{plots/plot2_latenz.pdf}
	\caption{Links: Erwartete Antwortlatenz $E[L]$ in AbhÃ¤ngigkeit von der Bedenkzeit
	$T_{\mathrm{think}}$ des Clients, fÃ¼r $T_{\mathrm{prep}}=100\,\mathrm{ms}$ und
	$T_{\mathrm{exec}}=50\,\mathrm{ms}$. Ohne Prefetching (rot, gestrichelt) ist die
	Latenz konstant $T_{\mathrm{prep}}+T_{\mathrm{exec}}=150\,\mathrm{ms}$, unabhÃ¤ngig von
	$T_{\mathrm{think}}$. Mit Prefetching (blau) sinkt die Latenz linear mit
	$T_{\mathrm{think}}$ bis zur unteren Schranke $T_{\mathrm{exec}}$, die bei
	$T_{\mathrm{think}}\geq T_{\mathrm{prep}}$ erreicht wird. Rechts: Latenzgewinn
	$\Delta=\min(T_{\mathrm{think}},T_{\mathrm{prep}})$ gemÃ¤ÃŸ
	Satz~\ref{satz:latenzgewinn} -- ein stÃ¼ckweise linearer, konkaver Verlauf mit
	SÃ¤ttigung bei $T_{\mathrm{prep}}$.}
	\label{fig:latenz}
\end{figure}

\subsection{Robustheit: Idempotenz und Replay-Sicherheit}
\label{sec:robustheit}

\begin{definition}[Erweiterter Automat mit Idempotenz-Schleifen]
	\label{def:idempotenz}
	Sei $P=(S,\Sigma,\delta,s_0,F)$ ein Prozessautomat. Der \emph{idempotent erweiterte
	Automat} $P^{+}=(S,\Sigma,\delta^{+},s_0,F)$ ist definiert durch
	\[
		\delta^{+}(s,\sigma) =
		\begin{cases}
			\delta(s,\sigma), & \text{falls } \delta(s,\sigma) \text{ definiert ist}, \\
			s, & \text{falls } \exists\, s_{\mathrm{prev}}\in S:\ \delta(s_{\mathrm{prev}},\sigma)=s
			      \text{ und } \sigma \text{ die zuletzt erfolgreich}\\
			   & \text{ausgefÃ¼hrte Operation war}, \\
			\text{undef}, & \text{sonst.}
		\end{cases}
	\]
	Das heiÃŸt: Wiederholt ein Client genau die Operation, die ihn zuletzt erfolgreich in
	den aktuellen Zustand $s$ Ã¼berfÃ¼hrt hat (z.\,B. weil die ursprÃ¼ngliche Antwort durch
	einen Netzwerkfehler verloren ging), so verbleibt der Automat in $s$ und liefert
	erneut die (gecachte) Erfolgsantwort, statt eine Sequenzverletzung zu melden.
\end{definition}

\begin{satz}[Replay-Sicherheit]
	\label{satz:replay}
	Sei $P^{+}$ der idempotent erweiterte Automat zu $P$ gemÃ¤ÃŸ
	Definition~\ref{def:idempotenz}, und sei $c=(c_1,\ldots,c_k)$ vollstÃ¤ndig konform
	bezÃ¼glich $P$ mit Lauf $s_0,\ldots,s_k$. Sei $\hat c$ eine Sequenz, die aus $c$ durch
	beliebig hÃ¤ufige unmittelbare Wiederholung einzelner Elemente entsteht (d.\,h. jedes
	$\hat c_j \in \{c_i, c_i\}$ fÃ¼r ein passendes $i$, wobei die relative Reihenfolge der
	$c_i$ erhalten bleibt und Wiederholungen stets unmittelbar auf das Original folgen).
	Dann ist $\hat c$ vollstÃ¤ndig konform bezÃ¼glich $P^{+}$, und der erreichte Endzustand
	ist identisch zu $s_k$.
\end{satz}

\begin{proof}
	Wir zeigen die Aussage durch Induktion Ã¼ber die Position $j$ in $\hat c$, mit der
	Invariante, dass nach Verarbeitung des durch $\hat c_1,\ldots,\hat c_j$ definierten
	PrÃ¤fixes der Zustand von $P^{+}$ gleich $s_i$ ist, wobei $i$ die Anzahl der
	\emph{nicht wiederholten} (d.\,h. â€žneuenâ€œ) Elemente in $\hat c_1,\ldots,\hat c_j$ ist.

	\textbf{Fall â€žneues Elementâ€œ:} Ist $\hat c_j = c_{i+1}$ ein bisher nicht in dieser
	Position wiederholtes Element, so gilt nach Induktionsannahme
	$\pi(id)=s_i$ und nach KonformitÃ¤t von $c$ ist $\delta(s_i,c_{i+1})=s_{i+1}$
	definiert; nach Definition~\ref{def:idempotenz} gilt $\delta^{+}=\delta$ in diesem
	Fall, also $\pi(id):=s_{i+1}$.

	\textbf{Fall â€žWiederholungâ€œ:} Ist $\hat c_j$ eine unmittelbare Wiederholung von
	$\hat c_{j-1}=c_{i+1}$ (der zuletzt erfolgreich ausgefÃ¼hrten Operation, die
	$\pi(id)$ von $s_i$ nach $s_{i+1}$ Ã¼berfÃ¼hrt hat), so greift der zweite Fall von
	Definition~\ref{def:idempotenz} mit $s=s_{i+1}$, $s_{\mathrm{prev}}=s_i$,
	$\sigma=c_{i+1}$: $\delta^{+}(s_{i+1},c_{i+1})=s_{i+1}$, also bleibt
	$\pi(id)=s_{i+1}$ unverÃ¤ndert -- die Anzahl der â€žneuenâ€œ Elemente bleibt $i+1$, die
	Invariante bleibt erhalten.

	Nach Verarbeitung von $\hat c$ entspricht die Anzahl der â€žneuenâ€œ Elemente genau $k$
	(da $c$ vollstÃ¤ndig $k$ Elemente hat und jede Wiederholung per Definition keinem
	neuen Element entspricht), also ist $\pi(id)=s_k$. Da $s_k\in F$ (vollstÃ¤ndige
	KonformitÃ¤t von $c$), ist auch $\hat c$ vollstÃ¤ndig konform bezÃ¼glich $P^{+}$ mit
	demselben Endzustand. \qedhere
\end{proof}

\begin{bemerkung}
	Satz~\ref{satz:replay} zeigt, dass die strikte Sequenzkontrolle aus
	Definition~\ref{def:sc-rest-server} \emph{nicht} im Widerspruch zu robusten
	Netzwerkprotokollen steht: Ein Client, der wegen eines ZeitÃ¼berschreitung (Timeout)
	eine erfolgreiche Anfrage erneut sendet, wird durch $P^{+}$ nicht fÃ¤lschlich mit
	\texttt{409 Conflict} abgewiesen. In der Praxis wird $\delta^{+}$ Ã¼blicherweise mittels
	eines \emph{Idempotency-Key}-Headers realisiert (vgl. die Diskussion bei
	Helland~\cite{helland2007} zu â€žat-least-onceâ€œ-Semantik in verteilten Systemen): Der
	Server speichert zu jedem $(id, \text{Idempotency-Key})$-Paar die zuletzt gesendete
	Antwort und liefert diese bei Wiederholung erneut aus, ohne die Fachlogik erneut
	auszufÃ¼hren.
\end{bemerkung}

\section{Empirische Veranschaulichung}
\label{sec:empirisch}

Zur Veranschaulichung der formalen Ergebnisse aus Abschnitt~\ref{sec:analyse} wird die
EinfÃ¼hrung eines SC-REST-Servers in einem simulierten Szenario betrachtet: Ein
Integrations-Team bindet schrittweise einen neuen Client an den Bestellprozess aus
Beispiel~\ref{bsp:bestellprozess} (Abbildung~\ref{fig:automat}) an.

\begin{figure}[h]
	\centering
	\includegraphics[width=\textwidth]{plots/plot4_lernkurve.pdf}
	\caption{Links: Simulierter Anteil von \texttt{409 Conflict}-Antworten Ã¼ber
	$n=1,\ldots,30$ bearbeitete Prozessinstanzen, modelliert als exponentieller
	Lernprozess $p(n)=p_\infty+(p_0-p_\infty)e^{-\lambda n}$ mit $p_0=0{,}42$,
	$p_\infty=0{,}015$, $\lambda=0{,}18$ (durchgezogene blaue Kurve) und einer
	binomialverteilten Stichprobe mit $m=40$ Anfragen je Instanz (rote Balken,
	Zufallsgenerator-Seed $42$). Die anfÃ¤nglich hohe Fehlerrate sinkt rasch, da
	Sequenzverletzungen gemÃ¤ÃŸ Satz~\ref{satz:korrektheit}(c) sofort und mit prÃ¤ziser
	\texttt{allowedOperations}-Angabe zurÃ¼ckgemeldet werden -- der Client kann sein
	Verhalten unmittelbar korrigieren. Eine Restfehlerrate $p_\infty>0$ bleibt bestehen
	(z.\,B. durch seltene Edge-Cases oder konkurrierende Prozessinstanzen). Rechts:
	Kumulierte Latenzeinsparung gemÃ¤ÃŸ $\sum_n (1-p(n))\cdot m \cdot T_{\mathrm{prep}}$ mit
	$T_{\mathrm{prep}}=100\,\mathrm{ms}$ -- jede konform abgeschlossene Anfrage profitiert
	vom in Satz~\ref{satz:latenzgewinn} bewiesenen Latenzgewinn durch Prefetching, sodass
	sich der Gesamtgewinn Ã¼ber viele Prozessinstanzen linear mit der Trefferquote
	$1-p(n)$ aufsummiert.}
	\label{fig:lernkurve}
\end{figure}

Die Kombination der Abbildungen~\ref{fig:automat}, \ref{fig:latenz},
\ref{fig:komplexitaet} und \ref{fig:lernkurve} veranschaulicht den vollstÃ¤ndigen
Wirkungszusammenhang des SC-REST-Patterns: Der Prozessautomat
(Abbildung~\ref{fig:automat}) definiert sowohl die zulÃ¤ssigen Sequenzen als auch die
Menge $\Sigma(s)$, die im Fehlerfall kommuniziert wird (Satz~\ref{satz:korrektheit});
diese Information ermÃ¶glicht serverseitiges Prefetching mit beweisbarem Latenzgewinn
(Abbildung~\ref{fig:latenz}, Satz~\ref{satz:latenzgewinn}); fÃ¼r lineare Abschnitte des
Prozesses bleibt die PrÃ¼fung dabei $\Theta(k)$-optimal, wÃ¤hrend permutierbare Abschnitte
strukturell auf den optimalen Subgraph Algorithmus zurÃ¼ckgefÃ¼hrt werden
(Abbildung~\ref{fig:komplexitaet}, Satz~\ref{satz:permutierbar-subgraph}); und der
Gesamtnutzen kumuliert Ã¼ber den Lebenszyklus einer Client-Integration
(Abbildung~\ref{fig:lernkurve}).

\section{Implementierung}
\label{sec:implementierung}

Abschnitt~\ref{sec:pattern} hat bereits die Kernfunktion \texttt{handle\_request}
(Algorithmus~\ref{lst:transition}) eingefÃ¼hrt. Listing~\ref{lst:flask} zeigt eine
vollstÃ¤ndigere Referenzimplementierung als Flask-Middleware, die die
Ãœbergangsfunktion $\delta$ deklarativ definiert, Prefetching gemÃ¤ÃŸ
Abschnitt~\ref{sec:prefetch} anstÃ¶ÃŸt und die Problem-Detail-Antworten aus
Abschnitt~\ref{sec:pattern} erzeugt.

\begin{lstlisting}[language=Python, label=lst:flask, caption=Referenzimplementierung eines SC-REST-Prozessautomaten in Flask]
from dataclasses import dataclass, field
from typing import Dict, Tuple, Set, List
from concurrent.futures import ThreadPoolExecutor
from flask import Flask, jsonify, request, abort

app = Flask(__name__)
executor = ThreadPoolExecutor(max_workers=8)

@dataclass
class ProcessAutomaton:
    states: Set[str]
    delta: Dict[Tuple[str, str], str]
    start: str
    final: Set[str]
    pi: Dict[str, str] = field(default_factory=dict)

    def allowed_ops(self, state: str) -> List[str]:
        return [op for (s, op) in self.delta if s == state]

    def reachable_states(self, state: str) -> Set[str]:
        return {self.delta[(state, op)]
                for op in self.allowed_ops(state)}


# Definition 2: Prozessautomat fuer den Bestellprozess
order_process = ProcessAutomaton(
    states={"s0", "s1", "s2", "s3", "s4", "s5"},
    delta={
        ("s0", "POST /cart"):        "s1",
        ("s1", "POST /cart/items"):  "s2",
        ("s2", "POST /cart/items"):  "s2",
        ("s2", "POST /checkout"):    "s3",
        ("s3", "POST /payment"):     "s4",
        ("s4", "POST /confirm"):     "s5",
    },
    start="s0",
    final={"s5"},
)


def prefetch_resources(state: str) -> None:
    """Definition 9/10: spekulative Vorbereitung fuer alle s' in N(s)."""
    for target_state in order_process.reachable_states(state):
        executor.submit(prepare_state_resources, target_state)


def prepare_state_resources(target_state: str) -> None:
    # z.B. Inventar reservieren, Zahlungsdienstleister vorab
    # autorisieren, Bestaetigungs-Template vorrendern, ...
    ...


@app.route("/orders/<order_id>/<path:op_path>", methods=["POST"])
def process_step(order_id: str, op_path: str):
    operation = f"POST /{op_path}"
    current_state = order_process.pi.get(order_id, order_process.start)

    next_state = order_process.delta.get((current_state, operation))

    if next_state is None:
        # Satz 1 (Korrektheit, Fehlerlokalisierung):
        # Zustand bleibt unveraendert, Fehler ist eindeutig lokalisiert
        return jsonify({
            "type": "https://api.example.com/probs/sequence-violation",
            "title": "Ungueltige Operation fuer aktuellen Prozesszustand",
            "status": 409,
            "instance": f"/orders/{order_id}",
            "currentState": current_state,
            "attemptedOperation": operation,
            "allowedOperations":
                order_process.allowed_ops(current_state),
        }), 409

    # Konformer Uebergang (Satz 1b: Completeness)
    result = execute_business_logic(operation, order_id)
    order_process.pi[order_id] = next_state

    # Satz 5 (Latenzgewinn): Vorbereitung fuer N(next_state)
    # waehrend der Bedenkzeit des Clients anstossen
    prefetch_resources(next_state)

    return jsonify({
        "orderId": order_id,
        "state": next_state,
        "result": result,
        "_links": hateoas_links(order_process, next_state, order_id),
    }), 201
\end{lstlisting}

\section{Von SC-REST zu HST: Horizon State Transfer}
\label{sec:sst}

\subsection{Motivation: Das Navigationsdefizit von SC-REST}
\label{sec:hst-motivation}

Das Akronym REST steht fÃ¼r \emph{Representational State Transfer}; namensgebend ist die
BeschrÃ¤nkung der \emph{Zustandslosigkeit} der Kommunikation~\cite{fielding2000}. PrÃ¤zise
gefasst verlangt diese BeschrÃ¤nkung, dass jede Anfrage alle zur Bearbeitung notwendigen
Informationen enthÃ¤lt und der Server keinen \emph{Sitzungszustand} (\emph{session state})
fÃ¼hrt -- also keinen Zustand, der ausschlieÃŸlich der Kommunikation mit einem bestimmten
Client dient und ohne den frÃ¼here Anfragen dieses Clients nicht interpretierbar wÃ¤ren.
Davon zu unterscheiden ist der \emph{Ressourcenzustand} (in~\cite{fielding2000} auch als
Teil des \emph{application state} verstanden): persistente, Ã¼ber die Ressourcen-URI
adressierbare Daten, die unabhÃ¤ngig vom anfragenden Client existieren.

Die Bemerkung zur Zustandslosigkeit in Abschnitt~\ref{sec:pattern} stellt fest, dass
SC-REST diese Unterscheidung respektiert: $\pi(id)=s$ ist Ressourcenzustand, kein
Sitzungszustand. SC-REST ist insofern bereits REST-konform. Konsequent zu Ende gedacht
zeigt sich jedoch eine zweite, bislang unadressierte Asymmetrie zwischen Server und
Client -- das \emph{Navigationsdefizit}.

Formal kennt der Server zu jedem Zeitpunkt nicht nur den aktuellen Zustand
$\pi(id)=s$, sondern den \emph{gesamten} Prozessautomaten
$P=(S,\Sigma,\delta,s_0,F)$ -- insbesondere fÃ¼r jeden erreichbaren Zustand $s'\in N(s)$
wiederum $\Sigma(s')$, $N(s')$ usw. GemÃ¤ÃŸ Definition~\ref{def:sc-rest-server} Ã¼bermittelt
ein SC-REST-Server in seiner Antwort jedoch nur $\Sigma(\delta(s,\sigma))$ -- also
Informationen Ã¼ber genau \emph{einen} Schritt voraus. Ein Client, der $k>1$ Schritte
planen mÃ¶chte (etwa um zu entscheiden, ob ein mehrstufiger Vorgang Ã¼berhaupt zulÃ¤ssig
ist, oder um -- wie in Abschnitt~\ref{sec:prefetch} diskutiert -- Ressourcen fÃ¼r mehrere
Schritte vorzubereiten), muss dafÃ¼r $k$ aufeinanderfolgende Anfragen stellen,
\emph{obwohl} die hierfÃ¼r nÃ¶tige Information (die EinschrÃ¤nkung von $\delta$ auf den
relevanten Ausschnitt) bereits zum Zeitpunkt der ersten Antwort vollstÃ¤ndig und
deterministisch beim Server vorliegt.

\begin{bemerkung}
	\label{bem:navigationsdefizit}
	Das Navigationsdefizit ist kein VerstoÃŸ gegen die Zustandslosigkeit im Sinne
	von~\cite{fielding2000} -- die einzelne Anfrage bleibt selbstbeschreibend, und der
	Server speichert keinen client-spezifischen Sitzungszustand. Es handelt sich jedoch
	um eine \emph{UnvollstÃ¤ndigkeit der Ã¼bertragenen ReprÃ¤sentation} gegenÃ¼ber dem, was
	REST mit der BeschrÃ¤nkung der \emph{SelbstbeschreibungsfÃ¤higkeit} und insbesondere
	mit HATEOAS anstrebt: Eine ReprÃ¤sentation soll dem Client ermÃ¶glichen, ohne Ã¼ber die
	Anwendung hinausgehendes Wissen fortzufahren. SC-REST realisiert HATEOAS nur mit
	\emph{Horizont $h=1$} -- der folgende Abschnitt fÃ¼hrt den Horizont $h$ als expliziten
	Parameter ein und verallgemeinert SC-REST entsprechend.
\end{bemerkung}

Ziel dieses Abschnitts ist die formale Definition eines Patterns, das dieses Defizit
behebt, ohne die Zustandslosigkeit (Abschnitt~\ref{sec:hst-zustandslosigkeit})
einzuschrÃ¤nken. Wir nennen dieses Pattern \emph{Horizon State Transfer (HST)}, da --
wie sich zeigen wird -- die zusÃ¤tzlich Ã¼bertragene Information genau der von $s$ aus in
hÃ¶chstens $h$ Schritten erreichbare Teilgraph des Prozessautomaten ist
(formal behandelt in~\cite{epp2026subgraph}), also der \emph{Navigationshorizont} der
Tiefe $h$. Der Name ist bewusst in Analogie zu \emph{Representational State Transfer}
gewÃ¤hlt: WÃ¤hrend REST in jeder Antwort eine \emph{ReprÃ¤sentation} des aktuellen Zustands
Ã¼bertrÃ¤gt, Ã¼bertrÃ¤gt HST zusÃ¤tzlich den vollstÃ¤ndigen \emph{Horizont} $G_s^h$ des
Zustandsraums -- den lokal relevanten Ausschnitt des Navigationswissens, das der Server
ohnehin vollstÃ¤ndig besitzt, begrenzt durch den Parameter $h$.

\subsection{Das HST-Pattern: Formale Definition}
\label{sec:hst-definition}

\begin{definition}[$h$-Schritt-Horizont-Subgraph]
	\label{def:horizont-subgraph}
	Sei $P=(S,\Sigma,\delta,s_0,F)$ ein Prozessautomat, $s\in S$ und $h\in\mathbb{N}_0$.
	Wir fassen $P$ als gerichteten, mit $\Sigma$ beschrifteten Graphen $(S,\to)$ auf, mit
	$s_1\xrightarrow{\sigma}s_2 :\Leftrightarrow \delta(s_1,\sigma)=s_2$. FÃ¼r
	$(c_1,\ldots,c_k)\in\Sigma^k$ sei $\delta^{*}(s,(c_1,\ldots,c_k))$ der gemÃ¤ÃŸ
	Definition~\ref{def:lauf} erreichte Zustand nach AusfÃ¼hrung von
	$(c_1,\ldots,c_k)$ ab $s$, mit $\delta^{*}(s,())=s$. Der \emph{$h$-Schritt-Horizont-Subgraph}
	$G_s^h=(V_s^h,E_s^h)$ ist definiert durch
	\[
		V_s^h := \bigl\{\, s' \in S \;\bigm|\; \exists\, k\le h,\ \exists\, c\in\Sigma^k:\
		s'=\delta^{*}(s,c) \,\bigr\}
	\]
	und, fÃ¼r $h\ge1$,
	\[
		E_s^h := \bigl\{\, (s_1,\sigma,s_2) \;\bigm|\; s_1\in V_s^{h-1},\ \sigma\in\Sigma(s_1),\
		s_2=\delta(s_1,\sigma) \,\bigr\},
	\]
	mit $E_s^0:=\emptyset$. Jeder Knoten $s'\in V_s^h\setminus V_s^{h-1}$ (fÃ¼r $h\ge1$)
	heiÃŸt \emph{Randknoten} von $G_s^h$; die Menge der Randknoten wird mit $\partial
	G_s^h$ bezeichnet.
\end{definition}

Anschaulich ist $G_s^h$ der induzierte Teilgraph, der durch eine Breitensuche (BFS) der
Tiefe $h$ ab $s$ im Prozessautomaten entsteht -- einschlieÃŸlich aller Kanten, deren
Startknoten in hÃ¶chstens $h-1$ Schritten ab $s$ erreichbar ist.

\begin{bemerkung}
	\label{bem:sc-rest-spezialfall}
	$G_s^0=(\{s\},\emptyset)$, und $G_s^1$ entspricht genau der in
	Definition~\ref{def:sc-rest-server} Ã¼bertragenen Information: $V_s^1=\{s\}\cup N(s)$
	mit den Kanten $\{(s,\sigma,\delta(s,\sigma))\mid \sigma\in\Sigma(s)\}$. \textbf{SC-REST
	ist somit der Spezialfall $h=1$ von HST.}
\end{bemerkung}

\begin{definition}[HST-ReprÃ¤sentation]
	\label{def:hst-repraesentation}
	Sei $(id,s)$ eine Prozessinstanz gemÃ¤ÃŸ Definition~\ref{def:prozessinstanz} und
	$h\in\mathbb{N}$ ein (server- oder anwendungsseitig konfigurierter)
	\emph{Horizontparameter}. Die \emph{HST-ReprÃ¤sentation} der Prozessinstanz ist das
	Tupel
	\[
		R(id,s,h) := \bigl(\, \rho(id,s),\ s,\ G_s^h \,\bigr),
	\]
	wobei $\rho(id,s)$ die fachliche RessourcenreprÃ¤sentation (Nutzdaten der
	Prozessinstanz) bezeichnet und $G_s^h$ gemÃ¤ÃŸ Definition~\ref{def:horizont-subgraph}
	als annotierter Graph Ã¼bertragen wird: Jeder Knoten $s'\in V_s^h$ wird auf eine URI
	(bzw.\ einen URI-Template-Parameter) abgebildet, jede Kante $(s_1,\sigma,s_2)\in
	E_s^h$ auf einen HATEOAS-Link mit Methode, Pfad und annotiertem Zielzustand $s_2$.
\end{definition}

\begin{definition}[HST-Operationssemantik]
	\label{def:hst-server}
	Sei $P=(S,\Sigma,\delta,s_0,F)$ ein Prozessautomat, $\pi(id)=s$ der aktuelle Zustand
	einer Prozessinstanz und $h\in\mathbb{N}$ der Horizontparameter. Auf eine eingehende
	Anfrage $\sigma\in\Sigma$ reagiert ein \emph{HST-Server} wie folgt:
	\begin{enumerate}
		\item \textbf{Falls $\delta(s,\sigma)$ definiert ist:} Wie in
		Definition~\ref{def:sc-rest-server} (Fachlogik ausfÃ¼hren, $\pi(id):=
		\delta(s,\sigma)$), jedoch antwortet der Server mit der HST-ReprÃ¤sentation
		$R(id,\delta(s,\sigma),h)$ statt nur mit $\Sigma(\delta(s,\sigma))$.
		\item \textbf{Falls $\delta(s,\sigma)$ undefiniert ist:} Wie in
		Definition~\ref{def:sc-rest-server} (\texttt{409 Conflict} mit
		Problem-Detail-Objekt, $\pi(id)$ bleibt unverÃ¤ndert); zusÃ¤tzlich enthÃ¤lt die
		Antwort weiterhin $G_s^h$ (unverÃ¤ndert, da $\pi(id)$ unverÃ¤ndert bleibt), sodass
		der Client den Sequenzfehler unmittelbar im Kontext seines bereits bekannten
		Horizont-Subgraphen einordnen kann.
	\end{enumerate}
\end{definition}

\begin{beispiel}[Bestellprozess mit Horizont $h=2$]
	\label{bsp:hst-bestellprozess}
	FÃ¼r den Prozessautomaten aus Beispiel~\ref{bsp:bestellprozess} und $s=s_1$ (Zustand
	nach \texttt{POST /cart}) ergibt sich fÃ¼r $h=2$:
	\[
		V_{s_1}^2 = \{s_1,s_2,s_3\},
	\]
	\[
		\begin{aligned}
			E_{s_1}^2 = \bigl\{\, &(s_1,\texttt{POST /cart/items},s_2),\
				(s_2,\texttt{POST /cart/items},s_2),\\
				&(s_2,\texttt{POST /checkout},s_3) \,\bigr\}.
		\end{aligned}
	\]
	Ein Client, der $G_{s_1}^2$ erhÃ¤lt, kann \emph{ohne weitere Serveranfrage}
	feststellen, dass \texttt{POST /checkout} aus $s_1$ direkt nicht zulÃ¤ssig ist, jedoch
	nach genau einem \texttt{POST /cart/items} zulÃ¤ssig wird -- eine Information, die in
	der SC-REST-ReprÃ¤sentation ($h=1$, nur $\Sigma(s_1)=\{\texttt{POST /cart/items}\}$)
	nicht enthalten ist.
\end{beispiel}

\subsection{Zustandslosigkeit von HST}
\label{sec:hst-zustandslosigkeit}

\begin{satz}[Zustandslosigkeit von HST]
	\label{satz:hst-zustandslosigkeit}
	FÃ¼r jeden Horizont $h\in\mathbb{N}$ erfÃ¼llt ein HST-Server gemÃ¤ÃŸ
	Definition~\ref{def:hst-server} die Zustandslosigkeits-BeschrÃ¤nkung
	nach~\cite{fielding2000}: Der Server fÃ¼hrt keinen client-spezifischen
	Sitzungszustand, und jede Antwort ist eine deterministische Funktion ausschlieÃŸlich
	von (a) dem Prozessautomaten $P$ und (b) dem Ressourcenzustand $\pi(id)=s$.
\end{satz}

\begin{proof}
	Wir zeigen, dass $R(id,s,h)$ gemÃ¤ÃŸ Definition~\ref{def:hst-repraesentation}
	vollstÃ¤ndig durch $(P,id,s,h)$ bestimmt ist, unabhÃ¤ngig davon, welcher Client die
	Anfrage stellt oder welche Anfragen zuvor gestellt wurden.

	(1) $\rho(id,s)$ ist nach Definition~\ref{def:prozessinstanz} die fachliche
	ReprÃ¤sentation der Ressource $id$ im Zustand $s$ -- eine Funktion von $(id,s)$,
	unabhÃ¤ngig vom anfragenden Client.

	(2) $G_s^h$ ist nach Definition~\ref{def:horizont-subgraph} eine Funktion von
	$(P,s,h)$: Die Mengen $V_s^h$ und $E_s^h$ werden ausschlieÃŸlich mittels
	$\delta^{*}$ -- also iterierter Anwendung von $\delta$, einer Komponente von $P$ --
	aus $s$ berechnet. In diese Berechnung geht insbesondere keine Information Ã¼ber die
	IdentitÃ¤t des anfragenden Clients, Ã¼ber dessen vorherige Anfragen oder Ã¼ber andere
	Prozessinstanzen ein.

	(3) $h$ ist nach Voraussetzung ein server- oder anwendungsseitig (nicht
	client-sitzungsseitig) konfigurierter Parameter -- etwa eine Konstante des
	Prozessautomaten $P$ oder ein in der aktuellen Anfrage selbst Ã¼bermittelter Parameter
	(z.\,B.\ Query-Parameter \texttt{?horizon=2}); in beiden FÃ¤llen ist $h$ Teil der
	Eingabe der aktuellen Anfrage und keine gespeicherte Information.

	Damit ist $R(id,s,h)=(\rho(id,s),s,G_s^h)$ eine Funktion von $(P,id,s,h)$, wobei $P$
	statisch (bzw.\ unabhÃ¤ngig versioniert, siehe Abschnitt~\ref{sec:hst-cache}) und
	$\pi(id)=s$ Ressourcenzustand im Sinne der Bemerkung zur Zustandslosigkeit in
	Abschnitt~\ref{sec:pattern} ist. Insbesondere benÃ¶tigt der Server zur Beantwortung
	\emph{keinen} zusÃ¤tzlichen Speicher pro Client oder pro Sitzung -- die einzige
	persistente, Ã¼ber die Anfrage hinaus gespeicherte Information ist $\pi(id)$, welche
	bereits fÃ¼r SC-REST ($h=1$) als Ressourcenzustand klassifiziert wurde. Da diese
	Klassifikation nicht von $h$ abhÃ¤ngt, gilt sie fÃ¼r beliebiges $h\in\mathbb{N}$, also
	auch fÃ¼r HST. \qedhere
\end{proof}

\begin{bemerkung}
	\label{bem:zustandslosigkeit-frei}
	Satz~\ref{satz:hst-zustandslosigkeit} zeigt, dass die ErhÃ¶hung des Horizonts $h$
	\emph{zustandslosigkeitsneutral} ist: $h$ beeinflusst ausschlieÃŸlich die
	\emph{GrÃ¶ÃŸe} der Antwort (Abschnitt~\ref{sec:hst-groesse}), nicht deren Charakter als
	zustandslose, selbstbeschreibende Nachricht. Der vermeintliche Zielkonflikt zwischen
	â€žmehr Navigationsinformation fÃ¼r den Clientâ€œ und â€žREST-KonformitÃ¤tâ€œ -- der die in
	Bemerkung~\ref{bem:navigationsdefizit} beschriebene BeschrÃ¤nkung auf $h=1$ in der
	Praxis oft motiviert -- existiert formal nicht; lediglich der in
	Abschnitt~\ref{sec:hst-kosten} formalisierte Kosten-Nutzen-Tradeoff begrenzt $h$ auf
	einen endlichen, optimalen Wert $h^\ast$.
\end{bemerkung}
\subsection{GrÃ¶ÃŸe des Horizont-Subgraphen}
\label{sec:hst-groesse}

\begin{lemma}[GrÃ¶ÃŸenschranke fÃ¼r $G_s^h$]
	\label{lem:horizont-groesse}
	Sei $P=(S,\Sigma,\delta,s_0,F)$ ein Prozessautomat und $d:=\max_{s'\in S}d(s')$ der
	maximale Verzweigungsgrad (Definition~\ref{def:prozessautomat}). FÃ¼r jeden Zustand
	$s\in S$ und jeden Horizont $h\in\mathbb{N}_0$ gilt
	\[
		|V_s^h| \;\le\; \sum_{i=0}^{h} d^{\,i} \;=\;
		\begin{cases}
			h+1, & d=1,\\[2pt]
			\dfrac{d^{h+1}-1}{d-1}, & d\ne1.
		\end{cases}
	\]
	Gleichheit gilt genau dann, wenn der von $s$ aus erreichbare Teil von $P$ bis zur
	Tiefe $h$ ein Baum mit konstantem Verzweigungsgrad $d$ ist, d.\,h.\ wenn
	$\delta^{*}$ auf Pfaden der LÃ¤nge $\le h$ ab $s$ injektiv ist (keine zwei
	verschiedenen solchen Pfade fÃ¼hren zum selben Zustand) und jeder Zustand auf Tiefe
	$<h$ genau $d$ ausgehende Operationen besitzt.
\end{lemma}

\begin{proof}
	Wir zerlegen $V_s^h$ in Schichten $L_0,\ldots,L_h$ mit $L_0:=\{s\}$ und, fÃ¼r
	$i\ge1$, $L_i:=V_s^i\setminus V_s^{i-1}$ (mit $V_s^{-1}:=\emptyset$). Wir zeigen
	$|L_i|\le d^i$ fÃ¼r alle $i\le h$ durch Induktion Ã¼ber $i$.

	\emph{Induktionsanfang} ($i=0$): $L_0=\{s\}$, also $|L_0|=1=d^0$.

	\emph{Induktionsschritt}: Sei $|L_{i-1}|\le d^{i-1}$ gezeigt. Jeder Knoten
	$s'\in L_i$ ist nach Definition~\ref{def:horizont-subgraph} von der Form
	$s'=\delta(s'',\sigma)$ fÃ¼r ein $s''\in L_{i-1}$ und $\sigma\in\Sigma(s'')$ -- denn
	$s'\in V_s^i\setminus V_s^{i-1}$ bedeutet, dass der kÃ¼rzeste Pfad von $s$ nach $s'$
	genau LÃ¤nge $i$ hat, also Ã¼ber einen Knoten $s''$ auf Tiefe $i-1$ verlÃ¤uft
	(andernfalls wÃ¤re $s'$ bereits Ã¼ber einen kÃ¼rzeren Pfad in $V_s^{i-1}$ enthalten).
	Da $d(s'')\le d$ fÃ¼r alle $s''\in S$ (Definition von $d$ als Maximum), gibt es zu
	jedem $s''\in L_{i-1}$ hÃ¶chstens $d$ Operationen $\sigma\in\Sigma(s'')$ und damit
	hÃ¶chstens $d$ Kandidaten fÃ¼r Nachfolgeknoten. Da
	$L_i\subseteq\bigcup_{s''\in L_{i-1}}\{\delta(s'',\sigma)\mid\sigma\in\Sigma(s'')\}$,
	folgt
	\[
		|L_i| \le |L_{i-1}|\cdot d \le d^{i-1}\cdot d = d^i.
	\]

	Damit folgt $|V_s^h|=\sum_{i=0}^{h}|L_i|\le\sum_{i=0}^{h}d^i$.

	\emph{Gleichheitsfall}: Gleichheit $|L_i|=d\cdot|L_{i-1}|$ fÃ¼r alle $i\le h$ gilt
	genau dann, wenn (a) jeder Knoten $s''\in L_{i-1}$ \emph{genau} $d$ ausgehende
	Operationen besitzt (der maximale Verzweigungsgrad wird an jedem dieser Knoten
	tatsÃ¤chlich erreicht) \emph{und} (b) die resultierenden $d\cdot|L_{i-1}|$
	Nachfolgeknoten paarweise verschieden sind und nicht bereits in $V_s^{i-1}$ liegen.
	Bedingungen (a) und (b) fÃ¼r alle $i\le h$ sind Ã¤quivalent dazu, dass $\delta^{*}$ auf
	Pfaden der LÃ¤nge $\le h$ ab $s$ injektiv ist und jeder Knoten auf Tiefe $<h$
	Verzweigungsgrad genau $d$ hat -- d.\,h.\ der von $s$ aus erreichbare Teilgraph bis
	Tiefe $h$ ist ein $d$-nÃ¤rer Baum. \qedhere
\end{proof}

\begin{bemerkung}
	\label{bem:horizont-groesse-praxis}
	Lemma~\ref{lem:horizont-groesse} zeigt, dass $|V_s^h|$ fÃ¼r $d\ge2$
	\emph{exponentiell} in $h$ wÃ¤chst. FÃ¼r die in Abschnitt~\ref{sec:permutierbar}
	betrachteten SammelzustÃ¤nde mit $r$ permutierbaren optionalen Teilschritten ist dies
	kein Widerspruch zu Bemerkung~\ref{bem:permutierbar-fazit}: Dort wird $|S|\ge2^r$ als
	\emph{untere} Schranke fÃ¼r die naive Kodierung des \emph{gesamten} Automaten gezeigt,
	wÃ¤hrend Lemma~\ref{lem:horizont-groesse} eine \emph{obere} Schranke fÃ¼r den
	\emph{lokal pro Anfrage Ã¼bertragenen} Teilgraphen $G_s^h$ liefert -- fÃ¼r $h<r$ ist
	$|V_s^h|$ um GrÃ¶ÃŸenordnungen kleiner als $|S|$. Abbildung~\ref{fig:horizont-groesse}
	illustriert das Wachstum von $|V_s^h|$ fÃ¼r $d\in\{1,2,3\}$.
\end{bemerkung}

\begin{figure}[ht]
	\centering
	\begin{tikzpicture}[
		every node/.style={circle, draw, minimum size=7mm, font=\footnotesize},
		level distance=13mm,
		level 1/.style={sibling distance=30mm},
		level 2/.style={sibling distance=15mm},
		edge from parent/.style={draw, -{Stealth[length=2mm]}},
		>=Stealth
		]
		\node[fill=mainblue!25, draw=mainblue, line width=0.9pt] (s) {$s$}
			child { node[fill=mainblue!25, draw=mainblue, line width=0.9pt] (a) {$s_a$}
				child { node[fill=mainblue!25, draw=mainblue, line width=0.9pt] {$s_{a1}$}
					child { node[draw=lightgray, text=darkgray, dashed] {$\cdots$} edge from parent[draw=lightgray, dashed] }
				}
				child { node[fill=mainblue!25, draw=mainblue, line width=0.9pt] {$s_{a2}$}
					child { node[draw=lightgray, text=darkgray, dashed] {$\cdots$} edge from parent[draw=lightgray, dashed] }
				}
			}
			child { node[fill=mainblue!25, draw=mainblue, line width=0.9pt] (b) {$s_b$}
				child { node[fill=mainblue!25, draw=mainblue, line width=0.9pt] {$s_{b1}$}
					child { node[draw=lightgray, text=darkgray, dashed] {$\cdots$} edge from parent[draw=lightgray, dashed] }
				}
				child { node[fill=mainblue!25, draw=mainblue, line width=0.9pt] {$s_{b2}$}
					child { node[draw=lightgray, text=darkgray, dashed] {$\cdots$} edge from parent[draw=lightgray, dashed] }
				}
			};
		\node[draw=none, fill=none, font=\scriptsize] at ($(s)!0.4!(a) + (-0.25,0)$) {$\sigma_1$};
		\node[draw=none, fill=none, font=\scriptsize] at ($(s)!0.4!(b) + (0.25,0)$) {$\sigma_2$};
		\draw[dashed, accentred, line width=0.8pt, rounded corners]
			($(s.north)+(-4.6,0.35)$) rectangle ($(b.south)+(4.6,-1.55)$);
		\node[draw=none, fill=none, shape=rectangle, accentred, font=\scriptsize, anchor=north west]
			at ($(s.north)+(-4.55,0.3)$) {Horizont $h=2$: $G_s^2$};
	\end{tikzpicture}
	\caption{Schematische Darstellung des Horizont-Subgraphen $G_s^2$ (blau, innerhalb
	der gestrichelten roten Umrandung) fÃ¼r einen Prozessautomaten mit Verzweigungsgrad
	$d=2$ ab dem aktuellen Zustand $s$. GemÃ¤ÃŸ Lemma~\ref{lem:horizont-groesse} enthÃ¤lt
	$G_s^2$ hÃ¶chstens $\sum_{i=0}^2 2^i=1+2+4=7$ Knoten -- hier mit Gleichheit, da der
	dargestellte Ausschnitt ein binÃ¤rer Baum ist. Die hellgrau gestrichelten Knoten
	($\cdots$) liegen auf Tiefe $3$, also auÃŸerhalb des Horizonts, und sind dem Client
	ohne weitere Anfrage nicht bekannt: Erst nach einem ZustandsÃ¼bergang innerhalb des
	Horizonts (z.\,B.\ $s\xrightarrow{\sigma_1}s_a$) liefert der Server den
	\emph{aktualisierten} Subgraphen $G_{s_a}^2$, dessen Randknoten dann diese zuvor
	verborgenen ZustÃ¤nde enthÃ¤lt. Der Horizont â€žwandertâ€œ also mit der Prozessinstanz
	mit, ohne dass der Server jemals den vollstÃ¤ndigen Automaten $P$ in einer einzelnen
	Antwort Ã¼bertragen muss.}
	\label{fig:horizont-baum}
\end{figure}

\begin{figure}[ht]
	\centering
	\includegraphics[width=\textwidth]{plots/plot5_horizont_groesse.pdf}
	\caption{Links: Anzahl der Knoten $|V_s^h|$ des Horizont-Subgraphen (logarithmische
	Skala) in AbhÃ¤ngigkeit vom Horizont $h$, fÃ¼r maximale Verzweigungsgrade $d=1$
	(lineare Prozessabschnitte ohne Verzweigung, z.\,B.\ der Hauptpfad in
	Beispiel~\ref{bsp:bestellprozess}), $d=2$ und $d=3$, gemÃ¤ÃŸ der Schranke aus
	Lemma~\ref{lem:horizont-groesse} (mit angenommener Gleichheit als Worst-Case). FÃ¼r
	$d=1$ wÃ¤chst $|V_s^h|$ nur linear ($h+1$), fÃ¼r $d\ge2$ exponentiell -- bereits fÃ¼r
	$d=3$ und $h=8$ ergeben sich knapp $10^4$ Knoten. Rechts: geschÃ¤tzte
	ÃœbertragungsgrÃ¶ÃŸe der HST-ReprÃ¤sentation $R(id,s,h)$ in Kilobyte, unter der
	konservativen Annahme von ca.\ 250\,Byte pro Knoten bzw.\ Kante (URI, Methode,
	Zustandskennung, JSON-Strukturoverhead). Die gestrichelte Linie markiert 8\,KB als
	groben Richtwert fÃ¼r eine â€žleichtgewichtigeâ€œ HTTP-Antwort. FÃ¼r $d=2$ wird dieser Wert
	bereits bei $h\approx4$, fÃ¼r $d=3$ bei $h\approx3$ Ã¼berschritten -- dies motiviert
	den in Abschnitt~\ref{sec:hst-kosten} formalisierten kostenoptimalen Horizont
	$h^\ast$, der fÃ¼r praxisrelevante Parameter deutlich kleiner ist als die in
	Abschnitt~\ref{sec:permutierbar} betrachteten Werte $r\le20$.}
	\label{fig:horizont-groesse}
\end{figure}

\subsection{Lokale VollstÃ¤ndigkeit und Planungshorizont}
\label{sec:hst-vollstaendigkeit}

\begin{satz}[Lokale VollstÃ¤ndigkeit von $G_s^h$]
	\label{satz:lokale-vollstaendigkeit}
	Sei $(id,s)$ eine Prozessinstanz und $G_s^h$ der zugehÃ¶rige Horizont-Subgraph gemÃ¤ÃŸ
	Definition~\ref{def:horizont-subgraph}. FÃ¼r jede Aufrufsequenz $c=(c_1,\ldots,c_k)$
	mit $k\le h$ gilt: Die Frage, ob $c$ ab $s$ (partiell) konform ist
	(Definition~\ref{def:konformitaet}), und -- falls ja -- welcher Zustand
	$\delta^{*}(s,c)$ erreicht wird, ist \emph{vollstÃ¤ndig aus $G_s^h$ entscheidbar},
	ohne weitere Anfrage an den Server.
\end{satz}

\begin{proof}
	Wir zeigen zunÃ¤chst die monotone Inklusion $V_s^i\subseteq V_s^j$ fÃ¼r $i\le j$: Jeder
	in hÃ¶chstens $i$ Schritten ab $s$ erreichbare Zustand ist insbesondere auch in
	hÃ¶chstens $j\ge i$ Schritten erreichbar (man kann dieselbe Sequenz verwenden), also
	folgt die Inklusion direkt aus Definition~\ref{def:horizont-subgraph}.

	Wir zeigen die Behauptung nun durch Induktion Ã¼ber $k\le h$.

	\emph{Induktionsanfang} ($k=0$): $c=()$ ist nach Definition~\ref{def:konformitaet}
	trivial konform mit $\delta^{*}(s,())=s\in V_s^h$ fÃ¼r jedes $h\ge0$.

	\emph{Induktionsschritt}: Sei die Aussage fÃ¼r $k-1<h$ Schritte gezeigt, d.\,h.\ es
	existiert (unter der Annahme, dass $(c_1,\ldots,c_{k-1})$ konform ist) ein Zustand
	$s_{k-1}:=\delta^{*}(s,(c_1,\ldots,c_{k-1}))\in V_s^{k-1}$, und dieser Zustand sowie
	die KonformitÃ¤t von $(c_1,\ldots,c_{k-1})$ sind aus $G_s^h$ bestimmbar. Wegen
	$k-1\le h-1$ und der oben gezeigten Monotonie gilt $s_{k-1}\in V_s^{h-1}$.

	Zu prÃ¼fen ist, ob $\delta(s_{k-1},c_k)$ definiert ist. Da $s_{k-1}\in V_s^{h-1}$, ist
	nach Definition~\ref{def:horizont-subgraph} \emph{jede} Operation
	$\sigma\in\Sigma(s_{k-1})$ Quelle einer Kante
	$(s_{k-1},\sigma,\delta(s_{k-1},\sigma))\in E_s^h$ -- die Kantenmenge $E_s^h$
	enthÃ¤lt per Definition alle ausgehenden Kanten \emph{jedes} Knotens aus
	$V_s^{h-1}$, unabhÃ¤ngig davon, ob deren Zielknoten neu (Randknoten von $G_s^h$)
	oder bereits in $V_s^{h-1}$ enthalten ist. Somit gilt:
	\begin{itemize}
		\item $\delta(s_{k-1},c_k)$ ist definiert $\iff$ es existiert eine Kante
		      $(s_{k-1},c_k,s_k)\in E_s^h$ fÃ¼r ein $s_k\in V_s^h$ -- diese Bedingung ist
		      direkt aus der (endlichen) Kantenmenge $E_s^h$ ablesbar.
		\item Ist $\delta(s_{k-1},c_k)$ definiert, so ist der gemÃ¤ÃŸ
		      Definition~\ref{def:prozessautomat} eindeutige Zielknoten $s_k$ dieser
		      Kante genau $\delta^{*}(s,(c_1,\ldots,c_k))$, und $s_k\in V_s^{k}\subseteq
		      V_s^h$ (wieder nach Monotonie, $k\le h$).
		\item Ist $\delta(s_{k-1},c_k)$ undefiniert, so ist $c$ ab Schritt $k$ nicht
		      konform -- auch diese Information ist aus $E_s^h$ ablesbar (Abwesenheit
		      einer entsprechenden Kante mit Quelle $s_{k-1}$ und Beschriftung $c_k$).
	\end{itemize}
	In beiden FÃ¤llen ist die KonformitÃ¤t von $c$ bis Schritt $k$ und ggf.\ der erreichte
	Zustand $\delta^{*}(s,(c_1,\ldots,c_k))$ vollstÃ¤ndig aus $G_s^h$ bestimmbar. Per
	Induktion folgt die Behauptung fÃ¼r alle $k\le h$. \qedhere
\end{proof}

\begin{bemerkung}
	\label{bem:hst-permutierbar}
	Satz~\ref{satz:lokale-vollstaendigkeit} hat eine direkte Konsequenz fÃ¼r die in
	Abschnitt~\ref{sec:permutierbar} behandelten SammelzustÃ¤nde: Ist $s_c$ ein
	Sammelzustand mit $r$ permutierbaren optionalen Teilschritten
	(Definition~\ref{def:sammelzustand}) und wÃ¤hlt der Server den Horizont $h\ge r$, so
	enthÃ¤lt $G_{s_c}^h$ -- mit $d=2$ an jedem Zwischenzustand (Operation $\sigma_j$
	ausfÃ¼hren oder nicht) und damit nach Lemma~\ref{lem:horizont-groesse} mit
	$|V_{s_c}^h|\le\sum_{i=0}^r 2^i=2^{r+1}-1$ Knoten -- den \emph{vollstÃ¤ndigen}
	Entscheidungsbaum aller $r!$ mÃ¶glichen AusfÃ¼hrungsreihenfolgen. Ein Client kann damit
	fÃ¼r eine \emph{beliebige}, lokal geplante Reihenfolge $c'$ der $r$ Teilschritte
	mittels Satz~\ref{satz:lokale-vollstaendigkeit} \emph{vorab und lokal} prÃ¼fen, ob
	$c'$ konform ist -- und kann zusÃ¤tzlich, sofern $c'$ als Beobachtungsfolge im Sinne
	von Definition~\ref{def:signaturfolge-sammel} interpretiert wird, den Subgraph
	Algorithmus aus~\cite{epp2026subgraph} \emph{client-seitig} auf den in $G_{s_c}^h$
	enthaltenen Signaturen ausfÃ¼hren (Satz~\ref{satz:permutierbar-subgraph}(b),
	$\Theta(r^3)$). Die KonformitÃ¤tsprÃ¼fung fÃ¼r SammelzustÃ¤nde wird damit
	\emph{vollstÃ¤ndig dezentralisiert}: Der Server muss sie nur noch im Fehlerfall (echte
	Sequenzverletzung, etwa durch einen vom Plan abweichenden Client) erneut
	durchfÃ¼hren, vgl.\ Definition~\ref{def:hst-server}(2).
\end{bemerkung}
\subsection{Kostenoptimaler Horizont}
\label{sec:hst-kosten}

\begin{definition}[Kostenmodell fÃ¼r den Horizont]
	\label{def:kostenmodell-horizont}
	Sei $\alpha>0$ die marginale Ãœbertragungskosten pro Knoten/Kante des
	Horizont-Subgraphen (z.\,B.\ in normierten Bandbreiten- oder Byte-Einheiten) und
	$\beta>0$ die Kosten eines zusÃ¤tzlichen Anfrage-Antwort-Zyklus
	(\emph{Round-Trip}), der anfÃ¤llt, wenn der Client eine Operation ausfÃ¼hren mÃ¶chte,
	deren Zielzustand auÃŸerhalb von $G_s^h$ liegt (\emph{HorizontÃ¼berschreitung}). Wir
	modellieren die Anzahl der vom Client Ã¼ber den Horizont $h$ hinaus benÃ¶tigten
	Schritte als eine Zufallsvariable mit geometrisch fallender Ãœberschreitungswahrscheinlichkeit:
	Mit \emph{Fortsetzungswahrscheinlichkeit} $p\in(0,1)$ sei
	$\Pr[\text{Client Ã¼berschreitet Horizont }h] = p^{h+1}$, und im Ãœberschreitungsfall
	seien im Erwartungswert $\frac{1}{1-p}$ zusÃ¤tzliche Round-Trips erforderlich
	(geometrische Reihe Ã¼ber weitere mÃ¶gliche Ãœberschreitungen). Die \emph{erwarteten
	Gesamtkosten} fÃ¼r Horizont $h\in\mathbb{N}_0$ sind dann
	\[
		C(h) := \alpha\cdot|V_s^h| + \beta\cdot\frac{p^{h+1}}{1-p}
		     \;=\; \alpha\sum_{i=0}^{h} d^i \;+\; \beta\cdot\frac{p^{h+1}}{1-p},
	\]
	wobei $|V_s^h|=\sum_{i=0}^h d^i$ gemÃ¤ÃŸ Lemma~\ref{lem:horizont-groesse}
	(Gleichheitsfall, als Worst-Case-SchÃ¤tzung des Verzweigungsgrads $d\ge1$) verwendet
	wird.
\end{definition}

\begin{satz}[Existenz und Charakterisierung des optimalen Horizonts]
	\label{satz:optimaler-horizont}
	Sei $C(h)$ wie in Definition~\ref{def:kostenmodell-horizont}, mit $d\ge1$,
	$p\in(0,1)$, $\alpha,\beta>0$. Dann gilt:
	\begin{enumerate}
		\item[(a)] Die Differenzfunktion $\Delta C(h):=C(h+1)-C(h)=\alpha\,d^{h+1} -
		\beta\,p^{h+1}$ ist streng monoton wachsend in $h\in\mathbb{N}_0$.
		\item[(b)] $C$ ist \emph{unimodal}: Es existiert ein eindeutiges
		$h^\ast\in\mathbb{N}_0$, sodass $C(0)>C(1)>\cdots>C(h^\ast)\le
		C(h^\ast+1)<C(h^\ast+2)<\cdots$ (mit der Konvention, dass die linke Kette leer
		ist, falls bereits $\Delta C(0)\ge0$). Insbesondere gilt
		$C(h^\ast)=\min_{h\in\mathbb{N}_0}C(h)$.
		\item[(c)] Es gilt die geschlossene Form
		\[
			h^\ast = \max\!\left(0,\ \left\lceil \log_{d/p}\!\left(\frac{\beta}{\alpha}\right)
			\right\rceil - 1\right).
		\]
	\end{enumerate}
\end{satz}

\begin{proof}
	\textbf{(a)} Da $d\ge1$ und $0<p<1$, gilt $d/p>1$ (im Fall $d=1$ ist $d/p=1/p>1$
	wegen $p<1$; im Fall $d>1$ erst recht). FÃ¼r die zweite Differenz gilt
	\[
		\Delta C(h+1)-\Delta C(h)
		= \bigl(\alpha d^{h+2}-\beta p^{h+2}\bigr) - \bigl(\alpha d^{h+1}-\beta p^{h+1}\bigr)
		= \alpha d^{h+1}(d-1) + \beta p^{h+1}(1-p).
	\]
	Da $d\ge1$ (also $d-1\ge0$), $0<p<1$ (also $1-p>0$) und $\alpha,\beta>0$, ist der
	zweite Summand $\beta p^{h+1}(1-p)$ stets strikt positiv und der erste Summand
	nicht-negativ. Also $\Delta C(h+1)-\Delta C(h)>0$ fÃ¼r alle $h\ge0$, d.\,h.\ $\Delta C$
	ist streng monoton wachsend.

	\textbf{(b)} FÃ¼r $h\to\infty$ gilt $\Delta C(h)=\alpha d^{h+1}-\beta p^{h+1}$. Da
	$p^{h+1}\to0$ und $d^{h+1}\ge1$ fÃ¼r alle $h\ge0$ (mit $d^{h+1}\to\infty$ falls $d>1$
	bzw.\ $d^{h+1}=1$ konstant falls $d=1$), folgt $\Delta C(h)\to\alpha\cdot
	\lim_{h\to\infty}d^{h+1}\ge\alpha>0$ fÃ¼r $h\to\infty$ -- insbesondere wird
	$\Delta C(h)$ fÃ¼r hinreichend groÃŸes $h$ positiv. Da $\Delta C$ nach (a) streng
	monoton wachsend ist und fÃ¼r groÃŸe $h$ positiv wird, gibt es hÃ¶chstens einen
	Vorzeichenwechsel von â€ž$<0$â€œ zu â€ž$\ge0$â€œ. Sei
	\[
		h^\ast := \min\{\, h\in\mathbb{N}_0 \mid \Delta C(h)\ge0 \,\}
	\]
	(wohldefiniert, da die Menge nichtleer ist nach obigem Grenzwertargument). FÃ¼r
	$h<h^\ast$ gilt $\Delta C(h)<0$ (MinimalitÃ¤t von $h^\ast$ und Monotonie von
	$\Delta C$ schlieÃŸen $\Delta C(h)\ge0$ fÃ¼r $h<h^\ast$ aus), also
	$C(h+1)<C(h)$: $C$ ist auf $\{0,\ldots,h^\ast\}$ streng fallend. FÃ¼r $h\ge h^\ast$
	gilt $\Delta C(h)\ge0$ wegen Monotonie von $\Delta C$ und $\Delta C(h^\ast)\ge0$,
	also $C(h+1)\ge C(h)$, mit strikter Ungleichung fÃ¼r $h>h^\ast$ (da $\Delta C(h)>
	\Delta C(h^\ast)\ge0$ fÃ¼r $h>h^\ast$ nach strikter Monotonie). Also ist $C$ auf
	$\{h^\ast,h^\ast+1,\ldots\}$ streng wachsend. Damit ist $C$ unimodal mit eindeutigem
	Minimum bei $h^\ast$.

	\textbf{(c)} Die definierende Bedingung $\Delta C(h)\ge0$ ist Ã¤quivalent zu
	\[
		\alpha\,d^{h+1}\ge\beta\,p^{h+1}
		\iff \left(\frac{d}{p}\right)^{h+1}\ge\frac{\beta}{\alpha}
		\iff (h+1)\log\frac{d}{p}\ge\log\frac{\beta}{\alpha}
		\iff h+1\ge\log_{d/p}\frac{\beta}{\alpha},
	\]
	wobei die letzte Ã„quivalenz gilt, da $\log(d/p)>0$ nach (a) und das Logarithmieren
	einer Ungleichung mit positiver Basis $>1$ die Richtung erhÃ¤lt. Der kleinste
	ganzzahlige Wert $h\ge0$ mit $h+1\ge\log_{d/p}(\beta/\alpha)$ ist
	\[
		h = \max\!\left(0,\ \left\lceil\log_{d/p}\!\left(\frac{\beta}{\alpha}\right)\right\rceil-1\right)
	\]
	(die Maximumsbildung mit $0$ deckt den Fall ab, dass bereits $\Delta C(0)\ge0$ gilt,
	also $\log_{d/p}(\beta/\alpha)\le1$). Nach Definition von $h^\ast$ in (b) ist dies
	genau $h^\ast$. \qedhere
\end{proof}

\begin{bemerkung}
	\label{bem:optimaler-horizont-interpretation}
	Satz~\ref{satz:optimaler-horizont} bestÃ¤tigt formal die in
	Bemerkung~\ref{bem:zustandslosigkeit-frei} aufgeworfene Beobachtung: Der optimale
	Horizont $h^\ast$ hÃ¤ngt \emph{ausschlieÃŸlich} vom KostenverhÃ¤ltnis $\beta/\alpha$
	(Round-Trip- vs.\ Knotenkosten) und vom VerhÃ¤ltnis $d/p$ (Verzweigungsgrad vs.\
	Fortsetzungswahrscheinlichkeit) ab, \emph{nicht} jedoch von einer prinzipiellen
	REST-InkompatibilitÃ¤t (Satz~\ref{satz:hst-zustandslosigkeit}). FÃ¼r hochlatente
	Verbindungen (groÃŸes $\beta$, z.\,B.\ mobile Clients) oder Prozesse mit hoher
	Fortsetzungswahrscheinlichkeit $p$ (lange, vorhersehbare Sequenzen, etwa
	SammelzustÃ¤nde gemÃ¤ÃŸ Bemerkung~\ref{bem:hst-permutierbar}) wÃ¤chst $h^\ast$.
	Abbildung~\ref{fig:optimaler-horizont} (rechts) zeigt diese AbhÃ¤ngigkeit als
	Stufenfunktion, da $h^\ast$ ganzzahlig ist. Insbesondere ist fÃ¼r SammelzustÃ¤nde mit
	$r$ permutierbaren Teilschritten der Horizont $h^\ast\ge r$ (vollstÃ¤ndige
	Dezentralisierung gemÃ¤ÃŸ Bemerkung~\ref{bem:hst-permutierbar}) gemÃ¤ÃŸ (c) genau dann
	optimal, wenn $\beta/\alpha\ge(d/p)^{r}$ -- fÃ¼r latenzkritische Anwendungen (groÃŸes
	$\beta/\alpha$) also bereits fÃ¼r moderate $r$ erfÃ¼llt, wÃ¤hrend fÃ¼r
	latenzunkritische Anwendungen (kleines $\beta/\alpha$) ein kleinerer Horizont
	$h^\ast<r$ kosteneffizienter ist und die Sammelzustands-KonformitÃ¤t partiell
	serverseitig verbleibt.
\end{bemerkung}

\begin{figure}[ht]
	\centering
	\includegraphics[width=\textwidth]{plots/plot6_optimaler_horizont.pdf}
	\caption{Links: Gesamtkosten $C(h)$ (logarithmische Skala) gemÃ¤ÃŸ
	Definition~\ref{def:kostenmodell-horizont} fÃ¼r $\alpha=1$, $\beta=50$, $d=2$ und
	Fortsetzungswahrscheinlichkeiten $p\in\{0.3,0.5,0.7\}$. Die Sterne markieren das nach
	Satz~\ref{satz:optimaler-horizont} eindeutige Minimum $h^\ast$: FÃ¼r $p=0.3$ und
	$p=0.5$ liegt es bei $h^\ast=2$, fÃ¼r $p=0.7$ bei $h^\ast=3$ -- in Ãœbereinstimmung
	mit der geschlossenen Form aus (c), denn fÃ¼r $p=0.7$ gilt
	$\log_{2/0.7}(50)\approx3.83$, also $h^\ast=\lceil3.83\rceil-1=3$. Rechts: optimaler
	Horizont $h^\ast$ als Funktion des KostenverhÃ¤ltnisses $\beta/\alpha$
	(logarithmische Abszisse) fÃ¼r $d=2$ und dieselben $p$-Werte, als Stufenfunktion
	gemÃ¤ÃŸ (c). GrÃ¶ÃŸere $\beta/\alpha$ (teurere Round-Trips relativ zur Payload-GrÃ¶ÃŸe,
	z.\,B.\ bei hoher Netzwerklatenz) und grÃ¶ÃŸere $p$ (Prozesse mit tendenziell langen
	Aufrufsequenzen) fÃ¼hren zu grÃ¶ÃŸerem $h^\ast$; die Stufen markieren die in (c)
	bewiesenen Schwellenwerte $\beta/\alpha=(d/p)^{h+1}$.}
	\label{fig:optimaler-horizont}
\end{figure}

\subsection{Versionierung und Cache-KohÃ¤renz via Signaturen}
\label{sec:hst-cache}

\begin{definition}[Signatur des Horizont-Subgraphen]
	\label{def:signatur-horizont}
	Sei $G_s^h=(V_s^h,E_s^h)$ ein Horizont-Subgraph mit einer fest gewÃ¤hlten,
	kanonischen AufzÃ¤hlung $V_s^h=\{v_0,\ldots,v_{m-1}\}$ (z.\,B.\ in BFS-Reihenfolge
	gemÃ¤ÃŸ Definition~\ref{def:horizont-subgraph}, mit $v_0=s$) und sei
	$\mathrm{idx}:\Sigma\to\{0,\ldots,|\Sigma|-1\}$ eine feste Indizierung des
	Operationsalphabets. Die \emph{Signatur} von $G_s^h$ ist
	\[
		\mathrm{Sig}(G_s^h) := \sum_{(v_i,\sigma,v_j)\in E_s^h}
		\Bigl(\, i \;+\; j\cdot m \;+\; \mathrm{idx}(\sigma)\cdot m^2 \,\Bigr)\cdot
		\bigl(2|\Sigma|\,m^2\bigr)^{\,\mathrm{rang}(v_i,\sigma,v_j)},
	\]
	wobei $\mathrm{rang}(\cdot)$ eine feste AufzÃ¤hlung der Kanten $0,1,2,\ldots$
	bezeichnet. Diese Konstruktion Ã¼bertragt das Prinzip der Spalten-Signaturen
	$\sigma_j=\sum_iA_{ij}2^i+j\cdot2^n$ aus~\cite{epp2026subgraph} -- dort fÃ¼r
	Adjazenzmatrizen von Aufrufsequenzen definiert -- auf die (Knoten-, Kanten-,
	Beschriftungs-)Tripel von $G_s^h$: Jede Kante wird auf einen Wert im Bereich
	$[0,2|\Sigma|m^2)$ abgebildet und die Kanten werden, analog zur stellenwertigen
	Kodierung in~\cite{epp2026subgraph}, mit unterschiedlichen â€žStellenâ€œ (Potenzen von
	$2|\Sigma|m^2$) gewichtet, sodass $\mathrm{Sig}$ auf der Menge aller Graphen mit
	hÃ¶chstens $m$ Knoten Ã¼ber dem Operationsalphabet $\Sigma$ injektiv ist (Ãœbertragung
	des Eindeutigkeitslemmas aus~\cite{epp2026subgraph} auf die hier verwendete
	stellenwertige Kodierung).
\end{definition}

\begin{bemerkung}[Cache-KohÃ¤renz fÃ¼r Navigationsgraphen]
	\label{bem:cache-kohaerenz}
	Ein Client speichert $(G_s^h,\mathrm{Sig}(G_s^h))$ lokal. Wird der Prozessautomat
	$P$ -- etwa im Rahmen einer fachlichen ProzessÃ¤nderung -- zu $P'$ aktualisiert,
	sodass sich $\delta$ in einem fÃ¼r $s$ relevanten Bereich Ã¤ndert, so unterscheidet
	sich der neue Horizont-Subgraph $(G')_s^h$ von $G_s^h$ in mindestens einer Kante.
	Da $\mathrm{Sig}$ nach Definition~\ref{def:signatur-horizont} injektiv ist, folgt
	$\mathrm{Sig}((G')_s^h)\ne\mathrm{Sig}(G_s^h)$. Ein HST-Server kann die aktuelle
	Signatur in einem \texttt{ETag}-Header der HST-ReprÃ¤sentation Ã¼bermitteln; ein
	Client kann damit in $O(1)$ (Vergleich zweier Zahlen) erkennen, ob sein lokal
	zwischengespeicherter Horizont-Subgraph noch gÃ¼ltig ist, und nur im Falle einer
	Ã„nderung eine vollstÃ¤ndige NeuÃ¼bertragung von $G_s^h$ anfordern
	(\texttt{If-None-Match}-Mechanismus gemÃ¤ÃŸ~\cite{rfc7231}). Dies Ã¼bertrÃ¤gt das in
	Abschnitt~\ref{sec:pattern} (Unterabschnitt â€žZustandslosigkeit und SC-RESTâ€œ)
	beschriebene Prinzip der \texttt{ETag}-basierten VersionsprÃ¼fung von einzelnen
	RessourcenzustÃ¤nden auf den \emph{gesamten, im Horizont enthaltenen
	Navigationsgraphen} -- ein Client kann damit nicht nur erkennen, ob sich
	\emph{seine} Ressource geÃ¤ndert hat, sondern auch, ob sich die fÃ¼r ihn relevanten
	\emph{Spielregeln} ($\delta$ eingeschrÃ¤nkt auf $G_s^h$) geÃ¤ndert haben.
\end{bemerkung}

\subsection{HST als Architekturstil: Generalisierung von REST}
\label{sec:hst-architekturstil}

Fielding definiert Architekturstile in~\cite{fielding2000} durch sukzessives HinzufÃ¼gen
von BeschrÃ¤nkungen (\emph{constraints}) zu einem â€žNullstilâ€œ ohne jede BeschrÃ¤nkung; REST
entsteht durch HinzufÃ¼gen von Client-Server, Zustandslosigkeit, Cache, Uniform
Interface, Layered System und (optional) Code-on-Demand. Die in
Abschnitt~\ref{sec:hst-vollstaendigkeit} bewiesene lokale VollstÃ¤ndigkeit motiviert eine
\emph{zusÃ¤tzliche} BeschrÃ¤nkung, mit der sich HST formal als echte Verfeinerung von REST
einordnen lÃ¤sst.

\begin{definition}[HST-Architekturstil]
	\label{def:hst-architekturstil}
	Der \emph{HST-Architekturstil} ist der REST-Architekturstil~\cite{fielding2000},
	erweitert um die folgende zusÃ¤tzliche BeschrÃ¤nkung:
	\begin{quote}
		\textbf{Horizont-LokalitÃ¤t.} Es existiert ein anwendungsweit oder
		ressourcenspezifisch konfigurierter Horizont $h\ge1$, sodass jede
		ReprÃ¤sentation einer zustandsbehafteten Ressource zusÃ¤tzlich zu den unmittelbar
		gÃ¼ltigen ÃœbergÃ¤ngen (Uniform Interface / HATEOAS) den vollstÃ¤ndigen
		$h$-Schritt-Horizont-Subgraphen $G_s^h$ gemÃ¤ÃŸ
		Definition~\ref{def:horizont-subgraph} enthÃ¤lt, sodass jeder Client fÃ¼r
		Aufrufsequenzen der LÃ¤nge $\le h$ lokale VollstÃ¤ndigkeit im Sinne von
		Satz~\ref{satz:lokale-vollstaendigkeit} besitzt.
	\end{quote}
\end{definition}

\begin{table}[ht]
	\centering
	\renewcommand{\arraystretch}{1.35}
	\caption{Die REST-BeschrÃ¤nkungen nach~\cite{fielding2000} und ihre Realisierung in
	SC-REST ($h=1$) bzw.\ HST ($h\ge1$ allgemein).}
	\label{tab:rest-vergleich}
	\begin{tabular}{@{}p{3.3cm}p{5.55cm}p{5.55cm}@{}}
		\toprule
		REST-BeschrÃ¤nkung & SC-REST ($h=1$) & HST ($h\ge1$, allgemein) \\
		\midrule
		Client-Server & unverÃ¤ndert & unverÃ¤ndert \\
		Zustands\-losigkeit &
		$\pi(id)=s$ als Ressourcenzustand (Abschn.~\ref{sec:pattern}) &
		unverÃ¤ndert gÃ¼ltig fÃ¼r beliebiges $h$ (Satz~\ref{satz:hst-zustandslosigkeit}) \\
		Cache &
		Standard-HTTP-Caching pro Ressourcenzustand &
		zusÃ¤tzlich signaturbasierte KohÃ¤renz fÃ¼r $G_s^h$
		(Abschn.~\ref{sec:hst-cache}) \\
		Uniform Interface (\mbox{HATEOAS}) &
		Links auf $\Sigma(\delta(s,\sigma))$, Horizont $1$ &
		vollstÃ¤ndiger Horizont-Subgraph $G_s^h$, Horizont $h\ge1$ konfigurierbar \\
		Layered System & unverÃ¤ndert & unverÃ¤ndert \\
		Code-on-Demand & nicht betrachtet & nicht betrachtet \\
		\emph{(neu)}\newline Horizont-LokalitÃ¤t &
		trivial erfÃ¼llt fÃ¼r SequenzlÃ¤nge $\le1$ &
		erfÃ¼llt fÃ¼r SequenzlÃ¤nge $\le h$ (Satz~\ref{satz:lokale-vollstaendigkeit}) \\
		\bottomrule
	\end{tabular}
\end{table}

\begin{bemerkung}
	Die Einordnung von HST als \emph{Erweiterung}, nicht als Ersetzung, von REST
	entspricht Fieldings Methodik~\cite{fielding2000}, Architekturstile inkrementell
	durch HinzufÃ¼gen von BeschrÃ¤nkungen zu definieren. Jede HST-konforme Architektur ist
	insbesondere REST-konform -- Satz~\ref{satz:hst-zustandslosigkeit} zeigt dies
	explizit fÃ¼r die Zustandslosigkeits-BeschrÃ¤nkung, die unter allen Ã¼brigen
	BeschrÃ¤nkungen invariant bleibt. HST ist somit, ebenso wie SC-REST, kein
	alternativer Stil zu REST, sondern eine \emph{Verfeinerung}: SC-REST verfeinert REST
	um SequenzkonformitÃ¤t (Abschnitt~\ref{sec:pattern}), und HST verfeinert SC-REST
	zusÃ¤tzlich um Horizont-LokalitÃ¤t. Die Beziehung
	\[
		\text{REST} \;\supseteq\; \text{SC-REST} \;\supseteq\; \text{HST}
	\]
	(als Mengen von Architekturen, die die jeweiligen BeschrÃ¤nkungen erfÃ¼llen) ist
	gemÃ¤ÃŸ Bemerkung~\ref{bem:sc-rest-spezialfall} sogar eine Gleichheit zwischen
	SC-REST und HST fÃ¼r $h=1$: SC-REST ist also nicht nur \emph{enthalten} in HST,
	sondern der Grenzfall $h=1$ \emph{ist} HST.
\end{bemerkung}

\subsection{Referenzimplementierung: Berechnung des Horizont-Subgraphen}
\label{sec:hst-implementierung}

Listing~\ref{lst:hst-bfs} erweitert die Middleware aus
Listing~\ref{lst:transition}/\ref{lst:flask} um eine Breitensuche, die den
Horizont-Subgraphen $G_s^h$ gemÃ¤ÃŸ Definition~\ref{def:horizont-subgraph} berechnet, und
um eine Funktion, die diesen Subgraphen gemÃ¤ÃŸ Definition~\ref{def:hst-repraesentation} in
die HST-ReprÃ¤sentation einbettet.

\begin{lstlisting}[language=Python, label=lst:hst-bfs, caption=Breitensuchbasierte Berechnung des Horizont-Subgraphen $G_s^h$ und Einbettung in die HST-Repraesentation]
def horizon_subgraph(process: ProcessAutomaton,
                      s: str, h: int):
    """
    Berechnet G_s^h = (V, E) gemaess Definition
    "h-Schritt-Horizont-Subgraph" per Breitensuche.

    V: Menge erreichbarer Zustaende (Knoten)
    E: Liste von (s1, operation, s2)-Tripeln (Kanten)
    """
    V = {s}
    E = []
    frontier = {s}

    for _ in range(h):
        next_frontier = set()
        for s1 in frontier:
            for op in process.allowed_operations(s1):
                s2 = process.delta[(s1, op)]
                E.append((s1, op, s2))
                if s2 not in V:
                    V.add(s2)
                    next_frontier.add(s2)
        frontier = next_frontier
        if not frontier:
            break  # Horizont reicht bis F oder Sackgasse

    return V, E


def sst_representation(process: ProcessAutomaton,
                        instance_id: str, h: int = 2):
    """
    Erstellt die HST-Repraesentation R(id, s, h) gemaess
    Definition "HST-Repraesentation".
    """
    s = process.pi[instance_id]
    V, E = horizon_subgraph(process, s, h)

    return {
        "orderId": instance_id,
        "state": s,
        "_horizon": h,
        "_graph": {
            "nodes": sorted(V),
            "edges": [
                {"from": s1, "op": op, "to": s2}
                for (s1, op, s2) in E
            ],
        },
        # h=1-Teilmenge entspricht den bisherigen
        # HATEOAS-Links (Listing 4, Bemerkung "SC-REST
        # ist Spezialfall h=1 von HST")
        "_links": hateoas_links(process, s, instance_id),
    }
\end{lstlisting}

\begin{bemerkung}
	Die Laufzeit von \texttt{horizon\_subgraph} ist $O(|V_s^h|\cdot d)= O(d^{h+1})$
	(Lemma~\ref{lem:horizont-groesse}), da jede der $h$ BFS-Schichten hÃ¶chstens
	$|V_s^h|$ Knoten mit jeweils hÃ¶chstens $d$ ausgehenden Operationen betrachtet; bei
	Hashtabellen-basierter Implementierung von $\delta$ (Satz~\ref{satz:laufzeit-pruefung})
	ist jeder Zugriff $\delta[(s_1,\sigma)]$ in erwarteter konstanter Zeit mÃ¶glich. FÃ¼r
	die in Abbildung~\ref{fig:horizont-groesse} betrachteten praxisrelevanten Horizonte
	$h\le4$ und $d\le3$ ist dieser Aufwand vernachlÃ¤ssigbar gegenÃ¼ber der ohnehin
	anfallenden Fachlogik-AusfÃ¼hrung in Definition~\ref{def:hst-server}.
\end{bemerkung}

\section{Zusammenfassung}

Diese Arbeit hat mit SC-REST ein formales Pattern fÃ¼r sequenzgebundene REST-Schnittstellen
entwickelt (Abschnitte~\ref{sec:pattern}--\ref{sec:implementierung}) und dieses Pattern in
Abschnitt~\ref{sec:sst} zu \emph{HST (Horizon State Transfer)} weiterentwickelt: SC-REST
Ã¼bermittelt pro Antwort die im aktuellen Zustand zulÃ¤ssigen Folgeoperationen ($1$-Schritt-
Horizont), wÃ¤hrend HST den vollstÃ¤ndigen $h$-Schritt-Horizont-Subgraphen $G_s^h$
Ã¼bertrÃ¤gt und damit lokale VollstÃ¤ndigkeit fÃ¼r Aufrufsequenzen der LÃ¤nge $\le h$
herstellt (Satz~\ref{satz:lokale-vollstaendigkeit}), ohne die Zustandslosigkeit nach
Fielding~\cite{fielding2000} zu beeintrÃ¤chtigen (Satz~\ref{satz:hst-zustandslosigkeit}).
SC-REST ist dabei formal der Spezialfall $h=1$ von HST
(Bemerkung~\ref{bem:sc-rest-spezialfall}); beide Pattern teilen daher dieselbe
Anwendungsgrundlage, die im Folgenden zusammengefasst wird, bevor in
Abschnitt~\ref{sec:ausblick} -- nun unter besonderer BerÃ¼cksichtigung von HST --
Erweiterungen und Forschungsrichtungen diskutiert werden.

\subsection{Anwendungen}

Das SC-REST-Pattern (und, mit grÃ¶ÃŸerem Horizont, HST) eignet sich insbesondere fÃ¼r:
\begin{itemize}
	\item E-Commerce-Checkout-Prozesse (Warenkorb, Versand, Zahlung, BestÃ¤tigung) wie in
	      Beispiel~\ref{bsp:bestellprozess},
	\item mehrstufige Antrags- und Genehmigungsprozesse in Verwaltung und Finanzwesen
	      (Einreichung, PrÃ¼fung, Freigabe, Auszahlung),
	\item Onboarding-Flows mit zwingender Reihenfolge (IdentitÃ¤tsprÃ¼fung vor
	      Vertragsabschluss vor Zahlungseinrichtung),
	\item zustandsbehaftete GerÃ¤teprotokolle, die Ã¼ber REST gekapselt werden
	      (Initialisierung, Kalibrierung, Messung, Abschluss),
	\item Multi-Step-Formulare und Wizards in Web-Anwendungen, bei denen
	      Schrittreihenfolgen serverseitig erzwungen werden mÃ¼ssen, um inkonsistente
	      ZwischenzustÃ¤nde zu verhindern.
\end{itemize}

FÃ¼r HST (Abschnitt~\ref{sec:sst}) treten insbesondere solche Anwendungen hinzu, bei denen
\emph{Planungsvorsprung} fachlich wertvoll ist: Mobile Clients mit instabiler Verbindung
(groÃŸes $\beta$ in Definition~\ref{def:kostenmodell-horizont}, daher grÃ¶ÃŸeres $h^\ast$
nach Satz~\ref{satz:optimaler-horizont}), grafische Prozess-Editoren und Wizard-UIs, die
dem Nutzer \emph{vorab} alle erreichbaren Folgeschritte mehrerer Ebenen anzeigen mÃ¶chten
(â€žFortschrittsanzeigeâ€œ als direkte UI-ReprÃ¤sentation von $G_s^h$), sowie -- gemÃ¤ÃŸ
Bemerkung~\ref{bem:hst-permutierbar} -- SammelzustÃ¤nde mit permutierbaren Teilschritten,
fÃ¼r die HST mit $h\ge r$ die in Abschnitt~\ref{sec:permutierbar} entwickelte
signaturbasierte KonformitÃ¤tsprÃ¼fung vollstÃ¤ndig auf den Client verlagert.

\subsection{Ausblick}
\label{sec:ausblick}

Das in dieser Arbeit vorgestellte SC-REST-Pattern und seine Verallgemeinerung zu HST
(Abschnitt~\ref{sec:sst}) bilden die formale Grundlage fÃ¼r zahlreiche Erweiterungen und
Forschungsrichtungen, die im Folgenden ausfÃ¼hrlich diskutiert werden. Die ersten zwÃ¶lf
Unterabschnitte betreffen primÃ¤r SC-REST und seine in Abschnitt~\ref{sec:analyse} bis
\ref{sec:implementierung} entwickelten Resultate; die abschlieÃŸenden Unterabschnitte
widmen sich speziell den durch HST erÃ¶ffneten Fragestellungen.

\subsubsection{Formale Spezifikationssprache fÃ¼r Prozessautomaten}

Die Definitionen~\ref{def:prozessautomat}--\ref{def:konformitaet} legen nahe, $\delta$
nicht imperativ (wie in Listing~\ref{lst:flask}) zu kodieren, sondern \emph{deklarativ} in
einer maschinenlesbaren Spezifikation. Ein naheliegender Ansatz ist eine Erweiterung von
OpenAPI um ein Feld \texttt{x-process-state}, das pro Operation den Vorbedingungszustand
(\texttt{requires-state}) und den Folgezustand (\texttt{transitions-to}) angibt. Aus einer
solchen Spezifikation lieÃŸen sich automatisch (1) die Hashtabelle $\delta$ fÃ¼r
Satz~\ref{satz:laufzeit-pruefung}, (2) Client-SDKs mit typsicheren ZustandsÃ¼bergÃ¤ngen, (3)
interaktive API-Dokumentation mit Zustandsdiagrammen analog zu
Abbildung~\ref{fig:automat}, und (4) Testsuiten generieren, die systematisch alle
$\delta(s,\sigma)$ sowie alle erwarteten \texttt{409}-FÃ¤lle (alle $(s,\sigma)\notin
\mathrm{dom}(\delta)$) abdecken. Eine solche DSL wÃ¼rde die in
Bemerkung~\ref{bem:prefetch-kosten} geforderte Begrenzung $d(s)\leq D_{\max}$ bereits zur
Designzeit statisch prÃ¼fbar machen.

\subsubsection{Verteilte Prozessinstanzen und das Saga-Pattern}

Reale GeschÃ¤ftsprozesse erstrecken sich hÃ¤ufig Ã¼ber mehrere Microservices, von denen jeder
nur einen Teil von $\Sigma$ implementiert. Eine natÃ¼rliche Erweiterung besteht darin, den
globalen Prozessautomaten $P$ als \emph{Choreographie} mehrerer lokaler Automaten
$P_1,\ldots,P_n$ zu modellieren, deren ZustÃ¤nde $S_i$ Ã¼ber Ereignisse synchronisiert
werden -- eine Verbindung zum klassischen Saga-Pattern fÃ¼r verteilte Transaktionen
\cite{garcia1987}. Im Unterschied zu klassischen Sagas, die primÃ¤r
\emph{Kompensationsaktionen} bei Fehlern definieren, wÃ¼rde eine SC-REST-Choreographie
zusÃ¤tzlich \emph{prÃ¤ventiv} verhindern, dass ein Client Ã¼berhaupt eine Operation auslÃ¶st,
deren Vorbedingungen in einem anderen Service noch nicht erfÃ¼llt sind. Hierzu wÃ¤re
Satz~\ref{satz:korrektheit} auf ein verteiltes Setting zu verallgemeinern, in dem $\pi(id)$
nicht von einem einzelnen Server, sondern von einer verteilten, eventuell replizierten
Datenstruktur verwaltet wird -- mit den entsprechenden Konsistenzfragen (siehe Punkt
â€žMulti-Tenantâ€œ weiter unten).

\subsubsection{Nichtdeterministische Automaten und parallele Teilprozesse}

Definition~\ref{def:prozessautomat} fordert, dass $\delta$ eine \emph{Funktion} ist
(deterministisch). Viele Prozesse enthalten jedoch \emph{parallele} Zweige, die
unabhÃ¤ngig voneinander und potenziell gleichzeitig fortschreiten (im Unterschied zu den in
Abschnitt~\ref{sec:permutierbar} behandelten \emph{sequentiell, aber in beliebiger
Reihenfolge} auszufÃ¼hrenden Teilschritten). Eine natÃ¼rliche Erweiterung ist der Ãœbergang
von Prozessautomaten zu \emph{Petri-Netzen}~\cite{murata1989}: Stellen (places)
modellieren Teilprozesse, Marken (tokens) den Fortschritt, und Transitionen werden erst
â€žfeuerbarâ€œ, wenn alle Vorbedingungs-Stellen markiert sind. Die KonformitÃ¤tsprÃ¼fung
(Satz~\ref{satz:pfadcharakterisierung}) wÃ¼rde dann zu einer ErreichbarkeitsprÃ¼fung im
Markierungsgraphen des Petri-Netzes, deren KomplexitÃ¤t im Allgemeinen deutlich hÃ¶her ist
als $\Theta(k)$ -- ein Forschungsfeld, in dem die Ãœbertragung der
Subgraph Algorithmus-Technik aus Satz~\ref{satz:permutierbar-subgraph} auf
Markierungsvektoren ein vielversprechender Ansatzpunkt fÃ¼r zukÃ¼nftige Arbeiten ist.

\subsubsection{Sicherheit: GeschÃ¼tzte Zustandstoken}

In der hier vorgestellten Form wird $\pi(id)$ serverseitig gespeichert. In hochskalierbaren
oder Edge-Architekturen kann es vorteilhaft sein, den Zustand $s$ direkt im Client zu
halten -- kodiert als signiertes JSON Web Token (JWT), das bei jeder Anfrage mitgesendet
wird. Dies wÃ¼rde die Statelessness im klassischen Sinn vollstÃ¤ndig wiederherstellen
(\emph{kein} serverseitiger Zustand pro Instanz), erfordert jedoch eine kryptografische
Signatur, die Manipulation des Zustands durch den Client ausschlieÃŸt (sonst kÃ¶nnte ein
Client durch direktes Setzen von $s:=s_4$ die PrÃ¼fung $\delta(s,\sigma)$ umgehen). Die
formale Korrektheitsaussage aus Satz~\ref{satz:korrektheit} bliebe in diesem Modell
gÃ¼ltig, sofern die SignaturprÃ¼fung als zusÃ¤tzliche, der eigentlichen
$\delta$-PrÃ¼fung vorgeschaltete Operation modelliert wird, deren Fehlschlagen ebenfalls zu
einer (jedoch von \texttt{sequence-violation} zu unterscheidenden,
\texttt{401}/\texttt{403}-artigen) Fehlerantwort fÃ¼hrt.

\subsubsection{Adaptive Vorhersage mittels maschinellem Lernen}

Das Latenzmodell aus Definition~\ref{def:latenzmodell} und Satz~\ref{satz:latenzgewinn}
geht von einem festen, durch $N(s)$ gegebenen Kandidatensatz fÃ¼r das Prefetching aus. Bei
hohem Verzweigungsgrad $d(s)$ (Bemerkung~\ref{bem:prefetch-kosten}) ist es jedoch
ineffizient, \emph{alle} $|N(s)|$ Kandidaten gleich zu priorisieren. Stattdessen kÃ¶nnte ein
Markov-Modell hÃ¶herer Ordnung -- basierend auf der Historie vorheriger Prozessinstanzen --
eine Wahrscheinlichkeitsverteilung $P(\sigma \mid s, \text{Kontext})$ Ã¼ber die nÃ¤chste
Operation schÃ¤tzen und das Prefetching nur fÃ¼r die $w$ wahrscheinlichsten Kandidaten
durchfÃ¼hren (\emph{Top-$w$-Prefetching}). Satz~\ref{satz:latenzgewinn} lieÃŸe sich dann zu
einem \emph{erwarteten} Latenzgewinn
$E[\Delta] = \sum_{\sigma\in\Sigma(s)} P(\sigma\mid s)\cdot
\min(T_{\mathrm{think}},T_{\mathrm{prep}}(\delta(s,\sigma)))$ verfeinern, und die Wahl von
$w$ wÃ¼rde einen expliziten Kompromiss zwischen erwartetem Latenzgewinn und
Ressourcenverbrauch (Bemerkung~\ref{bem:prefetch-kosten}) parametrisieren. Die in
Abbildung~\ref{fig:lernkurve} gezeigte Lernkurve liefert hierfÃ¼r bereits die empirische
Grundlage: Die mit zunehmender Prozessreife sinkende Fehlerrate $p(n)$ entspricht einer
zunehmend â€žspitzerenâ€œ Verteilung $P(\sigma\mid s)$.

\subsubsection{Verifikation und Model Checking}

Da $P=(S,\Sigma,\delta,s_0,F)$ ein endlicher Automat ist, lassen sich Eigenschaften wie
â€žjeder Zustand $s\in S\setminus F$ besitzt mindestens einen Pfad nach $F$â€œ
(\emph{Lebendigkeit}, keine â€žSackgassenâ€œ) oder â€ž$F$ ist von $s_0$ aus stets erreichbarâ€œ
mit Standardverfahren der Erreichbarkeitsanalyse in $O(|S|+|E|)$ prÃ¼fen (Tiefen- bzw.
Breitensuche auf $G_P$, Definition~\ref{def:prozessgraph}). FÃ¼r die in
Abschnitt~\ref{sec:permutierbar} diskutierten SammelzustÃ¤nde mit ihrer impliziten
$2^r$-Struktur (Lemma~\ref{lem:zustandsexplosion}) wÃ¤ren jedoch symbolische Verfahren
(BDD-basiertes Model Checking, LTL/CTL-Spezifikationen) notwendig, um Eigenschaften wie
â€žjede Reihenfolge der $r$ optionalen Teilschritte fÃ¼hrt zu einem konsistenten
Folgezustandâ€œ ohne explizite AufzÃ¤hlung aller $2^r$ ZwischenzustÃ¤nde zu verifizieren --
ein direkter AnknÃ¼pfungspunkt fÃ¼r die formale Methode dieser Arbeit.

\subsubsection{Integration mit GraphQL, gRPC und AsyncAPI}

Das SC-REST-Pattern wurde fÃ¼r REST/HTTP mit JSON-ReprÃ¤sentationen formuliert. Die
zugrundeliegenden Definitionen~\ref{def:prozessautomat}--\ref{def:sc-rest-server} sind
jedoch protokollunabhÃ¤ngig: FÃ¼r GraphQL lieÃŸe sich $\Sigma$ als Menge von Mutationen
interpretieren, wobei \texttt{409}-artige Fehler als typisierte GraphQL-Errors mit einem
analogen \texttt{sequence-violation}-Feld kodiert wÃ¼rden; fÃ¼r gRPC entsprÃ¤chen die
Operationen RPC-Methoden, und Sequenzverletzungen wÃ¼rden Ã¼ber den
\texttt{FAILED\_PRECONDITION}-Statuscode signalisiert, dessen Metadaten die Menge
$\Sigma(s)$ trÃ¼gen. FÃ¼r ereignisgetriebene Architekturen (AsyncAPI) wÃ¤re $\Sigma$ die
Menge der konsumierten Nachrichtentypen, und eine Sequenzverletzung wÃ¼rde als
\emph{Dead-Letter}-Ereignis mit Verweis auf $\Sigma(s)$ verÃ¶ffentlicht. In allen FÃ¤llen
blieben Satz~\ref{satz:korrektheit} (Korrektheit) und Satz~\ref{satz:laufzeit-pruefung}
(Laufzeit) unverÃ¤ndert gÃ¼ltig, da beide ausschlieÃŸlich auf der abstrakten Struktur von
$P$, nicht auf HTTP-spezifischen Details, beruhen.

\subsubsection{Hierarchische Automaten und Subprozesse}

Komplexe Prozesse lassen sich hÃ¤ufig in Subprozesse zerlegen, die selbst wieder
Prozessautomaten gemÃ¤ÃŸ Definition~\ref{def:prozessautomat} sind. Ein Zustand $s\in S$ des
Ã¼bergeordneten Automaten kÃ¶nnte dann â€žinternâ€œ einen vollstÃ¤ndigen Subprozessautomaten
$P_s=(S_s,\Sigma_s,\delta_s,s_0^{(s)},F_s)$ reprÃ¤sentieren, wobei der Ãœbergang aus $s$ im
Ã¼bergeordneten Automaten erst erfolgt, wenn $P_s$ einen Zustand in $F_s$ erreicht hat. Der
Prozessgraph $G_P$ (Definition~\ref{def:prozessgraph}) wÃ¼rde dadurch zu einem
\emph{hierarchischen Graphen}, in dem jeder â€žSuperknotenâ€œ selbst einen Graphen $G_{P_s}$
enthÃ¤lt. Die KonformitÃ¤tsprÃ¼fung einer Aufrufsequenz wÃ¼rde rekursiv: Auf jeder
Hierarchieebene wird Satz~\ref{satz:pfadcharakterisierung} angewendet, und fÃ¼r
SammelzustÃ¤nde mit permutierbaren Teilschritten (Abschnitt~\ref{sec:permutierbar}) auf
jeder Ebene unabhÃ¤ngig Satz~\ref{satz:permutierbar-subgraph}. Dies erÃ¶ffnet die
MÃ¶glichkeit, den Subgraph Algorithmus aus~\cite{epp2026subgraph} \emph{rekursiv} auf
verschachtelte Subgraphen anzuwenden -- ein direkter struktureller Bezug zur in
\cite{epp2026subgraph} diskutierten AST-Analyse, bei der TeilbÃ¤ume (Subgraphen)
ebenfalls rekursiv verglichen werden.

\subsubsection{Standardisierung: Vorschlag fÃ¼r eine OpenAPI-Erweiterung â€žx-sc-restâ€œ}

Die in dieser Arbeit entwickelten Definitionen kÃ¶nnten Grundlage eines formalen
Standardisierungsvorschlags sein, etwa als OpenAPI-Erweiterungsfeld
\texttt{x-sc-rest} oder als eigenstÃ¤ndiges Internet-Draft analog zu RFC~7807
\cite{rfc7807}. Ein solcher Standard mÃ¼sste mindestens spezifizieren: (1) das Format zur
Deklaration von $S,\Sigma,\delta,s_0,F$ (vgl. â€žFormale Spezifikationsspracheâ€œ oben), (2)
den Problem-Detail-Typ \texttt{sequence-violation} inklusive des Feldes
\texttt{allowedOperations}, (3) die HATEOAS-Link-Relationstypen fÃ¼r Folgeoperationen, und
(4) optionale Erweiterungsfelder fÃ¼r SammelzustÃ¤nde (Definition~\ref{def:sammelzustand}),
etwa \texttt{x-sc-rest-collection} mit den Feldern \texttt{operations} (entsprechend
$\Sigma_{\mathrm{opt}}$) und \texttt{completion} (entsprechend $\sigma_{\mathrm{done}}$).
Ein solcher Standard wÃ¼rde die in Pautasso et al.~\cite{pautasso2008} diskutierte Debatte
zwischen â€žRESTfulâ€œ und â€žBig Web Servicesâ€œ (WS-BPEL) um eine dritte Position ergÃ¤nzen:
prozessbewusste REST-APIs, die die Vorteile beider Welten -- die Einfachheit von REST und
die Prozessgarantien von WS-BPEL-Choreographien -- formal vereinen.

\subsubsection{Experimentelle Validierung in Produktionssystemen}

Die in Abschnitt~\ref{sec:empirisch} gezeigten Diagramme basieren auf simulierten Daten.
Eine naheliegende Folgearbeit ist die Instrumentierung einer SC-REST-Referenzimplementierung
(Abschnitt~\ref{sec:implementierung}) in einem produktiven Microservice-Umfeld
\cite{newman2021}, um (1) die in Satz~\ref{satz:latenzgewinn} postulierte
Latenzreduktion unter realer Lastverteilung von $T_{\mathrm{think}}$ und
$T_{\mathrm{prep}}$ zu vermessen, (2) die tatsÃ¤chliche Verteilung der
Verzweigungsgrade $d(s)$ in realen Prozessmodellen zu erheben (als Eingabe fÃ¼r
Bemerkung~\ref{bem:prefetch-kosten}), und (3) die in Lemma~\ref{lem:zustandsexplosion}
postulierte Zustandsraum-Explosion fÃ¼r reale SammelzustÃ¤nde (typische Werte fÃ¼r $r$) zu
quantifizieren. Eine offene Benchmark-Suite, die verschiedene Prozessautomaten
$P$ unterschiedlicher GrÃ¶ÃŸe $|S|$, $|\Sigma|$ und Verzweigungsgrade enthÃ¤lt, wÃ¼rde
zudem den systematischen Vergleich verschiedener Implementierungsstrategien fÃ¼r $\delta$
(Hashtabelle, Entscheidungsbaum, kompilierter endlicher Automat) ermÃ¶glichen.

\subsubsection{Energieeffizienz durch reduzierte Fehlversuche}

Jede \texttt{409}-Antwort gemÃ¤ÃŸ Satz~\ref{satz:korrektheit}(c) reprÃ¤sentiert eine
â€žvergeblicheâ€œ Anfrage: NetzwerkÃ¼bertragung, TLS-Handshake (falls keine Connection
wiederverwendet wird), Server-seitige Verarbeitung der Anfrage und der Fehlerantwort --
alles ohne fachlichen Nutzen. Die in Abbildung~\ref{fig:lernkurve} gezeigte sinkende
Fehlerrate $p(n)$ impliziert daher nicht nur einen Latenz-, sondern auch einen
Energieeffizienzgewinn: Pro vermiedener \texttt{409}-Antwort wird die gesamte
Verarbeitungskette einmal weniger durchlaufen. Eine quantitative AbschÃ¤tzung dieses
Effekts -- etwa in Joule pro vermiedener Anfrage, aggregiert Ã¼ber Rechenzentren mit
hohem API-Durchsatz -- wÃ¤re ein Beitrag zur Diskussion um â€žGreen Software Engineeringâ€œ und
ergÃ¤nzt die in Bemerkung~\ref{bem:prefetch-kosten} diskutierte Kostenseite des
Prefetchings: WÃ¤hrend Prefetching gezielt \emph{zusÃ¤tzliche} Berechnungen vorzieht, um
Latenz zu sparen, spart die Sequenzkontrolle selbst Berechnungen \emph{vollstÃ¤ndig} ein,
indem sie von vornherein unmÃ¶gliche Anfragen frÃ¼h und billig (Satz
\ref{satz:laufzeit-pruefung}, $O(1)$) abweist.

\subsubsection{Multi-Tenant-Architekturen und verteilte Zustandsspeicher}

Definition~\ref{def:prozessinstanz} setzt voraus, dass $\pi: \text{ID}\rightharpoonup S$
konsistent gelesen und geschrieben werden kann. In horizontal skalierten Systemen wird
$\pi$ typischerweise in einem verteilten SchlÃ¼ssel-Wert-Speicher (z.\,B. Redis-Cluster)
gehalten. Damit stellt sich die Frage, welche Konsistenzgarantien fÃ¼r $\pi$ erforderlich
sind, damit Satz~\ref{satz:korrektheit} weiterhin gilt: Wird $\pi(id)$ unter
Eventual Consistency repliziert, kÃ¶nnten zwei nahezu gleichzeitige Anfragen fÃ¼r dieselbe
Prozessinstanz $id$ auf unterschiedlichen Replikaten denselben Ausgangszustand $s$ sehen
und beide einen (jeweils fÃ¼r sich konformen) Ãœbergang $\delta(s,\sigma)$ und
$\delta(s,\sigma')$ ausfÃ¼hren -- ein klassisches \emph{Lost-Update}-Problem, das die
Eindeutigkeit des Laufs aus Definition~\ref{def:lauf} verletzt. Eine Erweiterung dieser
Arbeit mÃ¼sste daher entweder (a) starke Konsistenz fÃ¼r $\pi(id)$ fordern (z.\,B. mittels
eines verteilten Locks oder einer Single-Writer-Partitionierung nach $id$, was angesichts
der CAP-Theorem-Implikationen VerfÃ¼gbarkeitseinbuÃŸen bei Partitionierung bedeutet), oder
(b) Satz~\ref{satz:korrektheit} zu einer \emph{optimistischen} Variante verfeinern, bei
der konkurrierende ÃœbergÃ¤nge mittels eines VersionszÃ¤hlers (analog zu \texttt{ETag}/
\texttt{If-Match}) erkannt und einer der beiden ÃœbergÃ¤nge nachtrÃ¤glich mit
\texttt{409 Conflict} abgewiesen wird -- eine Idempotenz- und
Konfliktbehandlungsstrategie, die eng mit der in Abschnitt~\ref{sec:robustheit}
diskutierten Replay-Sicherheit (Satz~\ref{satz:replay}) zusammenhÃ¤ngt und in
\cite{helland2007} fÃ¼r verteilte Transaktionen allgemein diskutiert wird.

\subsubsection{Adaptiver Horizont: Lernen von $h^\ast$ pro Client und Prozesstyp}

Satz~\ref{satz:optimaler-horizont} liefert $h^\ast$ als Funktion der Parameter
$\alpha,\beta,d,p$ -- in der Praxis sind insbesondere $\beta$ (Round-Trip-Latenz) und $p$
(Fortsetzungswahrscheinlichkeit) jedoch nicht global konstant, sondern hÃ¤ngen vom
\emph{Client} (mobile Verbindung vs.\ Rechenzentrums-zu-Rechenzentrums-Aufruf, also
unterschiedliches $\beta$) und vom \emph{Prozesstyp} bzw.\ sogar vom \emph{individuellen
Nutzer} (manche Nutzer brechen BestellvorgÃ¤nge hÃ¤ufig nach dem ersten Schritt ab, andere
durchlaufen sie zuverlÃ¤ssig bis zum Ende, also unterschiedliches $p$) ab. Dies setzt die
in Abschnitt~\ref{sec:ausblick} (Unterabschnitt â€žAdaptive Vorhersage mittels
maschinellem Lernenâ€œ) fÃ¼r $T_{\mathrm{think}}$ und $T_{\mathrm{prep}}$ skizzierte Idee
fort: Ein lernendes System kÃ¶nnte $\hat\beta$ aus den \texttt{RTT}-Werten der
TCP-Verbindung des jeweiligen Clients und $\hat p$ aus der historischen
SequenzlÃ¤ngenverteilung des jeweiligen Nutzers (oder, bei neuen Nutzern, aus einem
Prozesstyp-Prior) schÃ¤tzen und daraus gemÃ¤ÃŸ der geschlossenen Form in
Satz~\ref{satz:optimaler-horizont}(c) einen \emph{personalisierten} Horizont
$\hat h^\ast(\text{client},\text{user})$ berechnen, der pro Anfrage als
\texttt{?horizon=}-Parameter (Definition~\ref{def:hst-server}) gesetzt wird. Im
Gegensatz zu den meisten ML-Einsatzfeldern in zustandsbehafteten APIs ist diese Anwendung
\emph{folgenlos im Fehlerfall}: Eine falsche SchÃ¤tzung von $\hat h^\ast$ fÃ¼hrt nach
Satz~\ref{satz:hst-zustandslosigkeit} hÃ¶chstens zu suboptimalen Kosten $C(\hat
h^\ast)>C(h^\ast)$, niemals zu einer inkorrekten oder inkonsistenten Antwort -- das
Lernproblem ist also ein reines Optimierungs-, kein Korrektheitsproblem, was den Einsatz
von ML hier mit deutlich geringerem Risiko behaftet als etwa bei der in
\cite{murata1989}-artigen Systemen Ã¼blichen Zustandsvorhersage.

\subsubsection{Informationsfreigabe und Zugriffskontrolle im Horizont-Subgraphen}

Definition~\ref{def:hst-repraesentation} setzt implizit voraus, dass \emph{der gesamte}
Horizont-Subgraph $G_s^h$ fÃ¼r den anfragenden Client offenlegbar ist. In Prozessen mit
rollenbasierter Autorisierung -- etwa Genehmigungs-Workflows, in denen ein
Sachbearbeiter nur die fÃ¼r seine Rolle bestimmten ÃœbergÃ¤nge sehen darf, wÃ¤hrend
Eskalationspfade (z.\,B.\ \texttt{POST /reject-and-escalate}) nur fÃ¼r Vorgesetzte
sichtbar sein sollen -- wÃ¼rde eine ungefilterte Ãœbertragung von $G_s^h$ dem Client
\emph{Wissen Ã¼ber die Struktur des Prozessautomaten} offenlegen, das Ã¼ber seine eigenen
HandlungsmÃ¶glichkeiten hinausgeht: Allein die \emph{Existenz} eines
Eskalationszustands im Horizont kÃ¶nnte ein Informationsleck darstellen (etwa wenn die
bloÃŸe Existenz eines â€žSonderkonditionenâ€œ-Zustands fÃ¼r regulÃ¤re Kunden vertraulich sein
soll). Eine konsequente Erweiterung von Definition~\ref{def:hst-server} wÃ¤re daher eine
\emph{rollenfilterte} Variante
\[
	G_s^h\!\restriction_\rho := \bigl(V_s^h\cap\mathrm{sichtbar}(\rho),\;
	E_s^h\cap(\mathrm{sichtbar}(\rho)\times\Sigma\times\mathrm{sichtbar}(\rho))\bigr),
\]
wobei $\rho$ die Rolle des Clients und $\mathrm{sichtbar}(\rho)\subseteq S$ die fÃ¼r $\rho$
sichtbaren ZustÃ¤nde bezeichnet. Eine formale Folgearbeit mÃ¼sste klÃ¤ren, ob
$G_s^h\!\restriction_\rho$ weiterhin die in Satz~\ref{satz:lokale-vollstaendigkeit}
gezeigte lokale VollstÃ¤ndigkeit \emph{fÃ¼r die dem Client tatsÃ¤chlich erlaubten
Operationen} besitzt -- vermutlich ja, sofern $\mathrm{sichtbar}(\rho)$ unter $\delta$
fÃ¼r die Operationen aus $\Sigma_\rho\subseteq\Sigma$ abgeschlossen ist -- und wie sich
dies auf die in Definition~\ref{def:signatur-horizont} definierte Signatur auswirkt
(vermutlich mÃ¼sste $\mathrm{Sig}(G_s^h\!\restriction_\rho)$ rollenabhÃ¤ngig berechnet
werden, was die Cache-KohÃ¤renz aus Abschnitt~\ref{sec:hst-cache} um eine zusÃ¤tzliche
Dimension -- die Rolle -- erweitert).

\subsubsection{Komposition zu einem globalen Anwendungsgraphen}

Reale Anwendungen bestehen aus mehreren Prozessautomaten $P_1,\ldots,P_n$ -- etwa einem
Bestellprozess (Beispiel~\ref{bsp:bestellprozess}), einem Retourenprozess und einem
Reklamationsprozess, die Ã¼ber gemeinsame Ressourcen (dieselbe Bestellung) verknÃ¼pft sind.
Eine natÃ¼rliche Erweiterung von Definition~\ref{def:horizont-subgraph} wÃ¤re ein
\emph{globaler Anwendungsgraph} $G_{\mathrm{App}} := \bigsqcup_{i=1}^n P_i \cup
E_{\mathrm{cross}}$, wobei $E_{\mathrm{cross}}$ \emph{Querverweis-Kanten} zwischen
ZustÃ¤nden verschiedener Prozessautomaten enthÃ¤lt (z.\,B.\ vom Endzustand $s_5\in
F_{\text{Bestellung}}$ zum Startzustand $s_0'\in S_{\text{Retoure}}$, gemÃ¤ÃŸ der Operation
\texttt{POST /orders/\{id\}/returns}). Der Horizont-Subgraph $G_s^h$ eines Zustands $s$
wÃ¼rde dann gegebenenfalls \emph{ZustÃ¤nde anderer Prozessautomaten} als Randknoten
enthalten, sobald $h$ groÃŸ genug ist, um $E_{\mathrm{cross}}$-Kanten zu erreichen. Dies
wÃ¼rde HATEOAS von der Navigation \emph{innerhalb} eines Prozesses zur Navigation
\emph{zwischen} Prozessen verallgemeinern -- formal mÃ¼sste hierzu
Lemma~\ref{lem:horizont-groesse} auf $G_{\mathrm{App}}$ mit einem
\emph{kompositionalen} Verzweigungsgrad $d_{\mathrm{App}}=\max(d_1,\ldots,d_n,
|E_{\mathrm{cross}}|)$ verallgemeinert werden, und Satz~\ref{satz:optimaler-horizont}
mÃ¼sste klÃ¤ren, ob ein einheitlicher Horizont $h^\ast$ Ã¼ber Prozessgrenzen hinweg
sinnvoll ist oder ob $E_{\mathrm{cross}}$-Kanten mit einem eigenen, typischerweise
grÃ¶ÃŸeren Kostenfaktor $\alpha_{\mathrm{cross}}>\alpha$ (Cross-Service-Aufruf statt
lokaler Tabellenzugriff) zu gewichten sind.

\subsubsection{Graphkompression fÃ¼r groÃŸe Horizonte mittels Signaturen}

FÃ¼r $h\ge r$ (Bemerkung~\ref{bem:hst-permutierbar}) enthÃ¤lt $G_{s_c}^h$ fÃ¼r einen
Sammelzustand $s_c$ den vollstÃ¤ndigen $2^r$-Knoten-Entscheidungsbaum aller Teilmengen
$T\subseteq\{1,\ldots,r\}$ -- nach Lemma~\ref{lem:zustandsexplosion} potenziell sehr
groÃŸ. Viele dieser TeilbÃ¤ume sind jedoch \emph{isomorph}: Der von einem Zwischenzustand
$s_T$ aus erreichbare Teilgraph hÃ¤ngt (bei symmetrischen optionalen Teilschritten) nur
von $|\{1,\ldots,r\}\setminus T|$, der Anzahl noch ausstehender Teilschritte, ab, nicht
von $T$ selbst. Eine signaturbasierte Kompression kÃ¶nnte daher isomorphe TeilbÃ¤ume durch
\emph{Verweise} auf eine einzige kanonische ReprÃ¤sentation ersetzen, identifiziert Ã¼ber
die Signatur $\mathrm{Sig}(\cdot)$ aus Definition~\ref{def:signatur-horizont}: Statt
$\binom{r}{k}$ strukturell identischer TeilbÃ¤ume fÃ¼r $|T|=k$ wÃ¼rde nur \emph{ein}
Teilbaum Ã¼bertragen und $\binom{r}{k}$ Referenzen darauf. Dies entspricht dem
klassischen Prinzip der \emph{strukturellen Teilung} (\emph{structure sharing}) in
funktionalen Datenstrukturen, Ã¼bertragen auf die HST-ReprÃ¤sentation; eine formale
Analyse mÃ¼sste zeigen, dass diese Kompression die in
Bemerkung~\ref{bem:horizont-groesse-praxis} festgestellte exponentielle
ÃœbertragungsgrÃ¶ÃŸe $|V_s^h|=O(d^h)$ auf eine \emph{polynomielle} GrÃ¶ÃŸe der komprimierten
ReprÃ¤sentation reduziert -- ein direktes Analogon zur in
Bemerkung~\ref{bem:permutierbar-fazit} gezeigten Raumersparnis von $O(2^r)$ auf $O(r)$
fÃ¼r die serverseitige Kodierung, nun angewandt auf die clientseitig Ã¼bertragene
HST-ReprÃ¤sentation.

\subsubsection{Beziehung zu GraphQL: clientseitig verhandelter Horizont}

Der in Abschnitt~\ref{sec:ausblick} (Unterabschnitt â€žIntegration mit GraphQL, gRPC und
AsyncAPIâ€œ) angesprochene Vergleich mit GraphQL gewinnt durch HST eine neue Dimension:
GraphQL erlaubt dem Client, die \emph{Tiefe} verschachtelter Relationen einer Anfrage
selbst zu spezifizieren (etwa â€žlade einen Autor, dessen BÃ¼cher, und fÃ¼r jedes Buch dessen
Rezensionenâ€œ -- eine Tiefe von $2$). Der Horizont $h$ in
Definition~\ref{def:hst-repraesentation} ist strukturell analog: Ein HST-Server kÃ¶nnte
$h$ nicht als statische Konfiguration, sondern als \emph{vom Client pro Anfrage
spezifizierten Parameter} behandeln (wie bereits in Satz~\ref{satz:hst-zustandslosigkeit}
unter Punkt (3) als Option erwÃ¤hnt), wodurch der Client den in
Satz~\ref{satz:optimaler-horizont} berechneten Tradeoff selbst -- abhÃ¤ngig von seiner
\emph{eigenen} EinschÃ¤tzung von $\beta$ (seiner VerbindungsqualitÃ¤t) und $p$ (seinem
geplanten Workflow) -- vornehmen kÃ¶nnte, statt sich auf eine serverseitige SchÃ¤tzung von
$\hat\beta,\hat p$ zu verlassen. Eine GraphQL-artige Anfrage kÃ¶nnte dann etwa lauten:
\texttt{GET /orders/\{id\}?horizon=3\&fields=state,total}, wobei \texttt{horizon=3} den
gewÃ¼nschten $h$-Wert und \texttt{fields} die gewÃ¼nschten Felder von $\rho(id,s)$
spezifiziert -- eine Verschmelzung der â€ždeklarativen Datenanfrageâ€œ von GraphQL mit der
â€ždeklarativen Navigationsanfrageâ€œ von HST, die formal als Erweiterung von
Definition~\ref{def:hst-repraesentation} um einen client-spezifizierten Parameter $h$
(statt eines fest konfigurierten) zu fassen wÃ¤re.

\subsubsection{Empirische Validierung des Kostenmodells $(\alpha,\beta,p)$}

Der in Abschnitt~\ref{sec:ausblick} (Unterabschnitt â€žExperimentelle Validierung in
Produktionssystemenâ€œ) fÃ¼r SC-REST skizzierte empirische Validierungsbedarf gilt fÃ¼r HST
in verstÃ¤rktem MaÃŸe, da Satz~\ref{satz:optimaler-horizont} -- anders als die
asymptotischen Aussagen zu SC-REST -- von \emph{konkreten Werten} der Parameter
$\alpha,\beta,p,d$ abhÃ¤ngt. Eine produktive Instrumentierung mÃ¼sste (1) $\alpha$ aus der
tatsÃ¤chlich gemessenen SerialisierungsgrÃ¶ÃŸe von Knoten/Kanten kalibrieren (die in
Abbildung~\ref{fig:horizont-groesse} angenommenen 250\,Byte sind eine grobe SchÃ¤tzung;
reale JSON- oder HAL-ReprÃ¤sentationen kÃ¶nnten durch Kompression wie gzip oder durch das
in Abschnitt~\ref{sec:ausblick} (Graphkompression) skizzierte \emph{structure sharing}
deutlich darunter liegen), (2) $\beta$ aus der tatsÃ¤chlichen Verteilung der
Round-Trip-Zeiten verschiedener Clientklassen (mobil, Desktop, Service-zu-Service)
erheben, und (3) $p$ aus den \emph{tatsÃ¤chlichen} SequenzlÃ¤ngenverteilungen
unterschiedlicher Prozesstypen schÃ¤tzen -- letzteres ist methodisch eng mit der fÃ¼r
Abbildung~\ref{fig:lernkurve} verwendeten empirischen Fehlerratenkurve $p(n)$ verwandt,
jedoch mit umgekehrter Fragestellung: WÃ¤hrend $p(n)$ die Wahrscheinlichkeit \emph{eines
Sequenzfehlers nach $n$ Trainingsanfragen} misst, beschreibt $p$ in
Definition~\ref{def:kostenmodell-horizont} die Wahrscheinlichkeit \emph{einer
Fortsetzung} der Sequenz unabhÃ¤ngig von deren Korrektheit. Mit kalibrierten
$(\hat\alpha,\hat\beta,\hat p)$ lieÃŸe sich die in
Abbildung~\ref{fig:optimaler-horizont} gezeigte theoretische Stufenfunktion $h^\ast$
gegen den empirisch tatsÃ¤chlich kostenoptimalen Horizont validieren.

\subsubsection{Versionsmigration laufender Prozessinstanzen}

Satz~\ref{satz:hst-zustandslosigkeit} setzt voraus, dass $P$ wÃ¤hrend der Bearbeitung
einer Anfrage statisch ist; Abschnitt~\ref{sec:hst-cache} behandelt \emph{Erkennung} von
Ã„nderungen $P\to P'$ via Signaturvergleich, jedoch nicht deren \emph{Behandlung} fÃ¼r
\emph{bereits laufende} Prozessinstanzen $(id,s)$ mit $s\in S$. Wird $P$ zu $P'$
aktualisiert (etwa weil ein neuer optionaler Teilschritt $\sigma_{r+1}$ eingefÃ¼hrt oder
ein Zustand $s\in S$ aus $S'$ entfernt wird), stellt sich die Frage, wie eine Instanz mit
$\pi(id)=s\notin S'$ zu behandeln ist. Eine formale Behandlung dieses Problems kÃ¶nnte
eine \emph{Migrationsabbildung} $\mu: S\rightharpoonup S'$ definieren, mit den
Anforderungen: (a) $\mu(s)=s$ fÃ¼r $s\in S\cap S'$ (unverÃ¤nderte ZustÃ¤nde bleiben
erhalten), (b) fÃ¼r $s\in S\setminus S'$ muss $\mu(s)\in S'$ einen â€žÃ¤quivalentenâ€œ Zustand
in $P'$ bezeichnen, wobei Ã„quivalenz bedeutet, dass die fÃ¼r $s$ in $P$ konformen
Restsequenzen (gemÃ¤ÃŸ Definition~\ref{def:konformitaet}) unter $\mu$ auf in $P'$ ab
$\mu(s)$ konforme Restsequenzen abgebildet werden -- eine Bedingung, die strukturell an
die in Satz~\ref{satz:lokale-vollstaendigkeit} verwendete Charakterisierung Ã¼ber
$\delta^{*}$ erinnert, jedoch \emph{zwischen zwei verschiedenen Automaten} $P$ und $P'$
statt innerhalb eines einzelnen $G_s^h$. Existiert kein solches $\mu(s)$ (etwa weil ein
ganzer Prozesszweig in $P'$ entfernt wurde), mÃ¼sste die Instanz in einen speziellen
Migrationszustand Ã¼berfÃ¼hrt und gegebenenfalls manuell oder durch eine
Kompensationslogik (vgl.\ das in Abschnitt~\ref{sec:ausblick} erwÃ¤hnte Saga-Pattern und
\cite{garcia1987}) abgeschlossen werden. Die in Abschnitt~\ref{sec:hst-cache}
beschriebene signaturbasierte Erkennung von $P\to P'$ wÃ¤re damit der \emph{Trigger}, die
hier skizzierte Migrationsabbildung $\mu$ die \emph{Behandlung} -- zusammen ergÃ¤ben sie
ein vollstÃ¤ndiges Lebenszyklusmodell fÃ¼r Prozessautomaten, die sich Ã¼ber die Lebensdauer
einzelner Instanzen hinweg Ã¤ndern kÃ¶nnen.

\subsection{Implementierungshinweis}

Der vollstÃ¤ndige Code der Referenzimplementierung sowie ergÃ¤nzende TestfÃ¤lle sind
verfÃ¼gbar unter: \url{https://github.com/hjstephan86/science/tree/main/science/screst}.

\begin{thebibliography}{99}

\bibitem{fielding2000}
Fielding, R.~T. (2000).
\emph{Architectural Styles and the Design of Network-based Software Architectures}.
Dissertation, University of California, Irvine.

\bibitem{rfc7231}
Fielding, R., Reschke, J. (Hrsg.) (2014).
\emph{Hypertext Transfer Protocol (HTTP/1.1): Semantics and Content}.
RFC~7231, IETF.

\bibitem{rfc7807}
Nottingham, M., Wilde, E. (2016).
\emph{Problem Details for HTTP APIs}.
RFC~7807, IETF.

\bibitem{richardson2007}
Richardson, L., Ruby, S. (2007).
\emph{RESTful Web Services}.
O'Reilly Media.

\bibitem{fowler2010}
Fowler, M. (2010).
\emph{Richardson Maturity Model -- steps toward the glory of REST}.
martinfowler.com.

\bibitem{webber2010}
Webber, J., Parastatidis, S., Robinson, I. (2010).
\emph{REST in Practice: Hypermedia and Systems Architecture}.
O'Reilly Media.

\bibitem{newman2021}
Newman, S. (2021).
\emph{Building Microservices: Designing Fine-Grained Systems}, 2.~Auflage.
O'Reilly Media.

\bibitem{hohpe2003}
Hohpe, G., Woolf, B. (2003).
\emph{Enterprise Integration Patterns: Designing, Building, and Deploying Messaging
Solutions}.
Addison-Wesley.

\bibitem{garcia1987}
Garcia-Molina, H., Salem, K. (1987).
\emph{Sagas}.
ACM SIGMOD Record, 16(3), S.~249--259.

\bibitem{helland2007}
Helland, P. (2007).
\emph{Life Beyond Distributed Transactions: An Apostate's Opinion}.
3rd Biennial Conference on Innovative Data Systems Research (CIDR).

\bibitem{abboud2015}
Abboud, A., Backurs, A., Vassilevska Williams, V. (2015).
\emph{Tight Hardness Results for LCS and Other Sequence Similarity Measures}.
IEEE 56th Annual Symposium on Foundations of Computer Science (FOCS).

\bibitem{epp2026subgraph}
Epp, S. (2026).
\emph{Der Subgraph Algorithmus}.
GitHub: \url{https://github.com/hjstephan86/subgraph}.

\bibitem{murata1989}
Murata, T. (1989).
\emph{Petri Nets: Properties, Analysis and Applications}.
Proceedings of the IEEE, 77(4), S.~541--580.

\bibitem{pautasso2008}
Pautasso, C., Zimmermann, O., Leymann, F. (2008).
\emph{RESTful Web Services vs. ``Big'' Web Services: Making the Right Architectural
Decision}.
Proceedings of the 17th International Conference on World Wide Web (WWW).

\end{thebibliography}

\end{document}

