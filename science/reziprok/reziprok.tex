\documentclass[a4paper,12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[main=german,provide=*]{babel}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{microtype}

\geometry{margin=2.5cm}
\setlength{\emergencystretch}{3em}
\sloppy

\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem{beispiel}{Beispiel}
\newtheorem{satz}{Satz}
\newtheorem{lemma}{Lemma}
\newtheorem{bemerkung}{Bemerkung}
\newtheorem{korollar}{Korollar}

\setlist[itemize,1]{label=--}

% ---------- Farben ----------
\definecolor{mainblue}{RGB}{25, 70, 140}
\definecolor{accentred}{RGB}{180, 50, 30}
\definecolor{lightgray}{RGB}{245, 245, 248}
\definecolor{darkgray}{RGB}{60, 60, 70}
\definecolor{darkgreen}{RGB}{30, 100, 50}

\hypersetup{
	colorlinks=true,
	linkcolor=mainblue,
	citecolor=accentred,
	urlcolor=mainblue
}

%  Code-Highlighting
\lstset{
	basicstyle=\ttfamily\small,
	keywordstyle=\color{blue},
	commentstyle=\color{gray},
	stringstyle=\color{red},
	numbers=left,
	numberstyle=\tiny,
	breaklines=true,
	frame=single,
	literate=%
	{Ã–}{{\"O}}1
	{Ã„}{{\"A}}1
	{Ãœ}{{\"U}}1
	{ÃŸ}{{\ss}}1
	{Ã¼}{{\"u}}1
	{Ã¤}{{\"a}}1
	{Ã¶}{{\"o}}1
	{Î£}{{$\Sigma$}}1
	{â†’}{{$\rightarrow$}}1
	{âŠ†}{{$\subseteq$}}1
	{âŠ‡}{{$\supseteq$}}1
	{â‰¥}{{$\geq$}}1
}

\title{Der paradoxe Gewinn im Reziproken:\\
Die Rolle des Exponenten $n$ zur Basis $2$\\
in der Signaturfunktion des Subgraph Algorithmus}
\author{Stephan Epp}
\date{11. Juni 2026}

\begin{document}

\maketitle

\tableofcontents
\newpage

\section{EinfÃ¼hrung}

Der Subgraph Algorithmus vergleicht zwei Graphen $G$ und $G'$ mittels eindeutiger
Spalten-Signaturen und zyklischer Rotation und erreicht dabei eine optimale
Laufzeit von $O(n^3)$. Zentrales Element der Konstruktion ist die Signaturfunktion
\[
	\sigma_j = \sum_{i=0}^{n-1} A_{ij} \cdot 2^i + j \cdot 2^n,
\]
in welcher der Exponent $n$ zur Basis $2$ gleich zweifach auftritt: einmal als
obere Grenze der Zeilensumme (\emph{Zeilenkomponente}, $\sum_i A_{ij}2^i < 2^n$)
und einmal als multiplikatives Gewicht der Spalte (\emph{Spaltenkomponente},
$j \cdot 2^n$).

Die vorliegende Arbeit untersucht ein auf den ersten Blick paradoxes PhÃ¤nomen,
das wir als \emph{paradoxen Gewinn im Reziproken} bezeichnen: WÃ¤hrend der
Term $2^n$ mit wachsendem $n$ exponentiell divergiert, konvergiert sein
Reziprokes $2^{-n}$ exponentiell gegen $0$. Genau diese Divergenz-Konvergenz-DualitÃ¤t
ist jedoch keine SchwÃ¤che, sondern die strukturelle Ursache dafÃ¼r, dass
\begin{itemize}
	\item die Signaturfunktion $\sigma$ \emph{injektiv} ist (Eindeutigkeit, vgl.
	      Lemma~1 der Originalarbeit \cite{epp2026subgraph}),
	\item Zeilen- und Spalteninformation durch eine einzige reziproke Skalierung
	      $\sigma_j \cdot 2^{-n}$ \emph{verlustfrei und ohne zusÃ¤tzlichen Speicheraufwand}
	      getrennt werden kÃ¶nnen,
	\item und diese Trennung exakt die Struktur ist, welche die untere
	      Schranke $\Omega(n^3)$ fÃ¼r das zyklische Subgraph-Problem erzwingt.
\end{itemize}

\subsection{Problemstellung}

Sei $n \in \mathbb{N}$, $n \geq 1$, und sei $A \in \{0,1\}^{n \times n}$ eine
Adjazenzmatrix. FÃ¼r jede Spalte $j \in \{0,\ldots,n-1\}$ sei
\[
	r_j := \sum_{i=0}^{n-1} A_{ij} \cdot 2^i \in \{0, 1, \ldots, 2^n - 1\}
\]
die \emph{Zeilenkomponente} und $\sigma_j = r_j + j \cdot 2^n$ die zugehÃ¶rige
Signatur.

\textbf{Frage:} Welche formalen Eigenschaften besitzt die Abbildung
$\sigma_j \mapsto (j, r_j)$, und in welchem Sinn stellt der Ãœbergang
$2^n \mapsto 2^{-n}$ einen \emph{Gewinn} dar, obwohl er auf den ersten Blick
nur eine triviale algebraische Inversion ist?

\subsection{Motivation}

In der Praxis erscheinen exponentiell wachsende GrÃ¶ÃŸen wie $2^n$ zunÃ¤chst
als Nachteil: Speicherplatz und Zahlendarstellung wachsen mit $n$. Das
\emph{Reziproke} $2^{-n}$ hingegen erscheint als vernachlÃ¤ssigbar kleine GrÃ¶ÃŸe,
die â€žverschwindet''. Der paradoxe Gewinn besteht darin, dass die
Multiplikation mit $2^{-n}$ \emph{nicht} Information vernichtet, sondern
sie in zwei disjunkte, unmittelbar auslesbare Komponenten â€“ Ganzzahlanteil
(Spaltenindex $j$) und Fraktionalanteil (Zeileninformation $r_j / 2^n$) â€“
zerlegt. Dieser Mechanismus ist die mathematische Grundlage dafÃ¼r, dass der
Subgraph Algorithmus auf zusÃ¤tzliche Indexstrukturen verzichten kann und
dennoch $O(n^3)$ erreicht.

\section{Grundlagen}

\subsection{Definitionen}

\begin{definition}[Zeilen- und Spaltenkomponente]
	FÃ¼r $A \in \{0,1\}^{n\times n}$ und $j \in \{0,\ldots,n-1\}$ heiÃŸen
	\[
		r_j := \sum_{i=0}^{n-1} A_{ij}\cdot 2^i
		\qquad\text{und}\qquad
		c_j := j \cdot 2^n
	\]
	die \emph{Zeilenkomponente} bzw. \emph{Spaltenkomponente} der Signatur
	$\sigma_j = r_j + c_j$.
\end{definition}

\begin{definition}[Reziproke Skalierung]
	Die reziproke Skalierung der Signatur $\sigma_j$ ist definiert als
	\[
		\hat\sigma_j := \sigma_j \cdot 2^{-n} \in \mathbb{Q}_{\geq 0}.
	\]
\end{definition}

\begin{definition}[Ganzzahl- und Fraktionalanteil]
	FÃ¼r $x \in \mathbb{Q}_{\geq 0}$ bezeichne $\lfloor x \rfloor$ den
	Ganzzahlanteil und $\{x\} := x - \lfloor x \rfloor \in [0,1)$ den
	Fraktionalanteil von $x$.
\end{definition}

\subsection{Grundidee}

\begin{beispiel}
	FÃ¼r $n=4$ und Spalte $j=2$ mit Spaltenvektor $[1,0,1,1]^T$ gilt
	\[
		r_2 = 2^0 + 2^2 + 2^3 = 1 + 4 + 8 = 13, \qquad
		c_2 = 2 \cdot 2^4 = 32, \qquad
		\sigma_2 = 45.
	\]
	Reziproke Skalierung mit $2^{-4} = 1/16$ liefert
	\[
		\hat\sigma_2 = \frac{45}{16} = 2{,}8125
		\;=\; \underbrace{2}_{\lfloor \hat\sigma_2 \rfloor = j}
		\;+\; \underbrace{0{,}8125}_{\{\hat\sigma_2\} = r_2/2^4 = 13/16}.
	\]
	Der Ganzzahlanteil liefert exakt den Spaltenindex $j=2$ zurÃ¼ck, der
	Fraktionalanteil exakt die normierte Zeileninformation $r_2/2^n$ â€“
	und das, obwohl $2^{-4}$ als â€žkleine'' Zahl erscheint.
\end{beispiel}

\section{Hauptresultate}

\subsection{Exakte reziproke Zerlegung}

\begin{satz}[Reziproke Zerlegung der Signatur]
\label{satz:zerlegung}
	FÃ¼r alle $n \geq 1$, alle $j \in \{0,\ldots,n-1\}$ und alle
	$A \in \{0,1\}^{n\times n}$ gilt fÃ¼r $\hat\sigma_j = \sigma_j \cdot 2^{-n}$:
	\[
		\lfloor \hat\sigma_j \rfloor = j
		\qquad\text{und}\qquad
		\{\hat\sigma_j\} = \frac{r_j}{2^n}.
	\]
\end{satz}

\begin{proof}
	Nach Definition gilt $\sigma_j = r_j + j\cdot 2^n$ mit
	$r_j = \sum_{i=0}^{n-1}A_{ij}2^i$. Da $A_{ij}\in\{0,1\}$ fÃ¼r alle $i$, gilt
	\[
		0 \;\leq\; r_j \;\leq\; \sum_{i=0}^{n-1} 2^i \;=\; 2^n - 1 \;<\; 2^n.
	\]
	Multiplikation mit $2^{-n} > 0$ liefert
	\[
		\hat\sigma_j \;=\; \sigma_j \cdot 2^{-n}
		\;=\; \frac{r_j}{2^n} + j.
	\]
	Da $0 \leq r_j/2^n < 1$ und $j \in \mathbb{Z}_{\geq 0}$, ist $j$ bereits der
	Ganzzahlanteil von $\hat\sigma_j$ und $r_j/2^n \in [0,1)$ bereits der
	Fraktionalanteil. Aus der Eindeutigkeit der Zerlegung
	$x = \lfloor x \rfloor + \{x\}$ mit $\{x\}\in[0,1)$ fÃ¼r $x \in \mathbb{Q}_{\geq0}$
	folgt
	\[
		\lfloor \hat\sigma_j \rfloor = j, \qquad \{\hat\sigma_j\} = \frac{r_j}{2^n}. \qedhere
	\]
\end{proof}

\begin{korollar}[Konstanz der Trennung]
\label{kor:konstanz}
	Die Breite des Wertebereichs des Fraktionalanteils, $[0,1)$, ist
	\emph{unabhÃ¤ngig von $n$}. Insbesondere gilt fÃ¼r alle $n_1 \neq n_2$ und
	beliebige zugehÃ¶rige Signaturen $\sigma^{(n_1)}_j, \sigma^{(n_2)}_{j'}$:
	\[
		\{\sigma^{(n_1)}_j \cdot 2^{-n_1}\},\;
		\{\sigma^{(n_2)}_{j'} \cdot 2^{-n_2}\} \;\in\; [0,1).
	\]
\end{korollar}

\begin{proof}
	Folgt unmittelbar aus Satz~\ref{satz:zerlegung}, da $\{x\}\in[0,1)$ per
	Definition fÃ¼r jedes $x\in\mathbb{Q}_{\geq0}$ gilt, unabhÃ¤ngig von der
	GrÃ¶ÃŸe von $n$.
\end{proof}

\begin{bemerkung}
	Korollar~\ref{kor:konstanz} ist der formale Kern des â€žParadoxons``: Obwohl
	$\sigma_j$ selbst mit $O(2^n)$ wÃ¤chst (exponentiell in $n$), bleibt das
	durch $2^{-n}$ skalierte Bild $\hat\sigma_j$ in einem Intervall der
	\emph{konstanten} LÃ¤nge $n$ (nÃ¤mlich $[0,n)$, da $j \leq n-1$), und
	dessen Fraktionalanteil sogar in $[0,1)$ -- einer von $n$
	\emph{vollstÃ¤ndig unabhÃ¤ngigen} GrÃ¶ÃŸe. Der scheinbare â€žVerlust`` durch
	das verschwindend kleine $2^{-n}$ wird also vollstÃ¤ndig durch eine
	\emph{strukturelle Normierung} kompensiert: Information wird nicht
	skaliert, sondern \emph{aufgeteilt}.
\end{bemerkung}

\subsection{BijektivitÃ¤t und Informationserhalt}

\begin{lemma}[BijektivitÃ¤t der reziproken Abbildung]
\label{lem:bijektiv}
	Die Abbildung
	\[
		\Phi_n : \{0,1\}^n \times \{0,\ldots,n-1\} \longrightarrow
		\{0,\ldots,n-1\} \times \left\{\tfrac{0}{2^n},\tfrac{1}{2^n},\ldots,\tfrac{2^n-1}{2^n}\right\},
	\]
	\[
		\Phi_n(A_{\cdot j}, j) = (j,\ r_j/2^n) = (\lfloor\hat\sigma_j\rfloor, \{\hat\sigma_j\})
	\]
	ist eine Bijektion.
\end{lemma}

\begin{proof}
	\textbf{Wohldefiniertheit:} Nach Satz~\ref{satz:zerlegung} liegt
	$r_j/2^n$ stets in der angegebenen diskreten Menge von $2^n$ Ã¤quidistanten
	Werten in $[0,1)$, da $r_j \in \{0,\ldots,2^n-1\}$ ganzzahlig ist.

	\textbf{InjektivitÃ¤t:} Seien $(A_{\cdot j}, j) \neq (A'_{\cdot j'}, j')$.
	Fall (a) $j \neq j'$: dann unterscheiden sich die Bilder bereits in der
	ersten Komponente. Fall (b) $j = j'$, $A_{\cdot j} \neq A'_{\cdot j'}$:
	Da die Abbildung $A_{\cdot j} \mapsto r_j = \sum_i A_{ij}2^i$ die
	Standard-BinÃ¤rdarstellung einer Zahl in $\{0,\ldots,2^n-1\}$ ist, und
	diese Darstellung eindeutig ist, folgt $r_j \neq r'_j$ und somit
	$r_j/2^n \neq r'_j/2^n$.

	\textbf{SurjektivitÃ¤t:} FÃ¼r jedes Paar $(j, k/2^n)$ mit
	$k\in\{0,\ldots,2^n-1\}$ liefert die BinÃ¤rdarstellung von $k$ als Vektor
	$A_{\cdot j} \in \{0,1\}^n$ ein Urbild, da jede Zahl $k \in \{0,\ldots,2^n-1\}$
	eine eindeutige BinÃ¤rdarstellung mit $n$ Stellen besitzt. \qedhere
\end{proof}

\begin{satz}[Informationserhalt ohne Zusatzspeicher]
\label{satz:infoerhalt}
	Die Signatur $\sigma_j \in \{0,\ldots,n\cdot 2^n - 1\}$ kodiert das Paar
	$(j, A_{\cdot j}) \in \{0,\ldots,n-1\}\times\{0,1\}^n$ verlustfrei in
	einem einzigen Maschinenwort der BitlÃ¤nge
	\[
		B(n) = \left\lceil \log_2(n\cdot 2^n) \right\rceil
		= n + \left\lceil \log_2 n \right\rceil,
	\]
	ohne dass $j$ und $A_{\cdot j}$ in getrennten Speicherfeldern abgelegt
	werden mÃ¼ssen.
\end{satz}

\begin{proof}
	Nach Lemma~\ref{lem:bijektiv} ist $\Phi_n$ bijektiv, also ist die
	Gesamtabbildung
	\[
		(j, A_{\cdot j}) \;\longmapsto\; \sigma_j = r_j + j\cdot 2^n
	\]
	als Komposition von $\Phi_n$ mit der (ebenfalls bijektiven, da
	$2^n$-adische Stellenwertdarstellung) Abbildung
	$(j, r_j/2^n) \mapsto j\cdot 2^n + r_j$ injektiv. Der Bildbereich ist
	$\{0,\ldots, (n-1)\cdot 2^n + (2^n-1)\} = \{0,\ldots,n\cdot2^n-1\}$, also
	$n\cdot 2^n$ verschiedene Werte, was $\lceil\log_2(n\cdot2^n)\rceil$ Bit
	benÃ¶tigt.

	Die getrennte Speicherung von $j\in\{0,\ldots,n-1\}$
	($\lceil\log_2 n\rceil$ Bit) und $A_{\cdot j}\in\{0,1\}^n$ ($n$ Bit)
	benÃ¶tigt in der Summe ebenfalls $n + \lceil\log_2 n\rceil$ Bit â€“ jedoch
	in zwei getrennten Feldern, was in realen Implementierungen wegen
	Speicherausrichtung (\emph{alignment}) und Strukturkosten
	(\emph{struct padding}) typischerweise ein zusÃ¤tzliches Maschinenwort
	(z.\,B. $32$ oder $64$ Bit) erfordert. Die Signatur $\sigma_j$ vermeidet
	dies vollstÃ¤ndig, da $j$ und $r_j$ via Satz~\ref{satz:zerlegung} aus
	einer einzigen Zahl in $O(1)$ durch die reziproke Skalierung
	$\sigma_j \cdot 2^{-n}$ (Ã¤quivalent: Ganzzahldivision durch $2^n$
	und Modulo $2^n$) zurÃ¼ckgewonnen werden. \qedhere
\end{proof}

\begin{korollar}[Der paradoxe Gewinn]
\label{kor:paradox}
	Der Ãœbergang von $2^n$ (Multiplikation, Kodierung) zu $2^{-n}$
	(Division, Dekodierung) ist algebraisch trivial (eine einzige
	Multiplikation mit dem Kehrwert), bewirkt aber gemÃ¤ÃŸ
	Satz~\ref{satz:zerlegung} und Satz~\ref{satz:infoerhalt} die vollstÃ¤ndige,
	verlustfreie Trennung zweier strukturell verschiedener Informationsklassen
	(Position $j$ und Inhalt $A_{\cdot j}$) bei \emph{negativen} Zusatzkosten
	gegenÃ¼ber getrennter Speicherung. Der â€žGewinn`` entsteht somit nicht durch
	zusÃ¤tzliche Information, sondern durch \emph{Eliminierung struktureller
	Redundanz}, die bei naiver getrennter Kodierung anfallen wÃ¼rde.
\end{korollar}

\subsection{Konsequenz fÃ¼r die Laufzeit $O(n^3)$}

\begin{satz}[Reziproke Zerlegung als Voraussetzung fÃ¼r $O(n^3)$]
\label{satz:laufzeitkonsequenz}
	Die in Satz~\ref{satz:zerlegung} bewiesene reziproke Zerlegung ist eine
	notwendige Voraussetzung fÃ¼r die $O(n^2)$-Laufzeit der
	Rotation-Match-Operation (Schritt~3 des Subgraph Algorithmus, vgl.
	\cite{epp2026subgraph}), und damit fÃ¼r die Gesamtlaufzeit $O(n^3)$.
\end{satz}

\begin{proof}
	Im Rotation-Match-Schritt wird fÃ¼r jede der $n$ zyklischen Rotationen die
	\emph{Zeilenkomponente} $r_j = \sigma_j \bmod 2^n$ benÃ¶tigt (vgl. Listing
	â€žRotation-Match`` in \cite{epp2026subgraph}, Zeilen \texttt{row\_sigs\_A
	= [sig \% col\_weight\_A for sig in sig\_A]}). Diese Operation ist
	Ã¤quivalent zur Extraktion des Fraktionalanteils $\{\sigma_j\cdot2^{-n}\}$
	skaliert mit $2^n$, gemÃ¤ÃŸ Satz~\ref{satz:zerlegung}.

	WÃ¤re die Zerlegung \emph{nicht} in $O(1)$ pro Signatur durchfÃ¼hrbar
	(etwa weil $j$ und $r_j$ erst durch eine Suche oder einen
	Vergleichsschritt mit Aufwand $\omega(1)$ getrennt werden mÃ¼ssten),
	so entstÃ¼nde pro Rotation ein zusÃ¤tzlicher Faktor $f(n) = \omega(1)$,
	und die Gesamtlaufzeit der Rotation-Match-Phase wÃ¤re
	\[
		O\bigl(n \cdot (n^2 + n^2)\cdot f(n)\bigr) = \omega(n^3).
	\]
	Da nach Lemma~\ref{lem:bijektiv} und Satz~\ref{satz:zerlegung} die
	Zerlegung $\sigma_j \mapsto (j, r_j)$ jedoch durch die arithmetischen
	$O(1)$-Operationen Ganzzahldivision durch $2^n$ (bzw. Multiplikation mit
	$2^{-n}$) und Modulo $2^n$ erfolgt â€“ beides Bitschiebeoperationen
	(\texttt{shift}/\texttt{mask}), da $2^n$ eine Zweierpotenz ist â€“ gilt
	$f(n) = O(1)$, und die in \cite{epp2026subgraph} (Satz â€žLaufzeit``)
	gezeigte Schranke
	\[
		T_{\text{Rotation-Match}}(n) = O(n\cdot(n^2+n^2)) = O(n^3)
	\]
	bleibt gÃ¼ltig. \qedhere
\end{proof}

\begin{bemerkung}
	Satz~\ref{satz:laufzeitkonsequenz} zeigt, dass der â€žparadoxe Gewinn``
	keine bloÃŸe Ã¤sthetische Beobachtung ist, sondern eine
	\emph{Korrektheitsvoraussetzung} der in \cite{epp2026subgraph} bewiesenen
	$\Theta(n^3)$-OptimalitÃ¤t: Ohne die $O(1)$-Zerlegbarkeit via $2^{\pm n}$
	wÃ¼rde die Gesamtschranke aus Lemma â€žUnabhÃ¤ngigkeit der Rotationsvergleiche``
	(\cite{epp2026subgraph}) um den Faktor $f(n)$ verschlechtert, was die
	scharfe untere Schranke $\Omega(n^{3-\varepsilon})$ nicht mehr matchen
	wÃ¼rde.
\end{bemerkung}

\section{Veranschaulichung}

\subsection{Divergenz von $2^n$ und $2^{-n}$}

Abbildung~\ref{fig:plot1} zeigt die exponentielle Divergenz von $2^n$
(Spaltengewicht, linke Achse, logarithmisch) und die exponentielle
Konvergenz von $2^{-n}$ gegen $0$ (rechte Achse, logarithmisch) fÃ¼r
$n=1,\ldots,16$. Beide Kurven verlaufen exakt spiegelsymmetrisch auf der
logarithmischen Skala, was die algebraische ReziprozitÃ¤t
$\log_2(2^n) = -\log_2(2^{-n})$ unmittelbar sichtbar macht. Auf den ersten
Blick legt dies nahe, dass $2^{-n}$ fÃ¼r groÃŸe $n$ â€žbedeutungslos`` klein
und damit irrelevant wird.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.75\textwidth]{plot1.pdf}
	\caption{Divergenz von $2^n$ und $2^{-n}$ fÃ¼r $n=1,\ldots,16$ (doppelt-logarithmische
	Darstellung). Die scheinbare â€žBedeutungslosigkeit`` von $2^{-n}$ fÃ¼r groÃŸe $n$
	ist der Ausgangspunkt des untersuchten Paradoxons.}
	\label{fig:plot1}
\end{figure}

\subsection{Konstante Trennung trotz wachsendem $n$}

Abbildung~\ref{fig:plot2} widerlegt diese Intuition: FÃ¼r drei verschieden
groÃŸe Zufallsgraphen ($n=4,8,12$) wird die reziprok skalierte Signatur
$\hat\sigma_j = \sigma_j\cdot2^{-n}$ in Ganzzahl- und Fraktionalanteil
zerlegt. Trotz exponentiell wachsender absoluter Werte von $\sigma_j$
(GrÃ¶ÃŸenordnung $O(n\cdot2^n)$) bleibt der Fraktionalanteil â€“ die
Zeileninformation â€“ fÃ¼r \emph{alle} $n$ exakt im Intervall $[0,1)$, und der
(reskalierte) Ganzzahlanteil identifiziert exakt den Spaltenindex $j/n$.
Dies ist die graphische BestÃ¤tigung von Satz~\ref{satz:zerlegung} und
Korollar~\ref{kor:konstanz}: Die â€žAuflÃ¶sung`` der Trennung ist unabhÃ¤ngig
von $n$, obwohl der Wertebereich von $\sigma_j$ selbst mit $n$ wÃ¤chst.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=\textwidth]{plot2.pdf}
	\caption{Reziproke Skalierung $\sigma_j\cdot2^{-n}$ fÃ¼r $n=4,8,12$. GrÃ¼ne Punkte:
	Fraktionalanteil (Zeileninformation $r_j/2^n$), stets in $[0,1)$. Blaue Kreuze:
	normierter Ganzzahlanteil (Spaltenindex $j/n$). Die Struktur der Trennung ist
	fÃ¼r alle drei Werte von $n$ identisch.}
	\label{fig:plot2}
\end{figure}

\subsection{Informationsgewinn gegenÃ¼ber getrennter Kodierung}

Abbildung~\ref{fig:plot3} quantifiziert den in Satz~\ref{satz:infoerhalt} und
Korollar~\ref{kor:paradox} bewiesenen Gewinn. Die rote Kurve zeigt die
benÃ¶tigten Bits pro Spalte bei \emph{getrennter} Speicherung von
Zeilenvektor und Spaltenindex inklusive eines typischen Strukturkosten-Overheads
von $32$ Bit (z.\,B. durch \emph{padding} bei Wortgrenzen-Ausrichtung in
C-Strukturen). Die grÃ¼ne Kurve zeigt die Bitanzahl der kombinierten
Signatur $\sigma_j$, welche \emph{ohne} diesen Overhead auskommt, da $j$ und
$r_j$ gemÃ¤ÃŸ Satz~\ref{satz:zerlegung} verlustfrei aus einer einzigen Zahl
rekonstruierbar sind. Die schraffierte FlÃ¤che entspricht den eingesparten
Bits â€“ dem â€žparadoxen Gewinn`` in absoluten Speichereinheiten.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.75\textwidth]{plot3.pdf}
	\caption{Vergleich des Speicherbedarfs pro Spalte: getrennte Kodierung
	(Zeilenvektor + Spaltenindex + Strukturkosten) vs. kombinierte Signatur
	$\sigma_j$. Die schraffierte FlÃ¤che zeigt den durch die reziproke
	Zerlegbarkeit eingesparten Speicherbedarf.}
	\label{fig:plot3}
\end{figure}

\subsection{Konsequenz fÃ¼r die Gesamtlaufzeit}

Abbildung~\ref{fig:plot4} stellt schlieÃŸlich die in
Satz~\ref{satz:laufzeitkonsequenz} bewiesene Konsequenz dar: Links wird die
Operationsanzahl des Subgraph Algorithmus ($O(n^3)$) mit dem naiven
permutationsbasierten Ansatz ($O(n!\cdot n^2)$) fÃ¼r kleine $n$ verglichen
(logarithmische Skala) â€“ der Unterschied wird bereits ab $n\approx 6$
dramatisch. Rechts ist das kubische Wachstum $n^3$ fÃ¼r grÃ¶ÃŸere $n$ separat
dargestellt. Ohne die in Abschnitt~3 bewiesene $O(1)$-Zerlegbarkeit der
Signatur via $2^{\pm n}$ wÃ¤re selbst diese kubische Schranke nicht erreichbar.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=\textwidth]{plot4.pdf}
	\caption{Links: Vergleich der Operationsanzahl zwischen naivem
	Permutationsansatz $O(n!\cdot n^2)$ und Subgraph Algorithmus $O(n^3)$ fÃ¼r
	$n=1,\ldots,8$ (logarithmische Skala). Rechts: kubisches Wachstum $n^3$ fÃ¼r
	$n=1,\ldots,29$, die durch die reziproke Zerlegung garantierte
	GesamtkomplexitÃ¤t.}
	\label{fig:plot4}
\end{figure}

\section{Verallgemeinerung: Das Reziproke-Trennungs-Prinzip}
\label{sec:verallgemeinerung}

Die in Abschnitt~3 fÃ¼r die Signaturfunktion des Subgraph Algorithmus
bewiesenen Resultate (Satz~\ref{satz:zerlegung}, Lemma~\ref{lem:bijektiv},
Satz~\ref{satz:infoerhalt}) besitzen eine gemeinsame Struktur, die Ã¼ber den
konkreten Anwendungsfall hinausweist. In diesem Abschnitt formulieren wir
diese Struktur als allgemeines Prinzip, leiten daraus neue
Schlussfolgerungen ab und weisen formal nach, dass ein zweites,
strukturell unabhÃ¤ngiges Beispiel â€” die \emph{Gleitkommadarstellung reeller
Zahlen} nach IEEE~754 \cite{ieee754} â€” demselben Prinzip genÃ¼gt.

\subsection{Formulierung des allgemeinen Prinzips}

\begin{definition}[Reziprok trennbare Kodierung]
\label{def:rtk}
	Sei $X$ eine Menge von â€žkombinierten Objekten`` und seien $J$, $R$
	Mengen von â€žSkaleninformation`` bzw. â€žForminformation``. Eine Familie
	von Kodierungsfunktionen
	\[
		\sigma^{(p)} : J \times R \longrightarrow \mathbb{Q}_{\geq 0},
		\qquad p \in P
	\]
	(parametrisiert durch einen \emph{Skalenparameter} $p$ aus einer
	Parametermenge $P$, z.\,B. $P=\mathbb{N}$) heiÃŸt
	\emph{reziprok trennbar}, wenn es eine Funktion $w:P\to\mathbb{R}_{>0}$
	mit $w(p)\to\infty$ fÃ¼r $p\to\infty$ gibt, sodass fÃ¼r alle $p\in P$,
	$j\in J$, $r\in R$ gilt:
	\begin{enumerate}[label=(\roman*)]
		\item $\sigma^{(p)}(j,r) = \rho(r) + \chi(j)\cdot w(p)$ fÃ¼r feste
		      Funktionen $\rho:R\to[0, w(p))$ und $\chi:J\to\mathbb{Z}_{\geq0}$
		      (additive Zerlegung mit Gewicht $w(p)$),
		\item die Skalierung $\hat\sigma^{(p)}(j,r) := \sigma^{(p)}(j,r)\cdot w(p)^{-1}$
		      erfÃ¼llt
		      \[
			      \lfloor \hat\sigma^{(p)}(j,r) \rfloor = \chi(j)
			      \quad\text{und}\quad
			      \{\hat\sigma^{(p)}(j,r)\} = \rho(r)/w(p),
		      \]
		\item die Abbildung $(j,r)\mapsto(\chi(j),\rho(r)/w(p))$ ist eine
		      Bijektion auf ihr Bild, und
		\item das Bild $\{\rho(r)/w(p) : r\in R\}$ ist als Teilmenge von
		      $[0,1)$ \emph{unabhÃ¤ngig von $p$} (bis auf Umskalierung der
		      Diskretisierung).
	\end{enumerate}
\end{definition}

\begin{satz}[Reziprokes Trennungsprinzip]
\label{satz:rtp}
	Sei $\sigma^{(p)}$ eine reziprok trennbare Kodierung im Sinne von
	Definition~\ref{def:rtk}. Dann gelten die folgenden drei Aussagen, die
	wir als \emph{paradoxen Gewinn} bezeichnen:
	\begin{enumerate}[label=(G\arabic*)]
		\item \textbf{(Verlustfreiheit trotz Skalendivergenz)} Obwohl
		      $w(p)\to\infty$ fÃ¼r $p\to\infty$ und damit
		      $\sigma^{(p)}(j,r)\to\infty$ fÃ¼r festes $j>0$, bleibt die durch
		      $w(p)^{-1}$ skalierte GrÃ¶ÃŸe $\hat\sigma^{(p)}(j,r)$ in einem
		      Bereich, dessen Fraktionalteil-Wertebereich $[0,1)$ \emph{nicht}
		      mit $p$ wÃ¤chst (Korollar zu (iv)).
		\item \textbf{(Kostenfreie Trennung)} Die Berechnung von $\chi(j)$ und
		      $\rho(r)/w(p)$ aus $\sigma^{(p)}(j,r)$ erfordert genau eine
		      Multiplikation (mit $w(p)^{-1}$) und eine
		      Floor-/Fractional-Operation â€” unabhÃ¤ngig von $p$, also $O(1)$.
		\item \textbf{(Kompressionsgewinn)} Falls $J$ und $R$ in getrennten
		      Datenstrukturen mit einem $p$-unabhÃ¤ngigen Overhead $\Delta>0$
		      kodiert wÃ¼rden, ist die kombinierte Kodierung $\sigma^{(p)}$ fÃ¼r
		      hinreichend strukturierte $\rho,\chi$ um genau $\Delta$ Bit
		      bzw. Speichereinheiten gÃ¼nstiger.
	\end{enumerate}
\end{satz}

\begin{proof}
	\textbf{(G1):} Nach (i) gilt $\hat\sigma^{(p)}(j,r) = \rho(r)/w(p) + \chi(j)$.
	Nach Voraussetzung $\rho(r)\in[0,w(p))$, also $\rho(r)/w(p)\in[0,1)$ fÃ¼r
	\emph{jedes} $p$. Damit ist $\{\hat\sigma^{(p)}(j,r)\} = \rho(r)/w(p)\in[0,1)$
	fÃ¼r alle $p\in P$ â€” der Wertebereich des Fraktionalanteils ist somit
	$[0,1)$, eine von $p$ unabhÃ¤ngige Menge. Dies gilt, obwohl
	$\sigma^{(p)}(j,r) = \rho(r)+\chi(j)w(p) \geq \chi(j)w(p) \to \infty$ fÃ¼r
	$\chi(j)>0$ und $p\to\infty$. Dies ist exakt die Aussage von
	Korollar~\ref{kor:konstanz} im allgemeinen Rahmen.

	\textbf{(G2):} Nach (ii) ist
	$\chi(j) = \lfloor \sigma^{(p)}(j,r)\cdot w(p)^{-1}\rfloor$ und
	$\rho(r)/w(p) = \{\sigma^{(p)}(j,r)\cdot w(p)^{-1}\}$. Beide GrÃ¶ÃŸen
	werden durch dieselbe arithmetische Operationsfolge â€” eine
	Multiplikation mit der Konstanten $w(p)^{-1}$, gefolgt von Floor bzw.
	Subtraktion des Floor-Werts â€” berechnet. Diese Operationsfolge hat fÃ¼r
	jedes feste $p$ konstante LÃ¤nge, also $O(1)$ in der GrÃ¶ÃŸe der Eingabe
	$\sigma^{(p)}(j,r)$.

	\textbf{(G3):} Nach (iii) ist $(j,r)\mapsto(\chi(j),\rho(r)/w(p))$
	bijektiv auf ihr Bild $X^{(p)} := \chi(J)\times(\rho(R)/w(p))$. Die
	Anzahl der zur Kodierung von $X^{(p)}$ nÃ¶tigen Bits ist somit
	$\lceil\log_2|X^{(p)}|\rceil = \lceil\log_2(|\chi(J)|\cdot|\rho(R)/w(p)|)\rceil$.
	Eine getrennte Kodierung von $\chi(j)\in\chi(J)$ und
	$\rho(r)/w(p)\in\rho(R)/w(p)$ in zwei Feldern benÃ¶tigt
	$\lceil\log_2|\chi(J)|\rceil + \lceil\log_2|\rho(R)/w(p)|\rceil + \Delta$
	Bit, wobei $\Delta>0$ den strukturellen Overhead (Padding,
	Wortgrenzen-Ausrichtung) bezeichnet. Da
	$\lceil\log_2(ab)\rceil \leq \lceil\log_2 a\rceil + \lceil\log_2 b\rceil$
	fÃ¼r alle $a,b\geq1$ (SubadditivitÃ¤t des Logarithmus unter Aufrundung),
	ist die kombinierte Kodierung um mindestens $\Delta$ Bit gÃ¼nstiger.
	\qedhere
\end{proof}

\begin{bemerkung}
\label{bem:instanz}
	Die Signaturfunktion $\sigma_j = r_j + j\cdot 2^n$ des Subgraph
	Algorithmus ist eine Instanz von Definition~\ref{def:rtk} mit
	$P=\mathbb{N}$, $p=n$, $w(n)=2^n$, $J=\{0,\ldots,n-1\}$,
	$R=\{0,1\}^n$, $\chi(j)=j$ und $\rho(A_{\cdot j}) = r_j$. Satz~\ref{satz:rtp}
	spezialisiert sich in diesem Fall exakt zu
	Satz~\ref{satz:zerlegung}, Korollar~\ref{kor:konstanz} und
	Satz~\ref{satz:infoerhalt}.
\end{bemerkung}

\subsection{Schlussfolgerung: Ein Erkennungskriterium fÃ¼r â€žparadoxe Gewinne``}

Aus Satz~\ref{satz:rtp} ergibt sich unmittelbar die folgende methodische
Schlussfolgerung, die Ã¼ber den Subgraph Algorithmus hinausweist:

\begin{korollar}[Erkennungskriterium]
\label{kor:erkennung}
	Immer wenn eine Kodierung zweier Informationsklassen $J$ (Skala/Position)
	und $R$ (Form/Inhalt) additiv mit einem exponentiell in einem Parameter
	$p$ wachsenden Gewicht $w(p)$ kombiniert wird,
	$\sigma^{(p)} = \rho(r) + \chi(j)\cdot w(p)$ mit $\rho(r) < w(p)$ fÃ¼r alle
	$p$, tritt notwendig der in Satz~\ref{satz:rtp} beschriebene
	dreifache Gewinn (G1)â€“(G3) auf â€” \emph{unabhÃ¤ngig davon}, wie groÃŸ
	$w(p)$ wird, und insbesondere \emph{ohne dass} die GrÃ¶ÃŸe von $w(p)$
	selbst jemals explizit gespeichert oder verglichen werden muss
	(es genÃ¼gt, dass $w(p)^{-1}$ als Skalierungskonstante bekannt ist).
\end{korollar}

\begin{proof}
	Unmittelbare Folgerung aus Satz~\ref{satz:rtp}, da die im Korollar
	genannte Voraussetzung â€” additive Zerlegung mit $\rho(r)<w(p)$ â€” exakt
	Bedingung (i) aus Definition~\ref{def:rtk} ist, und (ii)--(iv) fÃ¼r
	$\chi(j)=j$, $\rho=$ identische Restklassenfunktion automatisch erfÃ¼llt
	sind, sofern $\rho$ surjektiv auf $\{0,\ldots,\lceil w(p)\rceil-1\}$
	abbildet (was fÃ¼r ganzzahlige $\rho(r)\in[0,w(p))$ stets der Fall ist).
\end{proof}

\begin{bemerkung}[Neue Erkenntnis]
	Korollar~\ref{kor:erkennung} liefert ein \emph{prÃ¼fbares Kriterium}: Um
	zu entscheiden, ob ein gegebenes Kodierungsschema einen â€žparadoxen
	Gewinn`` im Sinne dieser Arbeit aufweist, genÃ¼gt es, die additive Form
	$\sigma = \rho(r)+\chi(j)\cdot w(p)$ mit der Schranke $\rho(r)<w(p)$
	nachzuweisen â€” alle weiteren Eigenschaften (G1)â€“(G3) folgen dann
	\emph{automatisch}. Dies reduziert die Suche nach weiteren Instanzen des
	PhÃ¤nomens auf eine rein syntaktische Inspektion von Kodierungsformeln.
	Im folgenden Abschnitt wenden wir dieses Kriterium auf ein zweites,
	von Graphen und Adjazenzmatrizen vollstÃ¤ndig unabhÃ¤ngiges Beispiel an.
\end{bemerkung}

\subsection{Zweites Beispiel: Gleitkommadarstellung nach IEEE~754}
\label{subsec:ieee754}

Wir zeigen nun, dass die \emph{normalisierte Gleitkommadarstellung} reeller
Zahlen, wie sie im Standard IEEE~754 \cite{ieee754} sowie in der klassischen
Darstellung bei Knuth \cite{knuth1997} beschrieben wird, eine reziprok
trennbare Kodierung im Sinne von Definition~\ref{def:rtk} ist â€” mit dem
Exponenten $E$ in der Rolle des Spaltenindex $j$ und der Mantisse $M$ in der
Rolle des Zeilenvektors $A_{\cdot j}$.

\begin{definition}[Normalisierte Gleitkommazahl]
\label{def:floatdarstellung}
	Eine positive reelle Zahl $x\in\mathbb{R}_{>0}$ besitzt fÃ¼r jede Basis
	$b\geq2$ eine eindeutige \emph{normalisierte Darstellung}
	\[
		x = M \cdot b^{E}, \qquad M\in[1,b),\ E\in\mathbb{Z}.
	\]
	$E$ heiÃŸt \emph{Exponent} (Skaleninformation), $M$ heiÃŸt
	\emph{Mantisse} oder \emph{Signifikand} (Forminformation).
\end{definition}

\begin{lemma}[Existenz und Eindeutigkeit der normalisierten Darstellung]
\label{lem:floatexistenz}
	FÃ¼r jedes $x\in\mathbb{R}_{>0}$ und jede Basis $b\geq2$ existieren
	eindeutige $E\in\mathbb{Z}$ und $M\in[1,b)$ mit $x=M\cdot b^E$, nÃ¤mlich
	\[
		E = \lfloor \log_b x \rfloor, \qquad M = x\cdot b^{-E}.
	\]
\end{lemma}

\begin{proof}
	\textbf{Existenz:} Setze $E:=\lfloor\log_b x\rfloor\in\mathbb{Z}$ (wohldefiniert,
	da $x>0$ und $\log_b$ auf $\mathbb{R}_{>0}$ definiert ist) und
	$M:=x\cdot b^{-E}$. Dann gilt
	$\log_b M = \log_b x - E = \log_b x - \lfloor\log_b x\rfloor = \{\log_b x\}\in[0,1)$,
	also $M = b^{\{\log_b x\}}\in[b^0,b^1)=[1,b)$. Damit gilt $x=M\cdot b^E$
	mit $M\in[1,b)$, $E\in\mathbb{Z}$.

	\textbf{Eindeutigkeit:} Angenommen, es gelte zugleich
	$x=M\cdot b^E=M'\cdot b^{E'}$ mit $M,M'\in[1,b)$, $E,E'\in\mathbb{Z}$
	und $(M,E)\neq(M',E')$. Ohne EinschrÃ¤nkung sei $E<E'$, also $E'\geq E+1$.
	Aus $M\cdot b^E=M'\cdot b^{E'}$ folgt $M = M'\cdot b^{E'-E}\geq M'\cdot b\geq b$
	(da $M'\geq1$ und $E'-E\geq1$), im Widerspruch zu $M<b$. Also $E=E'$, und
	wegen $b^E>0$ folgt unmittelbar $M=M'$. \qedhere
\end{proof}

\begin{satz}[IEEE-754-Gleitkommadarstellung als reziprok trennbare Kodierung]
\label{satz:ieee_rtk}
	FÃ¼r $b=2$ ist die durch Lemma~\ref{lem:floatexistenz} gegebene
	Zerlegung $x\mapsto(E,M)$ mit $M\in[1,2)$, $E\in\mathbb{Z}$ eine
	reziprok trennbare Kodierung im Sinne von Definition~\ref{def:rtk},
	mit Skalenparameter $p:=|E|_{\max}$ (maximaler darstellbarer
	Exponentenbetrag, in IEEE~754 double durch $11$ Exponentenbits auf
	$|E|\leq1023$ begrenzt \cite{ieee754}) und Gewichtsfunktion
	$w(p)=2^{p}$.
\end{satz}

\begin{proof}
	Wir prÃ¼fen die vier Bedingungen aus Definition~\ref{def:rtk} mit
	$J:=\{-p,\ldots,p\}\ni\chi(x):=E$, $R:=[1,2)\ni\rho(x):=(M-1)\cdot 2^{p}$
	(skaliert, damit $\rho(r)\in[0,2^p)=[0,w(p))$ gilt, vgl. (i)), und
	$\sigma^{(p)}(E,M) := (M-1)\cdot2^p + (E+p)\cdot2^p$.

	\textbf{(i) Additive Zerlegung:} Nach Konstruktion ist
	$\sigma^{(p)}(E,M) = \underbrace{(M-1)\cdot2^p}_{=\rho(r)\in[0,2^p)}
	+ \underbrace{(E+p)}_{=\chi(j)\in\mathbb{Z}_{\geq0}}\cdot\underbrace{2^p}_{=w(p)}$,
	also von der Form (i) mit $\rho(r)\in[0,w(p))$, da $M-1\in[0,1)$ nach
	Lemma~\ref{lem:floatexistenz} (fÃ¼r $b=2$) und somit $(M-1)2^p\in[0,2^p)$.
	Die Verschiebung $E\mapsto E+p\geq0$ ist nÃ¶tig, damit $\chi(j)\in\mathbb{Z}_{\geq0}$
	gilt (vgl. die analoge Rolle von $j\geq0$ in der Subgraph-Signatur);
	sie ist eine Bijektion $\{-p,\ldots,p\}\to\{0,\ldots,2p\}$ und Ã¤ndert
	nichts an der nachfolgenden Argumentation.

	\textbf{(ii) Reziproke Skalierung:} FÃ¼r
	$\hat\sigma^{(p)} := \sigma^{(p)}(E,M)\cdot 2^{-p}
	= (M-1) + (E+p)$ gilt analog zu Satz~\ref{satz:zerlegung}, da
	$M-1\in[0,1)$ und $E+p\in\mathbb{Z}_{\geq0}$:
	\[
		\lfloor\hat\sigma^{(p)}\rfloor = E+p, \qquad
		\{\hat\sigma^{(p)}\} = M-1.
	\]
	Damit ist $\chi(j)=\lfloor\hat\sigma^{(p)}\rfloor-p$ (Exponent) und
	$\rho(r)/w(p) = \{\hat\sigma^{(p)}\}=M-1$ (Mantissenanteil) jeweils in
	$O(1)$ aus $\hat\sigma^{(p)}$ rekonstruierbar â€” dies ist exakt die in
	Hardware implementierte Operation des â€žAuseinandernehmens`` einer
	Gleitkommazahl in Vorzeichen-, Exponenten- und Mantissenbits, die in
	jeder modernen FPU als Festverdrahtung in $O(1)$ (konstanter
	Taktzykluszahl) realisiert ist.

	\textbf{(iii) BijektivitÃ¤t:} Nach Lemma~\ref{lem:floatexistenz} ist
	$x\mapsto(E,M)$ eine Bijektion $\mathbb{R}_{>0}\to\mathbb{Z}\times[1,2)$
	(eingeschrÃ¤nkt auf $|E|\leq p$ und auf die durch die Mantissenbreite
	$m$ diskretisierten Werte $M\in\{1,1+2^{-m},\ldots,2-2^{-m}\}$ eine
	Bijektion auf eine endliche Menge der GrÃ¶ÃŸe $(2p+1)\cdot2^m$). Die
	Komposition mit der affinen, bijektiven Abbildung
	$(E,M)\mapsto(E+p,(M-1))$ ist somit ebenfalls bijektiv.

	\textbf{(iv) $p$-UnabhÃ¤ngigkeit des Fraktionalanteil-Bildbereichs:}
	Der Bildbereich von $\rho(r)/w(p)=M-1$ ist $[0,1)$ fÃ¼r \emph{jedes}
	$p$ (bzw. fÃ¼r jede Exponentenbreite), da $M\in[1,2)$ nach
	Lemma~\ref{lem:floatexistenz} unabhÃ¤ngig von der konkreten GrÃ¶ÃŸe von
	$E$ bzw. $p$ gilt. \qedhere
\end{proof}

\begin{korollar}[Paradoxer Gewinn in der Gleitkommadarstellung]
\label{kor:floatparadox}
	Nach Satz~\ref{satz:rtp}, angewendet auf die in
	Satz~\ref{satz:ieee_rtk} nachgewiesene Instanz, gelten fÃ¼r die
	IEEE-754-Gleitkommadarstellung die folgenden konkreten Aussagen:
	\begin{enumerate}[label=(F\arabic*)]
		\item \textbf{(G1, Verlustfreiheit):} Obwohl der Wertebereich von
		      $x=M\cdot2^E$ fÃ¼r wachsende $|E|$ Ã¼ber $2^{1023}\approx10^{308}$
		      bzw. unter $2^{-1022}\approx10^{-308}$ hinausgeht â€” also um
		      \emph{Hunderte GrÃ¶ÃŸenordnungen} variiert â€”, bleibt der
		      Mantissenanteil $M-1$ stets im \emph{exakt gleichen} Intervall
		      $[0,1)$ mit \emph{exakt gleicher} Diskretisierung
		      ($2^{52}$ Stufen bei \texttt{double}), unabhÃ¤ngig vom Exponenten.
		\item \textbf{(G2, $O(1)$-Trennung):} Vorzeichen, Exponent und
		      Mantisse werden aus dem $64$-Bit-Wort einer
		      \texttt{double}-Zahl durch genau drei
		      Bitmasken-/Shift-Operationen extrahiert â€” unabhÃ¤ngig davon, ob
		      $|E|$ nahe $0$ oder nahe $1023$ liegt.
		\item \textbf{(G3, Kompressionsgewinn):} Eine \texttt{double}-Zahl
		      benÃ¶tigt $64$ Bit ($1$ Vorzeichen-, $11$ Exponenten-, $52$
		      Mantissenbit) fÃ¼r die kombinierte Kodierung von $E\in\{-1022,\ldots,1023\}$
		      und $M\in[1,2)$ (diskretisiert in $2^{52}$ Stufen). Eine
		      getrennte Speicherung von $E$ (als \texttt{int16}, $16$ Bit)
		      und $M$ (als $52$-Bit-Festkommazahl, aufgerundet auf $56$ Bit
		      wegen Byte-Ausrichtung) plus Vorzeichenbit benÃ¶tigte
		      $16+56+1=73$ Bit â€” also $\Delta=9$ Bit mehr pro Zahl als die
		      kombinierte IEEE-754-Darstellung.
	\end{enumerate}
\end{korollar}

\begin{proof}
	(F1) folgt aus (G1) (Satz~\ref{satz:rtp}) mit $w(p)=2^p$,
	$p=1023$: Der Fraktionalanteil $\{\hat\sigma^{(p)}\}=M-1\in[0,1)$ ist
	nach Definition unabhÃ¤ngig von $p$, wÃ¤hrend
	$\sigma^{(p)}(E,M)=(M-1)2^p+(E+p)2^p$ fÃ¼r $E\to1023$ Werte der
	GrÃ¶ÃŸenordnung $2^{1023}\cdot2^{1023}$ erreicht â€” eine Diskrepanz von
	mehr als $600$ Dezimalstellen zwischen der GrÃ¶ÃŸe von $\sigma^{(p)}$ und
	der GrÃ¶ÃŸe seines (immer in $[0,1)$ liegenden) Fraktionalanteils.

	(F2) folgt aus (G2): Die in Satz~\ref{satz:ieee_rtk}(ii) gezeigte
	Operationsfolge â€” Multiplikation mit $2^{-p}$ (Ã¤quivalent: Bitshift um
	$p$ Stellen, da $2^p$ eine Zweierpotenz ist), gefolgt von Floor und
	Subtraktion â€” entspricht exakt der Bitmasken-Operation
	\texttt{(bits >> 52) \& 0x7FF} (Exponent) bzw.
	\texttt{bits \& 0xFFFFFFFFFFFFF} (Mantisse) auf der $64$-Bit-ReprÃ¤sentation,
	beides $O(1)$-Operationen unabhÃ¤ngig vom Wert von $E$.

	(F3) folgt aus (G3) durch explizite BitzÃ¤hlung: $|\chi(J)|=2047$
	(Werte fÃ¼r $E\in\{-1022,\ldots,1024\}$, davon $2$ fÃ¼r Spezialwerte
	reserviert, vgl. \cite{ieee754}), kodierbar in $\lceil\log_2 2047\rceil=11$
	Bit; $|\rho(R)/w(p)|=2^{52}$, kodierbar in $52$ Bit; zusammen mit
	einem Vorzeichenbit also $1+11+52=64$ Bit fÃ¼r die kombinierte
	Darstellung â€” exakt die GrÃ¶ÃŸe eines \texttt{double}. Die getrennte
	Variante mit Byte-Ausrichtung ($16$-Bit-\texttt{int16} fÃ¼r $E$ statt
	$11$ Bit, $56$-Bit-Feld fÃ¼r $M$ statt $52$ Bit, plus $1$ Vorzeichenbit)
	ergibt $16+56+1=73>64$ Bit, also $\Delta=9$ Bit Mehrbedarf â€” in
	Ãœbereinstimmung mit der allgemeinen Aussage (G3), dass
	$\Delta>0$ fÃ¼r jede Kodierung mit Ausrichtungs-Overhead gilt. \qedhere
\end{proof}

\begin{bemerkung}[Tiefere Analogie und neue Erkenntnis]
\label{bem:tiefeanalogie}
	Die Analogie zwischen Subgraph-Signatur und IEEE-754-Float geht Ã¼ber die
	rein strukturelle Ãœbereinstimmung hinaus: In \emph{beiden} FÃ¤llen
	kodiert die \emph{Spaltenkomponente} bzw. der \emph{Exponent} eine
	\emph{diskrete Position} in einer geordneten Menge ($j\in\{0,\ldots,n-1\}$
	bzw. $E\in\{-1022,\ldots,1023\}$), wÃ¤hrend die \emph{Zeilenkomponente}
	bzw. die \emph{Mantisse} eine \emph{kontinuierliche bzw.
	hochauflÃ¶sende Forminformation} kodiert ($A_{\cdot j}\in\{0,1\}^n$ bzw.
	$M\in[1,2)$). Die neue Erkenntnis aus dieser Arbeit ist, dass dieses
	Muster â€” \emph{Position-mal-Skala plus beschrÃ¤nkte Form} â€” ein
	\textbf{universelles Entwurfsmuster fÃ¼r verlustfreie, $O(1)$-trennbare
	Kodierungen} darstellt, das unabhÃ¤ngig vom konkreten Anwendungsgebiet
	(Graphentheorie, numerische Datentypen) durch das in
	Korollar~\ref{kor:erkennung} formulierte syntaktische Kriterium erkannt
	werden kann. Insbesondere legt dies nahe, dass \emph{jede} Kodierung,
	die historisch unabhÃ¤ngig voneinander auf die Form
	$\sigma=\rho(r)+\chi(j)\cdot w(p)$ mit $\rho(r)<w(p)$ konvergiert ist
	(wie es sowohl bei der Subgraph-Signatur von 2026 als auch bei der
	IEEE-754-Norm von 1985 der Fall war), \emph{notwendigerweise} ein
	lokales Optimum bezÃ¼glich (G1)â€“(G3) realisiert â€” ein Resultat, das
	weitere, noch unentdeckte Instanzen in anderen Bereichen
	(Datenkompression, Adressierungsschemata, Zeitstempelformate) vermuten
	lÃ¤sst.
\end{bemerkung}

\subsection{Veranschaulichung des zweiten Beispiels}

Abbildung~\ref{fig:plot5} veranschaulicht Satz~\ref{satz:ieee_rtk} und
Korollar~\ref{kor:floatparadox}(F1) anhand von $40$ logarithmisch breit
gestreuten Zufallswerten $x\in(0{,}001,\,10000)$, also Ã¼ber sieben
GrÃ¶ÃŸenordnungen. Der Exponent $E=\lfloor\log_2 x\rfloor$ (links) variiert
entsprechend stark und trÃ¤gt die gesamte â€žSkaleninformation`` der Zahl $x$.
Die Mantisse $M=x\cdot2^{-E}$ (rechts) bleibt dagegen fÃ¼r \emph{jeden} Wert
von $x$ â€” unabhÃ¤ngig davon, ob $x\approx0{,}001$ oder $x\approx10000$ â€” exakt
im Intervall $[1,2)$. Diese Konstanz des Mantissenbereichs ist das exakte
Analogon zu Abbildung~\ref{fig:plot2} (konstante Trennung des
Fraktionalanteils $r_j/2^n$ in $[0,1)$ unabhÃ¤ngig von $n$): In beiden FÃ¤llen
wird eine Ã¼ber viele GrÃ¶ÃŸenordnungen variierende GrÃ¶ÃŸe durch eine reziproke
Skalierung in eine Komponente mit \emph{konstantem, von der Skala
unabhÃ¤ngigem Wertebereich} Ã¼berfÃ¼hrt.

\begin{figure}[htbp]
	\centering
	\includegraphics[width=\textwidth]{plot5.pdf}
	\caption{Reziproke Zerlegung $x=M\cdot2^E$ fÃ¼r $40$ Zufallswerte
	$x\in(0{,}001,10000)$. Links: Exponent $E$ (Skaleninformation), stark
	streuend. Rechts: Mantisse $M\in[1,2)$ (Forminformation), fÃ¼r alle
	Werte im selben Intervall â€” unabhÃ¤ngig von der absoluten GrÃ¶ÃŸe von $x$.
	Dies ist das direkte Analogon zur konstanten Trennung in
	Abbildung~\ref{fig:plot2}.}
	\label{fig:plot5}
\end{figure}

\section{Zusammenfassung}

Die vorliegende Arbeit hat den scheinbar paradoxen Sachverhalt formal
nachgewiesen, dass die reziproke GrÃ¶ÃŸe $2^{-n}$ in der Signaturfunktion des
Subgraph Algorithmus
\begin{itemize}
	\item trotz exponentieller Konvergenz gegen $0$ keine Information
	      vernichtet (Satz~\ref{satz:zerlegung}),
	\item eine Bijektion zwischen Spaltenvektoren und Paaren
	      $(j, r_j/2^n)$ mit von $n$ unabhÃ¤ngigem Wertebereich des
	      Fraktionalanteils induziert (Lemma~\ref{lem:bijektiv},
	      Korollar~\ref{kor:konstanz}),
	\item den kombinierten Speicherbedarf gegenÃ¼ber getrennter Kodierung von
	      Position und Inhalt strikt reduziert (Satz~\ref{satz:infoerhalt},
	      Korollar~\ref{kor:paradox}),
	\item und eine notwendige Voraussetzung fÃ¼r die Erreichbarkeit der
	      $\Theta(n^3)$-OptimalitÃ¤t des Subgraph Algorithmus ist
	      (Satz~\ref{satz:laufzeitkonsequenz}).
\end{itemize}

Der â€žGewinn im Reziproken`` besteht somit darin, dass eine algebraisch
triviale Operation â€“ Multiplikation mit dem Kehrwert einer Zweierpotenz,
Ã¤quivalent zu einer Bitschiebeoperation â€“ eine strukturelle Entflechtung
zweier Informationsdimensionen bewirkt, deren getrennte Kodierung in
realen Systemen stets mit Mehraufwand verbunden wÃ¤re.

\subsection{Anwendungen}

Die hier bewiesenen Eigenschaften sind relevant fÃ¼r:
\begin{itemize}
	\item Kompakte Hashing-Schemata, in denen Position und Inhalt eines
	      Datensatzes in einem einzigen Maschinenwort kodiert werden sollen
	\item Bitfeld-basierte Datenstrukturen in eingebetteten Systemen, in
	      denen \emph{struct padding} vermieden werden muss
	\item Die Analyse weiterer polynomialer Hash-Funktionen mit
	      Zweierpotenz-Gewichten hinsichtlich BijektivitÃ¤t und
	      $O(1)$-Dekodierbarkeit
	\item Lehrmaterial zur Demonstration, dass â€žkleine`` reziproke GrÃ¶ÃŸen
	      in der Informatik strukturelle, nicht nur quantitative Bedeutung
	      haben kÃ¶nnen
\end{itemize}

\subsection{Ausblick}

Die in dieser Arbeit etablierten Resultate erÃ¶ffnen eine Reihe
weiterfÃ¼hrender Forschungsrichtungen, die im Folgenden ausfÃ¼hrlich
dargestellt werden.

\textbf{1. Verallgemeinerung auf beliebige Basen $b > 1$.}
Die gesamte Argumentation dieser Arbeit beruht auf der Basis $2$, was sowohl
mathematisch (BinÃ¤rdarstellung) als auch praktisch (Bitschiebeoperationen,
\texttt{shift}/\texttt{mask} in $O(1)$) motiviert ist. Eine naheliegende
Erweiterung besteht darin, die Signaturfunktion zu
$\sigma_j^{(b)} = \sum_{i=0}^{n-1}A_{ij}\cdot b^i + j\cdot b^n$ fÃ¼r beliebige
ganzzahlige Basen $b \geq 2$ zu verallgemeinern. Satz~\ref{satz:zerlegung}
und Lemma~\ref{lem:bijektiv} lassen sich strukturell unverÃ¤ndert auf diesen
Fall Ã¼bertragen, da $0 \leq r_j \leq b^n - 1$ und somit
$\hat\sigma_j^{(b)} = \sigma_j^{(b)}\cdot b^{-n} = j + r_j/b^n$ mit
$r_j/b^n\in[0,1)$ weiterhin gilt. Die entscheidende offene Frage ist jedoch,
ob die $O(1)$-Dekodierbarkeit (Satz~\ref{satz:laufzeitkonsequenz}) fÃ¼r $b\neq2^k$
erhalten bleibt: Division durch $b^n$ und Modulo $b^n$ sind fÃ¼r allgemeine
$b$ \emph{nicht} durch Bitschiebeoperationen realisierbar, sondern erfordern
echte Ganzzahldivision, deren Hardware-Latenz typischerweise hÃ¶her ist als
die von \texttt{shift}-Operationen, asymptotisch jedoch weiterhin $O(1)$
bleibt (fÃ¼r feste Wortbreite). Es wÃ¤re lohnend, empirisch zu untersuchen, ab
welcher Basis $b$ und welcher Knotenzahl $n$ dieser konstante, aber von Null
verschiedene Mehraufwand die Gesamtlaufzeit des Subgraph Algorithmus in der
Praxis (nicht asymptotisch) messbar beeinflusst, und ob es Basen $b$ gibt,
fÃ¼r die der â€žparadoxe Gewinn`` \emph{grÃ¶ÃŸer} ausfÃ¤llt als fÃ¼r $b=2$ â€“ etwa im
Hinblick auf eine dichtere Packung von Information bei nicht-binÃ¤ren
Adjazenzmatrizen (gewichtete Graphen mit Kantengewichten aus
$\{0,\ldots,b-1\}$).

\textbf{2. Mehrstufige reziproke Zerlegungen fÃ¼r hÃ¶herdimensionale
Strukturen.} Die in dieser Arbeit behandelte Zerlegung trennt genau zwei
Informationsdimensionen (Position $j$ und Inhalt $A_{\cdot j}$) durch genau
\emph{eine} reziproke Skalierung mit $2^{-n}$. FÃ¼r Tensoren hÃ¶herer Ordnung
â€“ etwa Hypergraphen mit $k$-Ã¤ren Relationen oder mehrschichtige
(\emph{multilayer}) Graphen â€“ stellt sich die Frage, ob eine
\emph{mehrstufige} Signatur der Form
\[
	\sigma_{j_1,\ldots,j_k} = r + \sum_{l=1}^{k} j_l \cdot 2^{n + \sum_{m<l} \log_2 N_m}
\]
(mit geeigneten Normierungskonstanten $N_m$) eine analoge Kette von
ineinander geschachtelten reziproken Zerlegungen
$\hat\sigma \cdot 2^{-n}, \{\hat\sigma\}\cdot N_1^{-1}, \ldots$ zulÃ¤sst, bei
der jede Stufe wieder Korollar~\ref{kor:konstanz} (Konstanz der Trennung,
unabhÃ¤ngig von den OriginalgrÃ¶ÃŸen) erfÃ¼llt. Eine vollstÃ¤ndige formale
Verallgemeinerung von Satz~\ref{satz:infoerhalt} auf $k$ Dimensionen mit
einem Beweis, dass auch hier jede Dekodierungsstufe in $O(1)$ erfolgt, steht
noch aus und wÃ¤re direkt relevant fÃ¼r die Erweiterung des Subgraph
Algorithmus auf gewichtete und gerichtete Graphen sowie auf Hypergraphen,
wie sie bereits als ErweiterungsmÃ¶glichkeit in \cite{epp2026subgraph}
genannt werden.

\textbf{3. Informationstheoretische Einordnung als Entropie-Zerlegung.}
Korollar~\ref{kor:paradox} wurde in dieser Arbeit operational (Bits,
Speicherplatz) formuliert. Eine tiefere informationstheoretische Fundierung
wÃ¼rde die Zerlegung $\sigma_j \mapsto (j, r_j)$ als Zerlegung der
gemeinsamen Entropie $H(J, R) = H(J) + H(R\mid J)$ interpretieren, wobei $J$
die Zufallsvariable des Spaltenindex und $R$ die Zufallsvariable der
Zeilenkomponente bezeichnet. Da $J$ und $R$ in der Konstruktion des
Subgraph Algorithmus stochastisch unabhÃ¤ngig gewÃ¤hlt werden kÃ¶nnen (der
Spaltenindex ist deterministisch durch die Position gegeben, der
Zeilenvektor ist die eigentliche â€žNutzinformation``), gilt
$H(J,R) = H(J) + H(R)$, und die reziproke Zerlegung realisiert diese additive
Entropiezerlegung \emph{architektonisch} â€“ nicht nur statistisch. Eine
formale Ausarbeitung dieser Sichtweise kÃ¶nnte den â€žparadoxen Gewinn`` mit dem
Begriff des \emph{kanonischen Codes} (im Sinne der Kolmogorov-KomplexitÃ¤t)
in Verbindung bringen: $\sigma_j$ wÃ¤re dann ein kanonischer, prÃ¤fixfreier
Code fÃ¼r das Paar $(j, A_{\cdot j})$, und die Frage, ob dieser Code in einem
geeigneten Sinn \emph{optimal} (d.\,h. von minimaler erwarteter LÃ¤nge) unter
allen $O(1)$-dekodierbaren Codes ist, wÃ¤re ein eigenstÃ¤ndiges
Forschungsthema.

\textbf{4. Numerische StabilitÃ¤t bei Gleitkomma-Implementierungen.} Diese
Arbeit hat die reziproke Skalierung $\sigma_j\cdot2^{-n}$ exakt in den
rationalen Zahlen $\mathbb{Q}$ behandelt (Satz~\ref{satz:zerlegung}). In
realen Implementierungen, insbesondere wenn Signaturen aus
Performance-GrÃ¼nden als \texttt{double}-Gleitkommazahlen statt als
beliebig genaue Ganzzahlen (\texttt{BigInteger}) reprÃ¤sentiert werden,
stellt sich die Frage der numerischen StabilitÃ¤t: FÃ¼r $n > 52$ Ã¼bersteigt
$2^n$ die Mantissenbreite eines IEEE-754-\texttt{double} ($53$ Bit), sodass
$\sigma_j$ und $2^{-n}$ nicht mehr exakt darstellbar sind und die in
Satz~\ref{satz:zerlegung} bewiesene exakte Zerlegung in Rundungsfehler
Ã¼bergeht. Eine systematische Fehleranalyse, die obere Schranken fÃ¼r den
Rundungsfehler von $\lfloor\hat\sigma_j\rfloor$ und $\{\hat\sigma_j\}$ in
AbhÃ¤ngigkeit von $n$ und der Mantissenbreite $p$ angibt, sowie ein
Vorschlag fÃ¼r eine numerisch stabile Variante (etwa durch Verwendung von
\texttt{shift}/\texttt{mask}-Operationen auf Ganzzahlen anstelle von
Gleitkomma-Multiplikation mit $2^{-n}$, wie in
Satz~\ref{satz:laufzeitkonsequenz} bereits angedeutet), wÃ¤re fÃ¼r
Implementierungen des Subgraph Algorithmus fÃ¼r sehr groÃŸe Graphen
($n>10000$, vgl. \cite{epp2026subgraph}, Abschnitt â€žAusblick``) von
unmittelbarer praktischer Relevanz.

\textbf{5. Empirische Validierung des Speichergewinns in realen
Systemen.} Abbildung~\ref{fig:plot3} illustriert den theoretischen
Speichergewinn unter der Annahme eines pauschalen Overheads von $32$ Bit
fÃ¼r getrennte Felder. Eine empirische Studie, die den tatsÃ¤chlichen
Speicherverbrauch konkreter Implementierungen (z.\,B. in C/C++ mit
\texttt{struct}-Definitionen unter verschiedenen Compilern und
Architekturen, sowie in Python mit \texttt{NumPy}-Arrays vs. Tupellisten)
fÃ¼r $A$ und $j$ getrennt gegenÃ¼ber der kombinierten Signatur $\sigma_j$
misst, wÃ¼rde die theoretischen Schranken aus
Satz~\ref{satz:infoerhalt} empirisch untermauern und gegebenenfalls
architekturspezifische Korrekturfaktoren liefern.

\textbf{6. Anwendung auf das ChatMe-Signatur-Chiffre-Schema.} Da der
Autor in verwandten Arbeiten ein Signatur-Chiffre-Verfahren fÃ¼r
verschlÃ¼sselte Kommunikation entwickelt, liegt es nahe zu untersuchen, ob
die hier bewiesene reziproke Zerlegung auch dort zur kompakten Kodierung
von SchlÃ¼ssel- und Nachrichtenanteilen in einem einzigen Wert verwendet
werden kann, analog zur Trennung von Spaltenindex und Zeileninformation in
$\sigma_j$. Eine solche Ãœbertragung wÃ¼rde jedoch eine sorgfÃ¤ltige
kryptographische Analyse erfordern, da die hier bewiesene BijektivitÃ¤t
(Lemma~\ref{lem:bijektiv}) zwar Verlustfreiheit garantiert, aber keinerlei
Aussage Ã¼ber kryptographische Sicherheitseigenschaften wie
Kollisionsresistenz gegenÃ¼ber einem Angreifer mit Kenntnis von $n$ macht â€“
im Gegenteil, die \emph{vollstÃ¤ndige Determiniertheit} der Zerlegung kÃ¶nnte
in einem kryptographischen Kontext eine SchwÃ¤che darstellen, die in einer
gesonderten Arbeit analysiert werden mÃ¼sste.

Zusammenfassend zeigt diese Arbeit, dass die scheinbar triviale
algebraische Beziehung zwischen $2^n$ und $2^{-n}$ in der Signaturfunktion
des Subgraph Algorithmus eine reichhaltige formale Struktur trÃ¤gt, deren
vollstÃ¤ndige AusschÃ¶pfung â€“ von allgemeinen Basen Ã¼ber
informationstheoretische Fundierung bis hin zu kryptographischen
Anwendungen â€“ Gegenstand zukÃ¼nftiger Forschung sein sollte.

\subsection{Implementierungshinweis}

Der Code des Subgraph Algorithmus, auf dem diese Arbeit aufbaut, ist
verfÃ¼gbar unter: \url{https://github.com/hjstephan86/science}.

\begin{thebibliography}{9}

\bibitem{epp2026subgraph}
Epp, S.: \emph{Der Subgraph Algorithmus}. Eigenverlag, 2026.

\bibitem{abboud2015}
Abboud, A., Backurs, A., Vassilevska Williams, V.:
\emph{Tight Hardness Results for LCS and Other Sequence Similarity Measures}.
In: Proceedings of the 56th Annual IEEE Symposium on Foundations of
Computer Science (FOCS), 2015, S. 59--78.

\bibitem{cormen2009}
Cormen, T. H., Leiserson, C. E., Rivest, R. L., Stein, C.:
\emph{Introduction to Algorithms}. 3. Auflage, MIT Press, 2009.

\bibitem{knuth1997}
Knuth, D. E.: \emph{The Art of Computer Programming, Volume 2:
Seminumerical Algorithms}. 3. Auflage, Addison-Wesley, 1997.

\bibitem{ieee754}
IEEE Standards Association: \emph{IEEE Standard for Floating-Point
Arithmetic (IEEE 754-2019)}. 2019.

\bibitem{shannon1948}
Shannon, C. E.: \emph{A Mathematical Theory of Communication}.
Bell System Technical Journal, 27(3), 1948, S. 379--423.

\bibitem{cover2006}
Cover, T. M., Thomas, J. A.: \emph{Elements of Information Theory}.
2. Auflage, Wiley-Interscience, 2006.

\end{thebibliography}

\end{document}

